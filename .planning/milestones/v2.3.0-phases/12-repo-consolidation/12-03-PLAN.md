---
phase: 12-repo-consolidation
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified: []
autonomous: false
requirements:
  - REPO-03
  - REPO-04

must_haves:
  truths:
    - "winget-storcat repo is archived (read-only)"
    - "winget-storcat README links to packaging/winget/ in main repo"
    - "homebrew-storcat README indicates the tap is auto-managed and links to main repo"
    - "homebrew-storcat repo is NOT archived (must remain live for brew tap)"
  artifacts: []
  key_links:
    - from: "scottkw/winget-storcat README"
      to: "packaging/winget/ in scottkw/storcat"
      via: "README link pointing users to main repo"
      pattern: "packaging/winget"
    - from: "scottkw/homebrew-storcat README"
      to: "packaging/homebrew/ in scottkw/storcat"
      via: "README link indicating auto-managed status"
      pattern: "auto-managed|main repo"
---

<objective>
Archive the winget-storcat satellite repo and update the homebrew-storcat README to indicate auto-managed status. Both satellite READMEs should direct users to the main StorCat repo for the source of truth.

Purpose: Complete the consolidation by archiving the now-redundant winget-storcat repo (REPO-03) and marking homebrew-storcat as auto-managed (REPO-04). This is the final step that depends on Plans 01 and 02 having successfully migrated all files.
Output: winget-storcat archived, both satellite READMEs updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-repo-consolidation/12-RESEARCH.md
@.planning/phases/12-repo-consolidation/12-01-SUMMARY.md
@.planning/phases/12-repo-consolidation/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update winget-storcat README and archive the repo</name>
  <read_first>
    .planning/phases/12-repo-consolidation/12-RESEARCH.md
    packaging/winget/manifests/s/scottkw/StorCat/2.1.0/scottkw.StorCat.yaml
  </read_first>
  <action>
    IMPORTANT: Verify the migration was successful BEFORE archiving. Run:
    `ls packaging/winget/manifests/s/scottkw/StorCat/2.1.0/scottkw.StorCat.yaml`
    If this file does not exist, STOP and report the issue. Do NOT proceed to archive.

    1. Clone winget-storcat to update its README before archiving:
       `gh repo clone scottkw/winget-storcat /tmp/winget-storcat-archive`

    2. Overwrite `/tmp/winget-storcat-archive/README.md` with:

    ```markdown
    # winget-storcat (Archived)

    > **This repository has been archived.** WinGet manifests for StorCat are now maintained in the main repository.

    ## Where to find WinGet manifests

    All WinGet manifest files have been moved to:
    **[scottkw/storcat/packaging/winget/](https://github.com/scottkw/storcat/tree/main/packaging/winget)**

    ## StorCat

    StorCat is a cross-platform desktop application and CLI tool for creating, browsing, and searching directory catalogs. Built with Go and Wails.

    - **Main repository:** [github.com/scottkw/storcat](https://github.com/scottkw/storcat)
    - **Releases:** [github.com/scottkw/storcat/releases](https://github.com/scottkw/storcat/releases)
    ```

    3. Commit and push the README update:
       ```
       cd /tmp/winget-storcat-archive
       git add README.md
       git commit -m "docs: mark repo as archived, link to main repo packaging/winget/"
       git push origin main
       ```

    4. Archive the repo (IRREVERSIBLE via CLI — can only be undone via GitHub web UI):
       `gh repo archive scottkw/winget-storcat -y`

    5. Clean up: `rm -rf /tmp/winget-storcat-archive`

    CRITICAL: Step 4 (`gh repo archive`) is irreversible via CLI. This is intentional — the manifests are now in the main repo.
  </action>
  <verify>
    <automated>gh repo view scottkw/winget-storcat --json isArchived -q '.isArchived' | grep -q 'true' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `gh repo view scottkw/winget-storcat --json isArchived -q '.isArchived'` returns `true`
    - `gh api repos/scottkw/winget-storcat/contents/README.md -q '.content' | base64 -d | grep -q 'archived'` succeeds (README mentions archived)
    - `gh api repos/scottkw/winget-storcat/contents/README.md -q '.content' | base64 -d | grep -q 'packaging/winget'` succeeds (README links to main repo)
  </acceptance_criteria>
  <done>winget-storcat repo is archived with README linking to packaging/winget/ in main repo</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify winget-storcat archive is correct</name>
  <action>
    Pause for user to verify the irreversible archive operation completed correctly.
  </action>
  <what-built>
    Archived the scottkw/winget-storcat repository. This is an irreversible CLI operation (can only be undone via GitHub web UI at https://github.com/scottkw/winget-storcat/settings). The README was updated before archiving to point users to the main repo.
  </what-built>
  <how-to-verify>
    1. Visit https://github.com/scottkw/winget-storcat — confirm it shows "This repository has been archived" banner
    2. Confirm the README text mentions "archived" and links to scottkw/storcat/packaging/winget/
    3. If this was done in error, unarchive via: GitHub repo Settings > Danger Zone > Unarchive
  </how-to-verify>
  <verify>
    <automated>gh repo view scottkw/winget-storcat --json isArchived -q '.isArchived' | grep -q 'true' && echo "PASS"</automated>
  </verify>
  <done>User has confirmed winget-storcat archive is correct</done>
  <resume-signal>Type "approved" to continue to homebrew-storcat README update, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update homebrew-storcat README to indicate auto-managed status</name>
  <read_first>
    .planning/phases/12-repo-consolidation/12-RESEARCH.md
  </read_first>
  <action>
    1. Clone homebrew-storcat locally:
       `gh repo clone scottkw/homebrew-storcat /tmp/homebrew-storcat-update`

    2. Overwrite `/tmp/homebrew-storcat-update/README.md` with:

    ```markdown
    # homebrew-storcat

    Homebrew tap for [StorCat](https://github.com/scottkw/storcat) — a cross-platform directory catalog manager built with Go and Wails.

    ## Installation

    ```bash
    brew tap scottkw/storcat
    brew install --cask storcat
    ```

    ## About this tap

    > **This tap is auto-managed.** The cask is automatically updated when a new StorCat release is published. Do not edit `Casks/storcat.rb` manually — changes will be overwritten on the next release.

    The source of truth for packaging configuration is:
    **[scottkw/storcat/packaging/homebrew/](https://github.com/scottkw/storcat/tree/main/packaging/homebrew)**

    ## Links

    - **Main repository:** [github.com/scottkw/storcat](https://github.com/scottkw/storcat)
    - **Releases:** [github.com/scottkw/storcat/releases](https://github.com/scottkw/storcat/releases)
    - **Issues:** [github.com/scottkw/storcat/issues](https://github.com/scottkw/storcat/issues)

    ## Note

    The cask may reference an older version if a release is in progress. Run `brew update && brew upgrade storcat` to get the latest available version.
    ```

    3. Commit and push:
       ```
       cd /tmp/homebrew-storcat-update
       git add README.md
       git commit -m "docs: mark tap as auto-managed, link to main repo for source of truth"
       git push origin main
       ```

    4. Clean up: `rm -rf /tmp/homebrew-storcat-update`

    IMPORTANT: Do NOT archive homebrew-storcat. It must remain live because `brew tap scottkw/storcat` resolves to `github.com/scottkw/homebrew-storcat`. Archiving it would break `brew upgrade` for existing users. The repo also needs to accept push commits for Phase 15 automation.
  </action>
  <verify>
    <automated>gh api repos/scottkw/homebrew-storcat/contents/README.md -q '.content' | base64 -d | grep -qi 'auto-managed' && gh api repos/scottkw/homebrew-storcat/contents/README.md -q '.content' | base64 -d | grep -q 'main repo\|packaging/homebrew' && gh repo view scottkw/homebrew-storcat --json isArchived -q '.isArchived' | grep -q 'false' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/scottkw/homebrew-storcat/contents/README.md -q '.content' | base64 -d | grep -ci 'auto-managed'` returns at least 1
    - `gh api repos/scottkw/homebrew-storcat/contents/README.md -q '.content' | base64 -d | grep -c 'packaging/homebrew'` returns at least 1
    - `gh repo view scottkw/homebrew-storcat --json isArchived -q '.isArchived'` returns `false` (repo is NOT archived)
  </acceptance_criteria>
  <done>homebrew-storcat README updated to indicate auto-managed status, links to main repo packaging/homebrew/, and repo remains live (not archived)</done>
</task>

</tasks>

<verification>
- `gh repo view scottkw/winget-storcat --json isArchived -q '.isArchived'` returns `true`
- `gh repo view scottkw/homebrew-storcat --json isArchived -q '.isArchived'` returns `false`
- homebrew-storcat README contains "auto-managed" and links to main repo
- winget-storcat README contains "archived" and links to main repo
</verification>

<success_criteria>
REPO-03 satisfied: winget-storcat is archived with README redirecting to main repo.
REPO-04 satisfied: homebrew-storcat README states auto-managed status and links to main repo as source of truth. Repo remains live (not archived) for brew tap compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/12-repo-consolidation/12-03-SUMMARY.md`
</output>
