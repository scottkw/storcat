---
phase: 23-rail-virtualized-tree
plan: 04
type: execute
wave: 3
depends_on: ["23-01", "23-02", "23-03"]
files_modified:
  - frontend/src/components/workspace/CatalogRail.tsx
  - frontend/src/components/workspace/StatusBar.tsx
  - frontend/src/contexts/AppContext.tsx
autonomous: true
requirements: [RAIL-01, RAIL-02, RAIL-03, RAIL-04, RAIL-05, RAIL-06, STATE-01, SHELL-06]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every catalog in the configured directory appears in the rail as a two-line row carrying its title, its JSON size, its filename and its file count (RAIL-01)."
    - "Typing in the filter narrows the rail case-insensitively against title and filename together, and the tree pane does not re-render while the user types (RAIL-02)."
    - "Selecting a rail row loads that catalog's tree and clears the previous catalog's selection and expansion in one atomic action (RAIL-03)."
    - "A catalog that failed to parse shows a red status dot in its row, and the row stays clickable and selectable rather than becoming disabled (RAIL-04)."
    - "The status dot element is always present in the row, transparent when the catalog is healthy, so a broken catalog does not change any row's height (RAIL-04)."
    - "The rail header count always reports the total number of catalogs in the directory, never the filtered subset (RAIL-01)."
    - "The directory chip shows the configured catalog directory and opens the native folder picker; choosing a directory persists it and reloads the rail against it (RAIL-05)."
    - "The status bar reports live catalog count, indexed file count and total bytes, all summed from the same rail array the user is looking at, with no binding of its own (SHELL-06)."
    - "Byte totals are summed as integers and converted to gigabytes exactly once at display time; display-rounded values are never summed a second time (SHELL-06 precision, RAIL-01 precision)."
    - "The gigabyte-to-terabyte switch is inclusive at 1024 gigabytes, and a directory with no catalogs reads zero catalogs, zero files indexed and zero point zero gigabytes (SHELL-06 boundary)."
    - "A file count of zero renders as zero files and a JSON size of zero renders through the same formatter as any other size — neither is special-cased into a dash or an empty string (RAIL-01 boundary)."
    - "Two catalogs sharing a title both appear as separate rows, distinguished by the filename on their second line; nothing is merged or de-duplicated (RAIL-01 adjacency)."
    - "A directory containing no catalogs shows the empty-library state rather than an empty list, and so does a session with no directory configured yet (RAIL-01 empty, STATE-01)."
    - "A count the sidecar cache does not yet know renders as absent rather than as a confident zero, and appears on the next rail load once the cache warms; a cache fill running during a rail load can never make a row show a wrong number (RAIL-01 concurrency)."
    - "An empty or whitespace-only filter shows every catalog, and the filtered subset keeps the rail's underlying filename order rather than being re-sorted by relevance (RAIL-02 empty, RAIL-02 ordering)."
    - "A filter fragment matching one catalog by its title and another by its filename shows both (RAIL-02 adjacency)."
    - "The plus-New pill renders in its Phase 22 form and stays inert: clicking it twice changes nothing and opens nothing, because its target slide-over does not exist until Phase 25 (RAIL-06 idempotency, RAIL-06 concurrency)."
    - "The status bar's totals are recomputed from one array reference rather than accumulated across updates, so a listing refresh mid-render cannot produce a torn total (SHELL-06 concurrency)."
    - "A mix of readable and broken catalogs in one directory is handled per row, with the red dot on broken rows only and no separate mixed-state treatment (23-UI-SPEC E1 partial)."
    - "The rail's scroll region is unchanged from Phase 22 and the row count has no upper bound in this contract (23-UI-SPEC E1 overflow)."
    - "One catalog and many catalogs render identically, with no singular or plural branching on the header count (23-UI-SPEC E1 zero-one-many)."
    - "Titles, filenames and the directory chip all ellipsize on one line rather than wrapping or widening the rail (23-UI-SPEC E1 long-text)."
    - "Rows render in whatever order the catalog listing returns, with no client-side sort added (23-UI-SPEC E1 populated)."
    - "The status bar keeps its fixed height and never wraps or scrolls; counts growing into the millions abbreviate rather than pushing a segment off the strip, and no catalog title or path ever appears there (23-UI-SPEC E5 overflow, E5 long-text)."
    - statement: "The rail has no loading treatment: verified by confirming it renders synchronously once the catalog listing resolves, with no intermediate skeleton or spinner frame, rather than by a timing assertion."
      verification: backstop
    - statement: "A directory that cannot be read at all is treated exactly as a directory with no catalogs — the empty-library state — rather than gaining its own error surface; verified by pointing the app at a non-existent path and confirming the empty-library block appears with no console error."
      verification: backstop
  artifacts:
    - "frontend/src/components/workspace/CatalogRail.tsx — populated rows, filter, directory chip, status dot, zero-match and empty-library states"
    - "frontend/src/components/workspace/StatusBar.tsx — three live segments derived from the rail array"
    - "frontend/src/contexts/AppContext.tsx — SET_CATALOG_DIR resets the current catalog, tree, expansion and selection together"
  key_links:
    - "filter string in local component state read through useDeferredValue -> never in AppContext -> the tree has nothing to re-render against on a keystroke (RAIL-02)"
    - "CatalogMetadata.parseError non-empty -> the red dot -> plan 23-05's unreadable-catalog panel on select; one field drives both"
    - "nullable count fields -> absent rather than zero in the row and excluded from the status-bar sum -> no fabricated totals while the cache is cold"
    - "storcat-catalog-directory in localStorage -> the catalog listing's argument -> the rail, the status bar and the tree all follow one directory"
    - "SET_CATALOG_DIR clearing the current catalog -> a directory change cannot leave the tree showing a catalog that is no longer in the list"
  prohibitions:
    - "Never put the filter string into AppContext. It is local component state read through useDeferredValue, and that is the entire mechanism by which the tree survives typing."
    - "Never wire the plus-New pill to a stub, a placeholder modal or a disabled-looking control. Its target is Phase 25; a button that opens a lie is worse than a button that waits."
    - "Never render a count the sidecar cache has not computed as zero. Absent and zero are different facts and the user can tell."
    - "Never re-sort, group or rank the rail rows. Their order is the filename order the Go layer returns, and the filtered subset preserves it."
    - "Never add a spinner, skeleton row or progress indicator to the rail or the status bar."

flagged_planner_assumptions:
  - id: FPA-23-04-A
    probe_row: "RAIL-04 (unclassified — probe could not classify)"
    assumption: "RAIL-04's red dot is driven solely by a non-empty parse-error string from the catalog listing. A JSON document that is structurally valid but is not a catalog therefore carries no dot and is reported only when opened — matching 23-UI-SPEC, which lists that case under the load-path surface and not the rail surface. Detecting it at rail level would require a full unmarshal of every catalog on every load, which is the cost Pitfall N3 warns against."
  - id: FPA-23-04-B
    probe_row: "RAIL-05 (unclassified — probe could not classify)"
    assumption: "RAIL-05's 'can change it' is satisfied by the existing directory-selection binding plus persistence under a new storcat-catalog-directory key, with no directory validation UI, no recent-directories list and no diffing — all of which belong to Settings in Phase 26. The native dialog itself cannot be driven from a browser session; the persistence and reload half is browser-verified and the dialog half needs one manual pass."
  - id: FPA-23-04-C
    probe_row: "STATE-01 (unclassified — probe could not classify)"
    assumption: "STATE-01 is treated as one state with two entry conditions — no directory configured, and a configured directory holding no catalogs — rendered with Phase 22's copy verbatim in both the rail and the tree pane. A directory that cannot be read is folded into the same state per 23-UI-SPEC E1 error rather than gaining a fourth surface."
  - id: FPA-23-04-D
    probe_row: "RAIL-06 versus its requirement text"
    assumption: "RAIL-06 reads 'user can open the create slide-over from the plus-New pill', but both 23-CONTEXT.md and 23-UI-SPEC.md lock the pill inert this phase because the slide-over is Phase 25, and both explicitly instruct against wiring it to a stub. This plan delivers the pill's rendered, accessible, inert form and RAIL-06 completes in Phase 25. This is a scope boundary inherited from the locked decisions, not a reduction made here."
---

<objective>
Fill the rail with real catalogs and make the status bar tell the truth: two-line rows carrying title, size, filename and count, a red dot on anything that failed to parse, a filter that does not disturb the tree, a directory chip that can actually change directories, and three live totals along the bottom.

Purpose: the rail is the only way into every other surface in this phase, and RAIL-02's "without the tree re-rendering" is an architectural constraint rather than a nicety — it is why the filter never touches shared state.
Output: a populated, filterable, selectable rail and a live status bar, both reading one array.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-rail-virtualized-tree/23-CONTEXT.md
@.planning/phases/23-rail-virtualized-tree/23-UI-SPEC.md
@.planning/phases/23-rail-virtualized-tree/23-RESEARCH.md
@.planning/phases/23-rail-virtualized-tree/23-PATTERNS.md
@.planning/phases/22-shell-token-layer/22-UI-SPEC.md
@.planning/phases/23-rail-virtualized-tree/23-01-SUMMARY.md
@.planning/phases/23-rail-virtualized-tree/23-02-SUMMARY.md
@.planning/phases/23-rail-virtualized-tree/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populated rail rows — two lines, a status dot that never moves the layout, and selection</name>
  <files>frontend/src/components/workspace/CatalogRail.tsx</files>
  <precondition>`wails dev` is running and the browser session targets http://localhost:34115, where bound Go methods resolve; a bare Vite server on 5173 has no bindings and returns no catalogs.</precondition>
  <read_first>
    - `.planning/phases/23-rail-virtualized-tree/23-UI-SPEC.md` — "Rail Contract" in full (row markup lines 1 and 2, selected and hover states, click behaviour, header count, red status dot), "Typography" (the 10.5px and 12.5px rows), "Color" (the selected, hover, faint and error-red roles), and "UI Considerations / E1" populated, partial, overflow, zero-one-many and long-text
    - `frontend/src/components/workspace/CatalogRail.tsx` as written by plan 23-01
    - `frontend/wailsjs/go/models.ts` as regenerated by plan 23-02 — the nullable count fields and the parse-error field
    - `frontend/src/lib/format.ts` as written by plan 23-03 — `formatBytes` and `formatCount`
    - `frontend/src/workspace.css` — the existing `.ws-rail-row` density rule and the Phase 22 hover conventions
    - `design_handoff_storcat_ui/designs/StorCat 1a Demo.dc.html` lines 100-135 — the prototype's rail row markup
  </read_first>
  <action>
Replace the tracer's title-only rows with the full contract, keeping the existing header, chip and filter shells in place for task 2.

Each row carries the `ws-rail-row` class for its density padding, a 6px corner radius, and a 1px gap from its neighbour. Line one is a flex line holding three things: a 6px round status dot as a block element, then the title at 12.5px weight 500, flexed to fill and ellipsized, then the JSON size at 10.5px monospace in the faint colour with no flex growth, rendered through `formatBytes`. Line two is a single 10.5px monospace line in the faint colour with nowrap, reading the filename followed by a middot and the file count with the word files — always plural, with no singular special case, matching the existing status-bar precedent.

The status dot is always in the document. It is filled with the theme-independent error red when the catalog's parse-error string is non-empty and is otherwise transparent, so a broken catalog and a healthy one produce rows of identical height. A broken catalog's row stays fully clickable and selectable — it must not be disabled, dimmed or made unfocusable, because selecting it is how the user reaches the diagnostic panel plan 23-05 builds.

When a catalog's file count is absent — the sidecar cache has not computed it yet — render the filename alone and omit the middot and the count fragment entirely. Do not substitute a zero, a dash or an ellipsis; absent and zero are different facts, and a fabricated zero is visible to the user on every row of a cold cache.

The selected row takes the selection background, a 2px accent left border and its title in the accent colour. Hover takes the hover background. Express both as class rules following the conventions already in `workspace.css` rather than as an inline background on the row: an inline background outranks a hover rule and silently disables it, which is exactly the defect Phase 22 found and fixed on the New pill.

The rail header's count is the total number of catalogs in the directory, never the count after filtering. Rows render in the order the listing returns them, with no client-side sort added. Selection continues to dispatch the atomic catalog-selection action from plan 23-01 and nothing else; the tree pane owns loading.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && npx tsc --noEmit && npm run build && test $(grep -c "ws-rail-row" src/components/workspace/CatalogRail.tsx) -ge 1 && test $(grep -c "formatBytes" src/components/workspace/CatalogRail.tsx) -ge 1</automated>
    <human-check>With `wails dev` running and a browser at http://localhost:34115 pointed at a directory holding several catalogs plus one deliberately corrupted file: every catalog has a row with both lines, the corrupted one carries a red dot, all rows measure the same height, and clicking any row loads its tree.</human-check>
  </verify>
  <acceptance_criteria>
    - In a directory with several catalogs and one deliberately corrupted catalog file, the rendered row count equals the number of catalog files present, and exactly one row shows a dot filled with the error red.
    - Every rendered row's measured height is identical, with and without a visible dot.
    - Two catalogs given the same title both render, with different filenames on their second lines.
    - A catalog whose count is absent renders its filename with no count fragment; once the cache is warm, the same catalog renders the count.
    - A catalog with a zero file count renders zero files, and a zero-byte catalog file renders through `formatBytes` rather than as a dash or a blank.
    - The header count matches the total number of catalogs and does not change when task 2's filter is applied.
    - Clicking the corrupted catalog's row selects it — the row shows the selection background and the accent left border — rather than being inert.
    - Long titles and long filenames ellipsize on one line and the rail does not widen.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The rail lists every catalog with real values, marks the broken ones without disturbing the layout, and selecting any row — broken or healthy — routes somewhere real.</done>
</task>

<task type="auto">
  <name>Task 2: A filter that leaves the tree alone, the directory chip, and the two empty states</name>
  <files>frontend/src/components/workspace/CatalogRail.tsx, frontend/src/contexts/AppContext.tsx</files>
  <precondition>`wails dev` is running and the browser session targets http://localhost:34115.</precondition>
  <read_first>
    - `.planning/phases/23-rail-virtualized-tree/23-UI-SPEC.md` — the "Rail Contract" filtering paragraph, the zero-match resolution, the directory chip paragraph in full including what is deliberately excluded and left to Phase 26, the first-run paragraph, the plus-New pill paragraph, and the "Copywriting Contract" rows for the zero-match line, the chip and the empty-library state
    - `.planning/phases/23-rail-virtualized-tree/23-CONTEXT.md` — the locked decisions that filtering runs in TypeScript over the already-loaded array, that the filter lives in local state read through a deferred value, and that it never enters `AppContext`
    - `.planning/phases/23-rail-virtualized-tree/23-RESEARCH.md` — the RAIL-02 row in the requirements table and Pitfall N2
    - `frontend/src/services/wailsAPI.ts` — the existing directory-selection and catalog-listing wrappers
    - `frontend/src/themeTokens.ts` lines 186-251 — the established safe localStorage helpers and the `storcat-` key convention
    - `frontend/src/contexts/AppContext.tsx` as written by plans 23-01 and 23-03
  </read_first>
  <action>
Hold the filter string in local component state inside the rail and read it through React's deferred-value hook for the matching pass. It must never be dispatched into the reducer, never be persisted, and never be handed to a component outside this file — that isolation is the entire mechanism by which the tree survives typing, and it is a locked decision rather than an optimisation. Do not add a debounce timer; the deferred value is the mechanism.

Matching is a case-insensitive substring test against the catalog's title and filename considered together. Trim the input before matching, so a whitespace-only filter shows everything. The filtered subset preserves the array's existing order — no relevance ranking, no re-sort, no grouping. The header count keeps reporting the unfiltered total.

When a non-empty filter matches nothing, replace the row list with a single centred line at 11.5px in the dim colour, reading exactly the zero-match sentence from the Copywriting Contract with the current filter text quoted inside it. This is deliberately one line and not the empty-library block — no heading, no body paragraph, no action link — because "no matches for a search" is a different situation from "no catalogs exist".

Make the directory chip interactive. It keeps its Phase 22 footprint exactly — 11px monospace, chip fill, 1px line border, ellipsized — and gains only a pointer cursor and an accent hover border, matching the existing search-field hover convention. Clicking it calls the existing directory-selection binding; on a returned path, persist it to localStorage under a new `storcat-catalog-directory` key using the same safe helpers `themeTokens.ts` established, dispatch the directory into the reducer, and re-run the catalog listing against it. Add nothing beyond that: no validation UI, no recent-directories list, no diffing — those belong to Settings in Phase 26.

Extend the directory action in the reducer so that changing the directory also clears the current catalog, returns the tree to its idle state, and empties expansion and selection in the same case. Without that, a directory change can leave the tree showing a catalog that is no longer in the list.

Render the empty-library state — the existing Phase 22 rail block, copy verbatim — under two conditions: no directory is configured yet, and a configured directory returned no catalogs. Do not call the listing binding at all when no directory is configured. A directory that cannot be read is folded into this same state rather than gaining its own error surface.

Leave the plus-New pill exactly as Phase 22 built it: rendered, hover-styled and inert. Do not attach a handler, do not open a placeholder, do not disable it. Its target is Phase 25.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && npx tsc --noEmit && npm run build && test $(grep -c "useDeferredValue" src/components/workspace/CatalogRail.tsx) -ge 1 && test $(grep -c "storcat-catalog-directory" src/components/workspace/CatalogRail.tsx) -ge 1</automated>
    <human-check>With `wails dev` running: click the directory chip, choose a different folder in the native dialog, and confirm the rail reloads against it and the choice survives a restart. This half needs the real dialog and cannot be driven from a browser session.</human-check>
  </verify>
  <acceptance_criteria>
    - With a catalog loaded and its tree rendered, a mutation observer attached to the tree's scroll container records zero mutations while ten characters are typed into the filter, and the tree's rendered row count and row text are identical before and after typing.
    - No dispatch call site in `CatalogRail.tsx` receives the filter string as any part of its payload.
    - A mixed-case fragment matching one catalog by title and a different catalog by filename shows both rows.
    - An empty filter and a whitespace-only filter both show every row.
    - The filtered subset's row order is a subsequence of the unfiltered order.
    - A filter matching nothing renders exactly one centred line quoting the filter, with no heading, body paragraph or action link.
    - With the directory key removed from localStorage and the page reloaded, the empty-library block renders in the rail and no catalog-listing call is made.
    - Pointing the directory key at a non-existent path and reloading renders the same empty-library block with no console error.
    - Clicking the plus-New pill twice produces no state change, no navigation and no console error.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>A user can narrow a long rail by typing without the tree so much as flickering, point the app at a different catalog folder and have it remembered, and sees an honest empty state whenever there is nothing to list.</done>
</task>

<task type="auto">
  <name>Task 3: The status bar goes live</name>
  <files>frontend/src/components/workspace/StatusBar.tsx</files>
  <precondition>`wails dev` is running and the browser session targets http://localhost:34115.</precondition>
  <read_first>
    - `.planning/phases/23-rail-virtualized-tree/23-UI-SPEC.md` — "Status Bar Contract" in full and "UI Considerations / E5" overflow and long-text
    - `.planning/phases/22-shell-token-layer/22-UI-SPEC.md` — the status bar's locked footprint and its zero-state strings
    - `frontend/src/components/workspace/StatusBar.tsx` — the Phase 22 skeleton with its three hardcoded segments
    - `frontend/src/lib/format.ts` as written by plan 23-03 — `formatCount` and `formatGB`
    - `.planning/phases/23-rail-virtualized-tree/23-CONTEXT.md` — the locked decision that status-bar counts are derived from the rail array and need no binding of their own
  </read_first>
  <action>
Read the catalog array from the reducer and derive all three segments from it in one memo, keeping the component's Phase 22 footprint untouched: 26px tall, secondary-panel fill, 1px top border, 11px monospace in the faint colour, three left-aligned segments that never shrink.

The first segment is the number of catalogs followed by the word catalogs, unpluralised, matching the rail header's own convention. The second is the sum of the known file counts rendered through `formatCount`, followed by the words files indexed. The third is the sum of the known byte totals rendered through `formatGB`.

Both sums skip catalogs whose counts are absent rather than treating them as zero, and both accumulate as integers — sum the raw byte totals and let `formatGB` do the single division at the end. Never sum values that have already been divided or rounded for display, and never keep a running total across renders: recompute from the current array so a listing refresh cannot produce a torn number.

Add no binding call, no third-party dependency and no watching segment: the watch indicator belongs to Phase 27, and summing three numbers does not need a Go round trip.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && npx tsc --noEmit && npm run build && test $(grep -c "formatGB" src/components/workspace/StatusBar.tsx) -ge 1 && test $(grep -c "wailsjs" src/components/workspace/StatusBar.tsx) -eq 0</automated>
    <human-check>With `wails dev` running and a browser at http://localhost:34115 pointed at a directory of several catalogs: the three status-bar segments agree with the numbers visible on the rail rows, and the bar stays one line at every window width down to the narrowest supported tier.</human-check>
  </verify>
  <acceptance_criteria>
    - The status bar's catalog count equals the rail header's count, and its files-indexed figure equals the sum of the counts visible on the rail rows.
    - With the directory key cleared and the page reloaded, the three segments read zero catalogs, zero files indexed and zero point zero gigabytes.
    - A byte total of exactly 1024 gigabytes renders in terabytes and one step below renders in gigabytes — confirmed by evaluating the formatter against both values and recording the result.
    - A file count at or above one million renders abbreviated rather than fully grouped, and the bar stays one line.
    - `grep -c "wailsjs" frontend/src/components/workspace/StatusBar.tsx` returns 0 — the status bar has no binding of its own.
    - The status bar renders no catalog title, filename or path.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The three numbers along the bottom of the window are live, agree with the rail above them, and stay on one line no matter how large the library gets.</done>
</task>

</tasks>

<flagged_planner_assumptions>
Four assumptions land in this plan and are recorded in frontmatter as FPA-23-04-A through FPA-23-04-D. Three are edge-probe rows the probe returned as `unclassified` and this planner resolved rather than dismissed (RAIL-04, RAIL-05, STATE-01). The fourth records the one place a requirement cannot fully complete in this phase: RAIL-06's target slide-over is Phase 25, and both `23-CONTEXT.md` and `23-UI-SPEC.md` lock the pill inert and explicitly forbid wiring it to a stub. That boundary is inherited from the locked decisions, is stated here rather than buried, and is the only requirement in this phase whose user-visible outcome completes later.
</flagged_planner_assumptions>

<artifacts_this_phase_produces>
**Created by this plan**

| Artifact | Kind |
|---|---|
| `CatalogRail`'s populated body — two-line rows, status dot, selection and hover states, filter, zero-match line, interactive directory chip, empty-library gating | filled-in React component |
| `StatusBar`'s live body — three derived segments | filled-in React component |
| `SET_CATALOG_DIR` extended to clear the current catalog, tree, expansion and selection in one case | reducer action semantics |
| `storcat-catalog-directory` | new localStorage key, following the established naming convention |

**Consumed from other plans** — the three new metadata fields and the regenerated bridge types from 23-02; `formatBytes`, `formatCount` and `formatGB` from 23-03; the atomic catalog-selection action from 23-01; Phase 22's rail footprint, empty-library copy, `.ws-rail-row` density rule and hover conventions.

**Not produced here** — no Go change, no binding, no new component file, no npm dependency. The phase-wide artifact inventory lives in `23-01-PLAN.md`.
</artifacts_this_phase_produces>

<coverage_counts>
- **UI-SPEC `## UI Considerations`:** 41 rows phase-wide (the section's prose says 36; its tables carry 41 and all 41 are represented). This plan carries **10** — E1 empty, loading, error, populated, partial, overflow, zero-one-many and long-text, plus E5 overflow and long-text. Two of the ten are authored as flat `verification: backstop` scalars (E1 loading, E1 error). Phase-wide split: 23-01 7 + 23-02 1 + 23-03 9 + 23-04 10 + 23-05 6 + 23-06 8 = 41.
- **Spec-less edge probe:** 32 phase-wide = 23 authored + 9 flagged. This plan authors **11** — SHELL-06 boundary, precision and concurrency; RAIL-01 boundary, adjacency and empty; RAIL-02 adjacency, empty and ordering; RAIL-06 idempotency and concurrency — and flags **3** (RAIL-04, RAIL-05, STATE-01).
- **Prohibitions:** 5 survivors authored descriptor-less. Dropped in the precision pass: "do not add a debounce timer" and "do not prop-drill the catalog array" (routine engineering direction, both carried in the task actions). No canon-security survivor applies here; the phase's command-injection item is registered as T-23-01 in plan 23-06.
</coverage_counts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog metadata → DOM | Catalog titles are extracted from a sibling HTML file's title tag by the Go layer, so their content is chosen by whoever wrote that file, not by this app |
| user input → filter matching | The filter string is user-supplied and is echoed back inside the zero-match sentence |
| localStorage → catalog listing argument | The persisted directory path is read at startup and passed straight to a Go binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-10 | Tampering (script injection) | catalog titles and filenames in rail rows, `CatalogRail.tsx` | medium | mitigate | Titles come from an attacker-influenceable HTML title tag and are rendered as React text children only; the file uses no raw-HTML assignment and no raw-HTML escape hatch. Title width is bounded by the row's ellipsis rather than by trusting the source. |
| T-23-17 | Tampering (script injection) | the filter string echoed in the zero-match line | low | mitigate | The filter is interpolated as a React text child, never as markup and never into a selector or a query. |
| T-23-18 | Tampering | persisted directory path read from localStorage at startup | low | mitigate | The stored value is passed only to the existing catalog-listing binding, which resolves it to an absolute path and reads it as a directory; a bad or hostile value fails the read and degrades to the empty-library state rather than to an error surface or a crash. No path from this key ever reaches a process spawn — that path is validated separately in plan 23-06. |
| T-23-19 | Information disclosure | the directory chip showing a filesystem path | low | accept | The chip displays the directory the user chose, ellipsized, which is the requirement. No additional machine-local path is surfaced, and the status bar deliberately shows no path at all. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run build` exits 0.
- Against a directory holding several catalogs plus one deliberately corrupted catalog file at http://localhost:34115: rows, red dot, identical row heights, selection, filter behaviour, zero-match line, empty-library state and live status-bar totals all confirmed and recorded.
- The mutation-observer check during ten filter keystrokes records zero mutations in the tree's scroll container.
- The rail header count and the status bar's catalog count agree, and the files-indexed total matches the sum visible on the rows.
- The plus-New pill remains inert with no handler attached.
</verification>

<success_criteria>
The rail lists, marks, filters and selects every catalog in the configured directory without ever disturbing the tree, the directory chip can change where it looks and remembers the choice, and the status bar's three numbers are live and agree with the rows above them.
</success_criteria>

<output>
Create `.planning/phases/23-rail-virtualized-tree/23-04-SUMMARY.md` when done.
</output>
