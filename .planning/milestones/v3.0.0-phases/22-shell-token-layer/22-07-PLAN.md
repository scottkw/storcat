---
phase: 22-shell-token-layer
plan: 07
type: execute
wave: 4
depends_on: ["22-01", "22-02", "22-03", "22-04", "22-05", "22-06"]
files_modified:
  - frontend/src/components/workspace/DetailsPanel.tsx
  - frontend/src/components/workspace/Toolbar.tsx
  - frontend/src/components/workspace/WorkspaceShell.tsx
  - frontend/src/workspace.css
autonomous: false
requirements: [SHELL-03, SHELL-09, THEME-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Below 1280px the details panel is the same component rendered into a fixed-position right drawer at 288px wide, spanning from the toolbar's bottom edge to the status bar's top edge, with a -24px 0 50px shadow (SHELL-03)."
    - "A Details chip appears in the toolbar only below 1280px, toggles the drawer, carries aria-expanded reflecting drawer state, and carries the no-drag opt-out like every other toolbar control (SHELL-03)."
    - "The drawer closes on Escape and on backdrop click, and both routes go through one close path (SHELL-03)."
    - "The drawer overlays the tree pane at both narrow tiers and the backdrop sits behind it, with every stacking value read from a named custom property (SHELL-09)."
    - "The details drawer and its backdrop both carry z-index var(--z-details-drawer); the drawer is rendered after the backdrop in DOM order, so it paints above without needing a second z-index value (SHELL-09 adjacency)."
    - "With no overlay open, no element in the shell carries a z-index above 0; the drawer and backdrop pair is the only stacking participant Phase 22 introduces (SHELL-09 empty)."
    - "The three scale values are declared once as --z-details-drawer 100, --z-overlay 200 and --z-dialog 300 in workspace.css, and no component file contains a numeric z-index literal (SHELL-09 ordering)."
    - "Toggling the Details chip twice returns detailOverlay to its prior value and leaves no orphaned backdrop node and no lingering Escape key listener (SHELL-09 idempotency)."
    - "Escape-to-close and backdrop-click-to-close both route through a single close path, so firing both in the same frame closes the drawer once and does not throw (SHELL-09 concurrency)."
    - "Crossing from below 1280px to above it while the drawer is open leaves the details panel visible as an inline pane with no orphaned backdrop and no scroll lock left behind."
    - "Accent-filled surfaces stay legible in all 11 themes at every width tier, including inside the drawer (THEME-02)."
  artifacts:
    - "frontend/src/components/workspace/DetailsPanel.tsx — variant prop of 'pane' | 'drawer' on DetailsPanelProps"
    - "frontend/src/components/workspace/Toolbar.tsx — Details chip, rendered only below 1280px"
    - "frontend/src/workspace.css — .ws-details--pane, .ws-details--drawer and .ws-backdrop rules"
  key_links:
    - "useMediaQuery('(min-width: 1280px)') in WorkspaceShell -> DetailsPanel variant and Details chip visibility -> SHELL-03; the same hook call drives both, so the chip can never appear while the panel is an inline pane"
    - "detailOverlay in the reducer -> drawer open state and the chip's aria-expanded -> one source of truth for both the visual and the announced state"
    - "--z-details-drawer 100 -> the drawer and backdrop; Phases 24 to 26 slot their overlays at --z-overlay 200 and --z-dialog 300 above it, which is the stacking bug the handoff calls easy to reintroduce"
  prohibitions:
    - "Do not build a second, mobile-only details component. The locked decision is one component rendered two ways; a duplicate would drift and the two would disagree about what a catalog's details are."
    - "Do not hardcode a numeric z-index anywhere. Later phases stack their palette, slide-over and dialogs against this scale, and a raw number is the specific defect the handoff warns is easy to reintroduce."
---

<objective>
Turn the details panel into a drawer below 1280px, add the toolbar chip that toggles it, and lock the overlay stacking scale — then run the phase's full manual verification matrix.

Purpose: this is the last expansion out from the tracer and the only overlay Phase 22 ships, so it is where the z-index scale that Phases 24 to 26 will build on gets established.
Output: the responsive details drawer, the Details chip, the stacking scale in use, and a completed phase verification pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-shell-token-layer/22-UI-SPEC.md
@.planning/phases/22-shell-token-layer/22-RESEARCH.md
@.planning/phases/22-shell-token-layer/22-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the details panel as a drawer below 1280px with a backdrop and one close path</name>
  <files>frontend/src/components/workspace/DetailsPanel.tsx, frontend/src/components/workspace/WorkspaceShell.tsx, frontend/src/workspace.css</files>
  <read_first>
    - `.planning/phases/22-shell-token-layer/22-UI-SPEC.md` — "Layout & Responsive Contract" (the drawer's exact geometry and shadow, and the same-component rule), "Z-Index Scale (SHELL-09)", "Spacing Scale" (the note that the drawer shadow is deliberately distinct from the slide-over shadow and must not be reused)
    - `.planning/phases/22-shell-token-layer/22-RESEARCH.md` — "Pattern 4" (`detailDisp`, `detailPos`, `detailW`, `detailShadow` quoted verbatim), "Anti-Patterns to Avoid" (do not build a real focus-trap or scroll-lock hook this phase; write the small inline version so Phase 24 can lift the pattern), "Pitfall: Z-index scale reconciliation"
    - `frontend/src/components/workspace/DetailsPanel.tsx`, `frontend/src/components/workspace/WorkspaceShell.tsx` and `frontend/src/workspace.css` as they now stand
  </read_first>
  <action>
**DetailsPanel.** Add a `variant` member of type `'pane' | 'drawer'` to the existing exported `DetailsPanelProps` interface, defaulting to `'pane'`. The component keeps its single body and single copy; `variant` only selects which class the root element carries — `ws-details--pane` or `ws-details--drawer` — alongside the existing `ws-details`. Do not fork the body, do not duplicate the placeholder copy, and do not add a width branch in JavaScript.

**workspace.css.** `.ws-details--pane` carries no positioning — it stays a grid child, which is what it already is. `.ws-details--drawer` sets `position: absolute`, `right: 0`, `top: 46px`, `bottom: 26px`, `width: 288px`, `box-shadow: -24px 0 50px rgba(0, 0, 0, 0.45)`, and `z-index: var(--z-details-drawer)`. That shadow value is specific to this drawer; the create slide-over's `-30px 0 70px rgba(0, 0, 0, 0.5)` is a different value for a different surface in Phase 25 and must not be substituted. Add `.ws-backdrop` with `position: absolute`, `top: 46px`, `right: 0`, `bottom: 26px`, `left: 0`, `background: rgba(0, 0, 0, 0.35)`, and `z-index: var(--z-details-drawer)`. Both the drawer and the backdrop sit at the same named layer; the drawer paints above because it is rendered after the backdrop in DOM order. Do not introduce a fourth z-index value and do not use `calc` on a z-index — the scale has exactly three named tiers.

`.ws-root` already sets `position: relative` from plan 22-01; confirm it, since it is the containing block both absolutely-positioned elements resolve against.

**WorkspaceShell.** With `isWide` already available from `useMediaQuery('(min-width: 1280px)')`, and `state.detailOverlay` and `dispatch` from `useAppContext()`, render exactly one of two shapes. When `isWide` is true: render `<DetailsPanel variant="pane" />` as the third grid child, unconditionally, and render no backdrop. When `isWide` is false: render the details panel outside `.ws-grid` as a sibling, and render both it and the backdrop only when `state.detailOverlay` is true — the backdrop first, then `<DetailsPanel variant="drawer" />`.

Write one `closeDrawer` function that dispatches `SET_DETAIL_OVERLAY` with `false`. Bind it to the backdrop's `onClick` and to a `keydown` listener that fires on the `Escape` key. Both paths call that one function, so firing both in the same frame dispatches the same action twice and settles on the same state rather than throwing. Register the key listener in a `useEffect` that runs only while the drawer is open and remove it in the cleanup, so no listener survives a close.

Add one more effect: when `isWide` becomes true, dispatch `SET_DETAIL_OVERLAY` with `false`. Crossing up through the boundary with the drawer open must not leave stale overlay state behind, because the inline pane renders regardless of `detailOverlay` and the flag would otherwise stay set invisibly until the user crossed back down.

Do not build a focus trap, a scroll-lock hook, or a shared modal-behaviour abstraction here. 22-RESEARCH.md scopes that to Phase 24's palette, which is the first genuinely interactive overlay; keep this close logic small and obvious so Phase 24 can lift the pattern rather than reconcile two divergent implementations.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && npx tsc --noEmit && npm run build && test $(grep -c "var(--z-details-drawer)" src/workspace.css) -eq 2 && test $(grep -c "\-24px 0 50px" src/workspace.css) -eq 1 && test $(grep -rc "z-index: *[0-9]" src/components/ | grep -v ':0$' | wc -l) -eq 0 && test $(grep -c "zIndex" src/components/workspace/WorkspaceShell.tsx) -eq 0</automated>
    <human-check>Run `wails dev`. Resize below 1280px: the details panel disappears from the grid and the tree widens. Toggle the drawer open (see Task 2 for the chip) and confirm it slides over the right edge of the tree at 288px wide, spanning from the toolbar's bottom edge to the status bar's top edge, with a visible left-edge shadow and a dimmed backdrop over the tree but not over the toolbar or status bar. Press Escape: it closes. Reopen and click the backdrop: it closes. Reopen, then widen the window past 1280px: the panel becomes an inline third column with no leftover backdrop. Narrow again and confirm the drawer is closed rather than reopening.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--z-details-drawer)" frontend/src/workspace.css` returns exactly 2 — the drawer and the backdrop.
    - No file under `frontend/src/components/` contains a numeric `z-index` or `zIndex` value.
    - `grep -c "\-24px 0 50px" frontend/src/workspace.css` returns 1 and `grep -c "\-30px 0 70px" frontend/src/workspace.css` returns 0 — the drawer shadow is not the slide-over's.
    - `grep -c "variant" frontend/src/components/workspace/DetailsPanel.tsx` returns at least 2 — the prop declaration and its use.
    - `grep -c "DetailsPanel" frontend/src/components/workspace/WorkspaceShell.tsx` returns exactly 3 — the import plus the two render sites.
    - `grep -c "SET_DETAIL_OVERLAY" frontend/src/components/workspace/WorkspaceShell.tsx` returns exactly 2 — the close path and the widen-boundary reset.
    - `grep -c "removeEventListener" frontend/src/components/workspace/WorkspaceShell.tsx` returns at least 1.
    - `grep -rc "focus-trap\|focusTrap\|overflow: *hidden.*body" frontend/src/components/workspace/ | grep -v ':0$' | wc -l` returns 0 — no premature modal-behaviour abstraction.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>Below 1280px the same details component renders as a 288px right drawer over a backdrop at the named drawer layer, closing on Escape and on backdrop click through one path, and crossing back above 1280px restores the inline pane with no orphaned overlay state.</done>
  <reversibility rating="costly">The three-tier z-index scale established here is depended on by Phases 24, 25 and 26; changing the numbers later means touching every overlay those phases add. Rated costly rather than one-way, since the values are named custom properties in one file.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add the toolbar Details chip, visible only below 1280px</name>
  <files>frontend/src/components/workspace/Toolbar.tsx, frontend/src/components/workspace/WorkspaceShell.tsx</files>
  <read_first>
    - `.planning/phases/22-shell-token-layer/22-UI-SPEC.md` — "Icons" (the accessible-name table row stating the visible text is the accessible name and that it carries `aria-expanded`), "Window Chrome & Drag Regions" (the explicit instruction that the Details chip needs the drag opt-out on first implementation), "UI Considerations / E1 partial"
    - `.planning/phases/22-shell-token-layer/22-RESEARCH.md` — "Pitfall: --wails-draggable on the Details chip specifically" (the risk here is forgetting the opt-out on first implementation, not regressing it later)
    - `frontend/src/components/workspace/Toolbar.tsx` and `frontend/src/components/workspace/WorkspaceShell.tsx` as they now stand
    - `design_handoff_storcat_ui/designs/StorCat 1a Demo.dc.html` line 86 — the Details chip markup to transcribe
  </read_first>
  <action>
Give `Toolbar` a props interface named `ToolbarProps` declared immediately above it, with three members: `showDetailsChip: boolean`, `detailsOpen: boolean`, and `onToggleDetails: () => void`. Pass all three from `WorkspaceShell`: `showDetailsChip` is the negation of `isWide`, `detailsOpen` is `state.detailOverlay`, and `onToggleDetails` dispatches `SET_DETAIL_OVERLAY` with the negation of the current value. The chip's visibility comes from the same single `useMediaQuery` call that chooses the panel variant, so the chip can never appear while the panel is an inline pane.

In the right cluster of the toolbar, before the theme chip, render the Details chip when `showDetailsChip` is true: a `button` with `type="button"`, `className="no-drag"`, `aria-expanded` bound to `detailsOpen`, `onClick` bound to `onToggleDetails`, and no `aria-label` — its visible text `Details` is its accessible name. Style it `fontSize: 11.5`, `border: '1px solid var(--l)'`, `borderRadius: 6`, `padding: '3px 8px'`, `background: 'transparent'`, `cursor: 'pointer'`, `whiteSpace: 'nowrap'`, with `color` reading `var(--ac)` when `detailsOpen` is true and `var(--dm)` when it is false — the prototype signals open state through the chip's foreground colour.

The drag opt-out is the specific failure this control is warned about. It must carry `.no-drag` in this same edit, not as a follow-up: Wails reads the inherited drag custom property from the exact element the mousedown lands on, so without the opt-out the chip inherits `drag` from the toolbar and its clicks are swallowed, most visibly on Windows.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && npx tsc --noEmit && npm run build && test $(grep -c 'className="no-drag"' src/components/workspace/Toolbar.tsx) -eq 4 && test $(grep -c "aria-expanded" src/components/workspace/Toolbar.tsx) -eq 1 && test $(grep -c "ToolbarProps" src/components/workspace/Toolbar.tsx) -ge 2</automated>
    <human-check>Run `wails dev` at 1280px or wider: no Details chip is present in the toolbar. Narrow below 1280px: the chip appears to the left of the theme chip. Click it: the drawer opens and the chip's text turns accent-coloured. Click it again: the drawer closes and the chip returns to the dim colour. Press and drag from the toolbar background beside the chip: the window moves. Press and drag starting on the chip itself: the window does not move and the click registers as a toggle. Widen past 1280px while the drawer is open: the chip disappears and the panel becomes an inline column.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'className="no-drag"' frontend/src/components/workspace/Toolbar.tsx` returns exactly 4 — search field, Details chip, theme chip, gear.
    - `grep -c "aria-expanded" frontend/src/components/workspace/Toolbar.tsx` returns exactly 1.
    - `grep -c "aria-label" frontend/src/components/workspace/Toolbar.tsx` still returns exactly 2 — the Details chip is named by its visible text, not by a label.
    - `grep -c "ToolbarProps" frontend/src/components/workspace/Toolbar.tsx` returns at least 2 — the interface and the component signature.
    - `grep -c "matchMedia\|useMediaQuery\|innerWidth" frontend/src/components/workspace/Toolbar.tsx` returns 0 — the tier arrives as a prop.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The Details chip appears only below 1280px, toggles the drawer, announces its expanded state, and stays clickable inside the drag region.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the phase verification matrix</name>
  <what-built>The complete Phase 22 shell: the token layer driving all 11 themes and both densities, vendored IBM Plex typography, the 46px toolbar with drag regions and the macOS traffic-light inset, the rail, tree pane, details panel and status bar as skeletons, the three responsive width tiers, the rail-side swap, and the details drawer with its stacking scale.</what-built>
  <how-to-verify>
    Work through every row of the "Manual-Only Verifications" table in `.planning/phases/22-shell-token-layer/22-VALIDATION.md` against a `wails dev` session, plus a built binary for the two rows that require one. Use the DEV affordance for the states with no shipping UI: Ctrl+Alt+T cycles themes, Ctrl+Alt+D toggles density, Ctrl+Alt+R flips rail side.

    The four rows most likely to fail and most worth slowing down for:

    1. **THEME-02, all 11 themes.** Cycle every theme and inspect both accent-filled surfaces in each — the rail's "+ New" pill on hover and the tree pane's "Catalog a volume" button. The `--onac` defect only shows on light accents, so Gruvbox Dark's orange, Monokai's green and GitHub Light's blue are the deciding cases, not the default theme.
    2. **THEME-05 offline.** Set DevTools Network to Offline and reload. Plex must still render, and a normal load must show zero third-party font requests.
    3. **THEME-06 against the built binary.** Set a non-default theme, density and rail side, fully quit the built app — not a `vite dev` page reload — relaunch, and confirm all three persist with no flash of the default theme on launch.
    4. **SHELL-08 on a real macOS build.** Traffic lights inside the 46px band with the inset clearing them. If the 78px value clips or leaves a visible gap, adjust it in `Toolbar.tsx` and note the corrected number in the summary; it is a locked-but-unverified value, not a derived one.

    Two rows this environment may not be able to discharge. Record the outcome either way rather than silently passing them:
    - **SHELL-07 on Windows.** Click-swallowing is most visible there. If no Windows build was produced, record the requirement as verified on the available platforms only.
    - **SHELL-09 full stacking order.** Only the details drawer exists this phase; there is no palette or dialog to stack it against until Phase 24. Verify the drawer overlays the tree and that the backdrop closes it, and record the full ordering check as deferred to Phase 24.

    Then run the full suite once: `cd frontend && npx tsc --noEmit && npm run build && cd .. && go build ./... && go test ./...`.
  </how-to-verify>
  <resume-signal>Type "approved" once every matrix row is either passed or explicitly recorded as untestable in this environment, or describe the failures.</resume-signal>
</task>

</tasks>

<flagged_planner_assumptions>
None of this phase's eight unresolved edge-probe rows land in this plan. SHELL-09's five probe rows are all classified and all authored explicitly into `must_haves.truths`. The scope caveat that SHELL-09's full stacking order cannot be verified until Phase 24 supplies a second overlay is carried as an explicit instruction in Task 3 rather than as an assumption, because it is a stated fact in `22-VALIDATION.md`, not a planner inference.
</flagged_planner_assumptions>

<artifacts_this_phase_produces>
**Created by this plan**

| Artifact | Kind |
|---|---|
| `DetailsPanelProps.variant` of type `'pane' \| 'drawer'` | prop on the interface 22-05 declared |
| `ToolbarProps` with `showDetailsChip`, `detailsOpen`, `onToggleDetails` | new exported interface |
| Details chip in the toolbar right cluster, with `aria-expanded` and `.no-drag` | React element |
| `.ws-details--pane`, `.ws-details--drawer`, `.ws-backdrop` | CSS classes in `workspace.css` |
| First consumers of `--z-details-drawer` | the drawer and its backdrop |

**Consumed from other plans in this phase** — `--z-details-drawer`, `--z-overlay`, `--z-dialog`, `.ws-root`'s `position: relative`, `.ws-details` and `.no-drag` from `workspace.css` (22-01); `DetailsPanel` and `DetailsPanelProps` (22-05); `Toolbar` (22-03); `useMediaQuery`, `SET_DETAIL_OVERLAY` and `detailOverlay` (22-06).

**Left for later phases** — `--z-overlay` at 200 is claimed by Phase 24's palette and Phase 25's slide-over; `--z-dialog` at 300 is claimed by Phase 26's Settings. Both are declared here and unused; they exist so those phases reference a name rather than invent a number.

**Not produced here** — no new file, no new CSS custom property, no reducer action, no Go change. The full phase-wide artifact index lives in `22-01-PLAN.md`.
</artifacts_this_phase_produces>

<coverage_counts>
- **UI-SPEC `## UI Considerations`:** 32 applicable phase-wide; **0 carried by this plan** — the probe's six surfaces are all carried by 22-01 (E6, 4), 22-03 (E1, 6), 22-04 (E2 and E5, 10) and 22-05 (E3 and E4, 12). 4 + 6 + 10 + 12 = 32. Zero dropped. Task 3's matrix re-verifies all 32 in the running app.
- **Spec-less edge probe:** 34 applicable phase-wide; this plan carries **5** — all of SHELL-09's adjacency, empty, ordering, idempotency and concurrency rows, each authored explicitly into `must_haves.truths`. Zero flagged assumptions here. Phase-wide: 26 authored (22-01 12, 22-02 7, 22-06 2, 22-07 5) + 8 flagged (22-01 3, 22-03 2, 22-06 3) = 34. Equality holds; zero dropped, zero auto-dismissed.
- **Prohibitions:** 2 survivors authored descriptor-less. Dropped during precision-drop: "do not build a focus trap this phase" and "do not reuse the slide-over shadow" (routine engineering scope, both enforced as acceptance criteria).
</coverage_counts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard and pointer -> overlay lifecycle | Escape and backdrop click both mutate overlay state; no other input crosses into this plan's surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-13 | Denial of service | drawer close path | medium | mitigate | Escape and backdrop click share one `closeDrawer` function, and the key listener is registered only while the drawer is open and removed in cleanup. A drawer that cannot be dismissed, or a listener that survives close and swallows Escape elsewhere, would deny the user the tree pane underneath. Enforced by acceptance criteria on the single `SET_DETAIL_OVERLAY` close path and the listener cleanup, plus manual open-close-reopen checks. |
| T-22-14 | Elevation of privilege (UI) | z-index scale | high | mitigate | A raw numeric z-index in any component lets a later overlay be visually covered by the details drawer, which the handoff names as an easy bug to reintroduce. Mitigated by declaring exactly three named tiers in one file and by an acceptance criterion that no file under `frontend/src/components/` contains a numeric z-index. Phases 24 to 26 must reference `--z-overlay` and `--z-dialog` rather than literals. |
| T-22-07 | Denial of service (input) | Details chip drag region | medium | mitigate | The chip carries `.no-drag` in the same edit that introduces it; without it the chip inherits the toolbar's drag value and becomes permanently unclickable. Enforced by an acceptance criterion counting exactly four `.no-drag` occurrences against four interactive toolbar controls. |
| T-22-04 | Tampering (injection) | drawer and backdrop markup | medium | mitigate | The drawer reuses `DetailsPanel`'s authored literals; the backdrop renders no text. No `dangerouslySetInnerHTML` and no `innerHTML` assignment. <!-- planner-discipline-allow: dangerouslySetInnerHTML --> |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run build && cd .. && go build ./... && go test ./...` all exit 0.
- Exactly two `var(--z-details-drawer)` references in `workspace.css` and zero numeric z-index values anywhere under `frontend/src/components/`.
- Exactly four `.no-drag` occurrences and exactly one `aria-expanded` in `Toolbar.tsx`.
- `DetailsPanel` is imported once and rendered at exactly two sites in `WorkspaceShell.tsx` — one component, two variants.
- All 13 rows of `22-VALIDATION.md`'s Manual-Only table are passed or explicitly recorded as untestable in this environment, with the Windows and full-stacking-order caveats named.
</verification>

<success_criteria>
Below 1280px the details panel becomes a 288px right drawer over a backdrop, toggled by a toolbar chip that appears only at those tiers, closing on Escape and backdrop click through one path, stacked at a named layer with no raw z-index anywhere; and the phase's full verification matrix has been worked through with every row passed or explicitly recorded as untestable.
</success_criteria>

<output>
Create `.planning/phases/22-shell-token-layer/22-07-SUMMARY.md` when done.
</output>
