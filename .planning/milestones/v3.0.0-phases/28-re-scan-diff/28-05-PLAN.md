---
phase: 28-re-scan-diff
plan: 05
type: execute
wave: 5
depends_on: ["28-04"]
files_modified:
  - frontend/src/components/workspace/UnreadableCatalogPanel.tsx
  - frontend/src/workspace.css
autonomous: false
requirements: [STATE-03]
user_setup: []

must_haves:
  truths:
    - "A user looking at a catalog whose JSON will not parse can re-scan its source volume, open its `.html` instead, or remove it from the library — three actions, from the panel itself. (D: STATE-03)"
    - "Re-scanning an unreadable catalog is a FRESH scan, not a diff: the dialog opens with no old tree, shows the reduced 'Scan complete' variant with no stat grid and no diff list, and offers overwrite or keep-both only. (D: STATE-03)"
    - "'Remove from library' opens the EXISTING Phase 27 delete confirmation and routes to the existing delete-to-Trash binding — no bespoke library-removal dialog, no hidden/excluded list, no second membership concept. (D: STATE-03)"
    - "'Open the .html instead' reuses the details-panel footer's existing open-HTML logic verbatim, and the button is OMITTED entirely (not greyed) when the catalog has no `.html`."
    - "Keep both from the unreadable path leaves the broken original exactly as unreadable as it was, and the Variant B caption says so."
  prohibitions:
    - "Removing an unreadable catalog must never invent a hidden, excluded, or archived state. Membership in the library IS the file living in the configured catalog directory; a second, invisible notion of membership would leave the user with files they believe are gone and a library that disagrees with the filesystem."
  artifacts:
    - frontend/src/components/workspace/UnreadableCatalogPanel.tsx
  key_links:
    - "'Re-scan volume' → `RescanDialog` with no old tree → Variant B"
    - "'Remove from library' → the existing `DeleteConfirmDialog` → the existing delete-to-Trash binding"
    - "'Open the .html instead' → the existing `handleOpenHtml` logic (`getCatalogHtmlPath` + `openExternal`)"
---

<objective>
Give the unreadable-catalog dead end three ways out.

Purpose: a catalog whose JSON will not parse is currently a terminal state with a stub comment where its
actions should be. STATE-03 closes that, and every one of the three actions is a reuse — the only genuinely
new thing here is the buttons themselves.

Output: the action trio in `UnreadableCatalogPanel`, wired to three already-shipped surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-re-scan-diff/28-CONTEXT.md
@.planning/phases/28-re-scan-diff/28-UI-SPEC.md
@.planning/phases/28-re-scan-diff/28-PATTERNS.md
@.planning/phases/28-re-scan-diff/28-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: The unreadable-catalog action trio</name>
  <files>frontend/src/components/workspace/UnreadableCatalogPanel.tsx, frontend/src/workspace.css</files>
  <read_first>
    - frontend/src/components/workspace/UnreadableCatalogPanel.tsx (FULL — especially the stub comment around lines 111-113 that marks the exact insertion point, the raw-parse-error box above it, and the existing `!` badge)
    - frontend/src/components/workspace/DetailsPanel.tsx (the `Footer`'s `handleOpenHtml` around lines 246-268 — `getCatalogHtmlPath` plus `openExternal`; and how `hasHtml` is determined)
    - frontend/src/components/workspace/DeleteConfirmDialog.tsx (its props, and the existing call site that invokes the delete binding — copy that call-site pattern exactly)
    - frontend/src/components/workspace/rescan/RescanDialog.tsx (its props, specifically the flag that selects Variant B)
    - .planning/phases/28-re-scan-diff/28-UI-SPEC.md (Entry Points item 3's table with exact labels and styling; Step 3 Variant B; the Variant B resolution caption)
    - frontend/src/workspace.css (existing button class conventions)
  </read_first>
  <action>
<!-- planner-discipline-allow: hidden -->
<!-- planner-discipline-allow: excluded -->
<!-- planner-discipline-allow: archived -->
Replace the stub comment with a row of buttons below the raw-parse-error box: `display: flex; gap: 10px`.

**Button 1 — `"Re-scan volume"`** (no ellipsis; deliberately different from the details-panel footer's
`"Re-scan volume…"` and the menu's `"Re-scan volume & diff…"` — all three are locked labels, and the
differences are intentional, not drift). Filled, 32px, `padding: 0 15px`, radius 8px, `--ac` fill with
`--onac` text, 12.5px/600. Opens `RescanDialog` at the pick-volume step with the old-tree flag FALSE, so
step 3 renders the reduced Variant B. This is the correct shape: the catalog's JSON does not parse, so
there is no old tree to compare against and a diff would be undefined. The user picks a volume, scans,
and is offered overwrite or keep-both with no diff view.

**Button 2 — `"Open the .html instead"`.** Outlined, 32px, `padding: 0 15px`, radius 8px, 1px `--l`
border, 12.5px, default text colour. Reuse the details-panel footer's existing open-HTML logic verbatim —
the same path helper and the same external-open call. Do not write a second path derivation. An
unreadable catalog can still have a perfectly valid `.html` companion, because only the JSON failed to
parse. **Omit this button entirely (do not grey it) when the catalog has no `.html`** — the same "no
button whose only outcome is inapplicable" rule the delete dialog's conditional `.html` row already
follows.

**Button 3 — `"Remove from library"`** (no ellipsis, locked). Outlined, 32px, `padding: 0 15px`, radius 8px,
1px `--l` border — the border stays neutral, only the TEXT is `--danger`. Opens the existing Phase 27
delete confirmation, passing this catalog, unchanged. There is no library-membership concept to toggle:
membership is the file living in the configured catalog directory, so this is Phase 27's delete-to-Trash,
reused. Do not build a bespoke removal dialog, do not add a hidden/excluded/archived list, and do not add
a new binding — the existing one already carries the confirmation and the Trash recovery story.

Apply the same shared disabled guard the other entry points use (`aria-disabled`, the locked
already-scanning tooltip, dimmed) to button 1 while a scan is running. Buttons 2 and 3 are unaffected by
scan state.

Add the matching `.ws-unreadable-actions*` classes to `workspace.css`. No new CSS custom properties.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
    <automated>git diff HEAD -- frontend/src/components/workspace/DeleteConfirmDialog.tsx | grep -c . | grep -qx 0 || (echo "FAIL: DeleteConfirmDialog was modified — it must be reused unchanged" &amp;&amp; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Open the .html instead' frontend/src/components/workspace/UnreadableCatalogPanel.tsx` returns 1.
    - `grep -c 'Remove from library' frontend/src/components/workspace/UnreadableCatalogPanel.tsx` returns 1.
    - `grep -c '"Re-scan volume"' frontend/src/components/workspace/UnreadableCatalogPanel.tsx` returns at least 1, and `grep -c 'Re-scan volume…' frontend/src/components/workspace/UnreadableCatalogPanel.tsx` returns 0 — this panel's label carries no ellipsis.
    - `grep -c 'DeleteConfirmDialog' frontend/src/components/workspace/UnreadableCatalogPanel.tsx` returns at least 1, and `git diff HEAD -- frontend/src/components/workspace/DeleteConfirmDialog.tsx` is EMPTY.
    - `grep -ciE 'hidden|excluded|archived|libraryMembership' frontend/src/components/workspace/UnreadableCatalogPanel.tsx` returns 0 — no second membership concept was introduced.
    - `git diff HEAD -- app.go` is EMPTY — this plan adds zero Go code.
    - `cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Three buttons wired to three existing surfaces; no new state, no new binding, no schema change.</reversibility>
  <done>The unreadable panel offers all three actions, each routed to an already-shipped surface, with the HTML button absent when there is no HTML.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification of the unreadable-catalog trio</name>
  <what-built>The three STATE-03 actions on `UnreadableCatalogPanel`, wired to the re-scan dialog's reduced variant, the existing open-HTML logic, and the existing delete-to-Trash confirmation.</what-built>
  <how-to-verify>
`wails dev` on **`:34115`**, driven with dev-browser. Stage fixtures with plain filesystem commands only.

1. Copy a real catalog `.json` to a scratch name and corrupt it so it will not parse (truncate it
   mid-object, or write a stray token into it). Keep its `.html` beside it. Open it in the app and
   confirm the unreadable panel renders with all three buttons.
2. Click **"Open the .html instead"** and confirm the companion HTML opens externally.
3. Delete the `.html`, reload, and confirm that button is now ABSENT — not greyed, not a placeholder.
4. Click **"Re-scan volume"**. Confirm the dialog opens at the pick-volume step, and that after the scan
   step 3 shows the reduced variant: header "Scan complete" (NOT anything claiming the catalog was
   rebuilt), no stat grid, no diff row list, no similarity banner, just the mono summary line and the
   Variant B caption. Confirm Keep both is offered and, when used, leaves the corrupt original exactly as
   corrupt as it was.
5. Then use **Overwrite catalog** on another corrupt copy and confirm the JSON now parses.
6. Click **"Remove from library"** on a third corrupt copy. Confirm the EXISTING Phase 27 delete
   confirmation appears (not a new dialog), the file goes to the OS Trash, and it disappears from the
   rail. Confirm it is recoverable from the Trash.
7. Confirm button 1 is dimmed with the locked already-scanning tooltip while a Create scan is running,
   and that buttons 2 and 3 are not.
8. Clean up every scratch catalog afterwards.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which action misbehaved.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces (plan 28-05)

**React — `frontend/src/components/workspace/UnreadableCatalogPanel.tsx`:** the action-trio row replacing
the stub comment; local handlers for the three actions (re-scan open, open-HTML reuse, delete-confirm
open).

**CSS — `frontend/src/workspace.css`:** `.ws-unreadable-actions`, `.ws-unreadable-action`,
`.ws-unreadable-action-primary`, `.ws-unreadable-action-danger`. No new CSS custom properties.

**No new Go symbols, no new Wails bindings, no new files.** All three actions route to surfaces shipped in
Phases 27 and 28-01..04.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unreadable catalog file → app | The panel is reached specifically because the file's contents failed to parse; nothing about its contents may be trusted |
| panel → existing delete binding | A destructive, filesystem-level action is triggered from a new call site |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-15 | Tampering | "Remove from library" deleting outside the catalog directory | high | mitigate | The existing delete binding's own `filepath.Abs` → `filepath.EvalSymlinks` → containment gate applies unchanged — this is a new CALL SITE of an already-gated binding, not a new deletion path. Enforced by the acceptance criterion that `app.go` is unmodified in this plan. |
| T-28-16 | Tampering | "Open the .html instead" opening an arbitrary external path | medium | mitigate | The HTML path is derived by the existing shared helper from the catalog's own already-validated path, and opened through the existing external-open call. No renderer-supplied path is introduced. |
| T-28-17 | Information Disclosure | the raw parse error rendered above the buttons | low | accept | Pre-existing behaviour of this panel, unchanged by this plan; the error concerns a file the user selected and can already open themselves. |
| T-28-SC | Tampering | npm/pip/cargo installs | high | accept | No package installed; no dependency manifest is touched. |

**Ledger note (not a new threat):** this plan adds a new reachable call site into the delete-to-Trash path
whose Windows and Linux backends remain runtime-unverified (WINDOWS.md entry #10, open). New call site,
same open status — plan 28-06's sweep records the new call site and does NOT close the entry.
</threat_model>

<flagged_assumptions>
1. **[UI-SPEC Variant B]** The Variant B header deliberately says "Scan complete" rather than anything
   claiming the catalog was rebuilt, because Keep both leaves the broken original untouched — the title
   must not presuppose which resolution the user picks. If a reviewer prefers a more reassuring title,
   that is a copy change that must be re-checked against the Keep-both semantics first.
2. **Corrupt-catalog fixtures are destructive to stage.** Verification requires deliberately corrupting
   catalog files. Task 2 works on scratch COPIES only, and cleans up. Do not corrupt a real catalog.
</flagged_assumptions>

<verification>
- `cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build` green.
- `git diff HEAD -- app.go frontend/src/components/workspace/DeleteConfirmDialog.tsx` empty.
- Task 2's live checkpoint approved, including the absent-not-greyed HTML button and the Trash recovery.
</verification>

<success_criteria>
An unreadable catalog is no longer a dead end: all three exits work, and none of them invented a new
concept to do it.
</success_criteria>

<output>
Create `.planning/phases/28-re-scan-diff/28-05-SUMMARY.md` when done
</output>
