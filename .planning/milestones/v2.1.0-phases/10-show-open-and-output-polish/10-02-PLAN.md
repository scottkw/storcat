---
phase: 10-show-open-and-output-polish
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - cli/open.go
  - cli/open_test.go
  - cli/stubs.go
  - cli/cli_test.go
autonomous: true
requirements:
  - CLCM-05
  - CLPC-05

must_haves:
  truths:
    - "User runs 'storcat open <catalog.json>' and the corresponding HTML file opens in the system default browser"
    - "open works on macOS (open), Linux (xdg-open), and Windows (cmd /c start) via pkg/browser"
    - "Missing arg, non-.json file, missing HTML file, or unreadable catalog produce clear stderr errors with non-zero exit"
  artifacts:
    - path: "cli/open.go"
      provides: "runOpen command with cross-platform browser launch"
      exports: ["runOpen"]
      contains: "func runOpen"
    - path: "cli/open_test.go"
      provides: "Tests for open command (HTML path derivation, error cases)"
      contains: "TestRunOpen"
  key_links:
    - from: "cli/open.go"
      to: "github.com/pkg/browser"
      via: "browser.OpenFile(htmlPath)"
      pattern: "browser\\.OpenFile"
    - from: "cli/open.go"
      to: "internal/search/service.go"
      via: "LoadCatalog for validating the JSON file is a real catalog"
      pattern: "svc\\.LoadCatalog"
---

<objective>
Implement the `storcat open` command that opens a catalog's HTML report in the system default browser, and clean up stubs.go.

Purpose: Users need a quick way to view the rich HTML catalog report from the command line. This is the cross-platform "view" command.
Output: cli/open.go (command implementation), cli/open_test.go (tests), stubs.go deleted
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-show-open-and-output-polish/10-RESEARCH.md

@cli/cli.go
@cli/stubs.go
@cli/list.go
@cli/cli_test.go
@pkg/models/catalog.go
@internal/search/service.go

<interfaces>
From pkg/models/catalog.go:
```go
type CatalogItem struct {
    Type     string         `json:"type"`
    Name     string         `json:"name"`
    Size     int64          `json:"size"`
    Contents []*CatalogItem `json:"contents"`
}
```

From internal/search/service.go:
```go
func NewService() *Service
func (s *Service) LoadCatalog(filePath string) (*models.CatalogItem, error)
```

From github.com/pkg/browser:
```go
func OpenFile(path string) error  // opens file in system browser; macOS: open, Linux: xdg-open, Windows: cmd /c start
```

From cli/cli_test.go:
```go
func captureOutput(fn func()) (string, string)
// TestRun_StubShow — currently asserts "not yet implemented", must be updated
// TestRun_StubOpen — currently asserts "not yet implemented", must be updated
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement open command with browser launch and HTML path derivation</name>
  <files>cli/open.go, cli/open_test.go</files>
  <read_first>
    - cli/stubs.go (current stub to replace)
    - cli/list.go (flag parsing pattern)
    - cli/cli_test.go (captureOutput helper, TestRun_StubOpen to update)
    - internal/search/service.go (LoadCatalog for validation)
    - pkg/models/catalog.go (CatalogItem struct)
  </read_first>
  <behavior>
    - TestRunOpen_NoArg: run `open` with no args, exit code 2, stderr contains "catalog file argument required"
    - TestRunOpen_NonJsonFile: run `open somefile.txt`, exit code 2, stderr contains "expected a .json catalog file"
    - TestRunOpen_NonExistentFile: run `open nonexistent.json`, exit code 1, stderr contains error
    - TestRunOpen_MissingHtml: create temp dir with catalog.json but no .html, run `open catalog.json`, exit code 1, stderr contains "HTML file not found"
    - TestRunOpen_Help: run `open --help`, exit code 0
    - TestRunOpen_HtmlPathDerivation: verify that for a file "/tmp/foo/my-catalog.json" the derived HTML path is "/tmp/foo/my-catalog.html" (tested indirectly via MissingHtml test error message)
    Note: Do NOT test actual browser.OpenFile — it requires a display. Test the error paths and argument validation only. The cross-platform launch is handled by pkg/browser (CLPC-05).
  </behavior>
  <action>
    Create cli/open_test.go with tests listed in behavior. Use captureOutput from cli_test.go.

    For test setup: create a temp catalog JSON (reuse writeTestCatalog from list_test.go or create inline). The MissingHtml test creates a .json without a .html. Do NOT create a test that actually calls browser.OpenFile — it would fail in CI/headless environments.

    Then create cli/open.go replacing the stub in stubs.go. Implementation:

    1. **runOpen(args []string) int** — follows interspersed flag pattern:
       - FlagSet "open" with ContinueOnError
       - No flags beyond --help (open has no flags per roadmap)
       - Usage func: "Usage: storcat open <catalog.json>\n\nOpen the catalog's HTML report in the default browser.\n"
       - Separate positional from flag args (same pattern as list.go)
       - Parse flags; if ErrHelp return 0
       - Validate: len(positional) < 1 -> stderr "storcat open: catalog file argument required\nUsage: storcat open <catalog.json>\n", return 2
       - Validate: !strings.HasSuffix(positional[0], ".json") -> stderr "storcat open: expected a .json catalog file\n", return 2
       - catalogPath := positional[0]
       - Validate catalog is readable: `svc := search.NewService(); _, err := svc.LoadCatalog(catalogPath)` — on error: stderr "storcat open: %v\n", return 1
       - Derive HTML path: `htmlPath := strings.TrimSuffix(catalogPath, ".json") + ".html"`
       - Stat HTML file: `if _, err := os.Stat(htmlPath); err != nil` -> stderr "storcat open: HTML file not found: %s\n", return 1
       - Open browser: `if err := browser.OpenFile(htmlPath); err != nil` -> stderr "storcat open: failed to open browser: %v\n", return 1
       - Return 0

    2. Imports: "flag", "fmt", "os", "strings", "github.com/pkg/browser", "storcat-wails/internal/search"

    After creating open.go, delete stubs.go entirely (runShow was removed in Plan 01, runOpen is now in open.go).

    Run `go mod tidy` after implementation to promote fatih/color and pkg/browser from indirect to direct dependencies.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat && go test ./cli/... -run "TestRunOpen" -v</automated>
  </verify>
  <acceptance_criteria>
    - cli/open.go contains `func runOpen(args []string) int`
    - cli/open.go contains `browser.OpenFile(htmlPath)`
    - cli/open.go contains `strings.TrimSuffix(catalogPath, ".json") + ".html"`
    - cli/open.go contains `strings.HasSuffix` for .json validation
    - cli/open.go contains `svc.LoadCatalog(`
    - cli/open_test.go contains `TestRunOpen_NoArg`
    - cli/open_test.go contains `TestRunOpen_MissingHtml`
    - cli/open_test.go contains `TestRunOpen_Help`
    - cli/stubs.go does NOT exist (deleted)
    - `go test ./cli/... -run TestRunOpen -v` exits 0
    - `go test ./cli/... -v` exits 0 (no regressions)
  </acceptance_criteria>
  <done>
    storcat open validates input, derives HTML path, opens browser via pkg/browser, stubs.go is deleted, all tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update stub tests in cli_test.go for implemented commands</name>
  <files>cli/cli_test.go</files>
  <read_first>
    - cli/cli_test.go (current stub tests to update)
  </read_first>
  <action>
    Update TestRun_StubShow and TestRun_StubOpen in cli_test.go. These currently assert "not yet implemented" in stderr and exit code 1. After implementation:

    **TestRun_StubShow** (rename to TestRun_ShowNoArg or keep name):
    - `show` with no args should now exit code 2 (usage error, not stub error)
    - stderr should contain "catalog file argument required" (not "not yet implemented")
    - Update assertion: `if code != 2` and `if !strings.Contains(stderr, "catalog file argument required")`

    **TestRun_StubOpen** (rename to TestRun_OpenNoArg or keep name):
    - `open` with no args should now exit code 2 (usage error, not stub error)
    - stderr should contain "catalog file argument required" (not "not yet implemented")
    - Update assertion: `if code != 2` and `if !strings.Contains(stderr, "catalog file argument required")`

    Keep the test function names as-is (TestRun_StubShow, TestRun_StubOpen) to minimize churn, but update the comments and assertions to reflect that these commands are now implemented.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat && go test ./cli/... -run "TestRun_Stub" -v</automated>
  </verify>
  <acceptance_criteria>
    - cli_test.go TestRun_StubShow asserts exit code 2 (not 1)
    - cli_test.go TestRun_StubShow asserts stderr contains "catalog file argument required"
    - cli_test.go TestRun_StubOpen asserts exit code 2 (not 1)
    - cli_test.go TestRun_StubOpen asserts stderr contains "catalog file argument required"
    - cli_test.go does NOT contain "not yet implemented" in any assertion
    - `go test ./cli/... -v` exits 0
  </acceptance_criteria>
  <done>
    All stub tests updated to match implemented command behavior, full test suite passes
  </done>
</task>

</tasks>

<verification>
- `go test ./cli/... -v` — all tests pass including new open tests, updated stub tests, and existing tests
- `go vet ./cli/...` — no issues
- `go build ./...` — compiles cleanly
- `ls cli/stubs.go` returns "no such file" — stubs.go is deleted
- `go mod tidy` produces no changes (deps already tidy)
</verification>

<success_criteria>
- storcat open <catalog.json> opens the HTML file in the system browser
- Missing args, non-.json files, missing HTML files produce clear stderr messages with non-zero exit codes
- stubs.go is deleted — no more stub functions in the CLI package
- All existing CLI tests continue to pass with updated assertions
</success_criteria>

<output>
After completion, create `.planning/phases/10-show-open-and-output-polish/10-02-SUMMARY.md`
</output>
