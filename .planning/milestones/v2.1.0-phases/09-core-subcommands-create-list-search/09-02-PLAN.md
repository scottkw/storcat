---
phase: 09-core-subcommands-create-list-search
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - cli/search.go
  - cli/search_test.go
  - cli/create.go
  - cli/create_test.go
  - cli/stubs.go
autonomous: true
requirements: [CLCM-01, CLCM-02, CLOF-01, CLOF-03]

must_haves:
  truths:
    - "User runs `storcat search <term> <dir>` and sees a table of matching files with path and catalog source"
    - "User runs `storcat search <term> <dir> --json` and gets a valid JSON array of SearchResult objects"
    - "User runs `storcat search` with missing args and gets a usage error on stderr with exit code 2"
    - "User runs `storcat create <dir>` and JSON+HTML catalog files are created on disk with a success message on stdout"
    - "User runs `storcat create <dir> --output <outdir>` and files are also copied to outdir"
    - "User runs `storcat create <dir> --title T --name N` and the catalog uses those values"
    - "User runs `storcat create <dir> --json` and gets a JSON object with paths and stats"
    - "Any error prints to stderr and exits non-zero"
  artifacts:
    - path: "cli/search.go"
      provides: "runSearch() implementation calling search.SearchCatalogs"
      contains: "func runSearch"
    - path: "cli/create.go"
      provides: "runCreate() implementation calling catalog.CreateCatalog"
      contains: "func runCreate"
    - path: "cli/search_test.go"
      provides: "Integration and unit tests for search command"
      contains: "TestRunSearch"
    - path: "cli/create_test.go"
      provides: "Integration and unit tests for create command"
      contains: "TestRunCreate"
    - path: "cli/stubs.go"
      provides: "Only show and open stubs remain"
  key_links:
    - from: "cli/search.go"
      to: "internal/search/service.go"
      via: "search.NewService().SearchCatalogs(term, dir)"
      pattern: "svc\\.SearchCatalogs"
    - from: "cli/create.go"
      to: "internal/catalog/service.go"
      via: "catalog.NewService().CreateCatalog(title, dir, name, output, nil)"
      pattern: "svc\\.CreateCatalog"
    - from: "cli/search.go"
      to: "cli/output.go"
      via: "printJSON() call for --json mode"
      pattern: "printJSON"
    - from: "cli/create.go"
      to: "cli/output.go"
      via: "printJSON() call for --json mode"
      pattern: "printJSON"
---

<objective>
Implement the `storcat search` and `storcat create` commands with table/JSON output, flag wiring, and comprehensive tests.

Purpose: Complete the three core CLI subcommands for Phase 9 — search and create build on the output helpers from Plan 01.
Output: Working `storcat search` and `storcat create` commands with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-subcommands-create-list-search/09-RESEARCH.md
@.planning/phases/09-core-subcommands-create-list-search/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output.go (created in 09-01) -->
From cli/output.go:
```go
func printJSON(v any) int       // marshals v to stdout as pretty JSON; returns exit code
func formatBytes(bytes int64) string  // "3.4M", "271K", "0B"
```

From cli/cli_test.go:
```go
func captureOutput(fn func()) (string, string)  // captures stdout, stderr
```

From internal/catalog/service.go:
```go
func NewService() *Service
func (s *Service) CreateCatalog(title, directoryPath, outputRoot string, copyToDirectory string, onProgress ProgressCallback) (*models.CreateCatalogResult, error)
// ProgressCallback is: type ProgressCallback func(path string)
```

From internal/search/service.go:
```go
func NewService() *Service
func (s *Service) SearchCatalogs(searchTerm, catalogDirectory string) ([]*models.SearchResult, error)
```

From pkg/models/catalog.go:
```go
type SearchResult struct {
    Catalog         string `json:"catalog"`
    CatalogFilePath string `json:"catalogFilePath"`
    Basename        string `json:"basename"`
    FullPath        string `json:"fullPath"`
    FullName        string `json:"fullName"`
    Type            string `json:"type"`
    Size            int64  `json:"size"`
}

type CreateCatalogResult struct {
    JsonPath     string `json:"jsonPath"`
    HtmlPath     string `json:"htmlPath"`
    FileCount    int    `json:"fileCount"`
    TotalSize    int64  `json:"totalSize"`
    CopyJsonPath string `json:"copyJsonPath,omitempty"`
    CopyHtmlPath string `json:"copyHtmlPath,omitempty"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement storcat search command with tests</name>
  <files>cli/search.go, cli/search_test.go, cli/stubs.go</files>
  <read_first>
    - cli/stubs.go (current runSearch stub — will be removed)
    - cli/output.go (printJSON, formatBytes from Plan 01)
    - cli/list.go (reference pattern: flag setup, dir resolution, service call, table output)
    - cli/list_test.go (reference pattern: writeTestCatalog helper, captureOutput usage)
    - internal/search/service.go (SearchCatalogs signature, lines 24-50)
    - pkg/models/catalog.go (SearchResult struct)
  </read_first>
  <behavior>
    - TestRunSearch_MissingArgs: `storcat search` (no args) exits 2, stderr contains "search term and directory required" or similar usage error
    - TestRunSearch_MissingDir: `storcat search myterm` (only 1 arg) — for simplicity, require both args; exits 2 with usage error. OR: default dir to cwd. Decision: default dir to cwd when only term provided (consistent with list behavior).
    - TestRunSearch_NoResults: `storcat search nonexistentterm <dir-with-catalog>` exits 0, stdout contains "No results found."
    - TestRunSearch_WithResults: create temp dir with catalog containing "test.txt", search for "test", exits 0, stdout contains "test.txt" in table
    - TestRunSearch_JSON: same as above but with --json, stdout is valid JSON array containing SearchResult objects
    - TestRunSearch_NonExistentDir: exits 1, stderr contains error
    - TestRunSearch_Help: `storcat search --help` exits 0
  </behavior>
  <action>
1. Create `cli/search.go` with package `cli`:
   - Imports: `"flag"`, `"fmt"`, `"os"`, `"path/filepath"`, `"storcat-wails/internal/search"`, `"storcat-wails/pkg/models"`, `"github.com/olekukonko/tablewriter"`
   - `func runSearch(args []string) int`:
     a. `fs := flag.NewFlagSet("search", flag.ContinueOnError)`
     b. `jsonFlag := fs.Bool("json", false, "Output results as JSON")`
     c. Set `fs.Usage` to print:
        ```
        Usage: storcat search <term> [directory] [flags]

        Search catalogs for files matching a term.
        If no directory is specified, the current directory is used.

        Flags:
          --json   Output results as JSON
        ```
     d. Parse: `if err := fs.Parse(args); err != nil { if err == flag.ErrHelp { return 0 }; return 2 }`
     e. Require at least 1 positional arg (search term): `if fs.NArg() < 1 { fmt.Fprintf(os.Stderr, "storcat search: search term required\nRun 'storcat search --help' for usage.\n"); return 2 }`
     f. `term := fs.Arg(0)`
     g. Directory: if `fs.NArg() >= 2` use `fs.Arg(1)`, else use `"."`. Resolve with `filepath.Abs()`. On error: print to stderr, return 1.
     h. `svc := search.NewService()`
     i. `results, err := svc.SearchCatalogs(term, dir)` — on error: `fmt.Fprintf(os.Stderr, "storcat search: %v\n", err)` return 1.
     j. If `*jsonFlag`: ensure nil slice becomes empty: `if results == nil { results = []*models.SearchResult{} }` then `return printJSON(results)`
     k. Else: call `printSearchTable(results)` return its result.

   - `func printSearchTable(results []*models.SearchResult) int`:
     a. If `len(results) == 0`: `fmt.Fprintln(os.Stdout, "No results found.")` return 0.
     b. `table := tablewriter.NewWriter(os.Stdout)`
     c. `table.SetHeader([]string{"File", "Path", "Type", "Size", "Catalog"})`
     d. `table.SetBorder(false)`
     e. `table.SetColumnSeparator("  ")`
     f. For each result: `table.Append([]string{r.Basename, r.FullPath, r.Type, formatBytes(r.Size), r.Catalog})`
     g. `table.Render()` return 0.

2. Edit `cli/stubs.go`: Remove the `runSearch` function. Keep `runCreate`, `runShow`, `runOpen`.

3. Create `cli/search_test.go` with package `cli_test`:
   - Reuse `writeTestCatalog` helper from list_test.go if in same package (it is — both are `cli_test`), OR duplicate it. Since Go test files in the same package share helpers, `writeTestCatalog` from list_test.go is accessible.
   - Tests as described in behavior section above.
   - For TestRunSearch_WithResults: create temp dir, call `writeTestCatalog(t, dir, "mycat")`, then `cli.Run([]string{"search", "test", dir}, "2.0.0")` — assert exit 0, stdout contains "test.txt".
   - For TestRunSearch_JSON: same setup, add `"--json"` arg, unmarshal stdout, verify array length > 0.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat && go test ./cli/... -run TestRunSearch -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - cli/search.go contains `func runSearch(args []string) int`
    - cli/search.go contains `svc.SearchCatalogs(term, dir)`
    - cli/search.go contains `printSearchTable(results)`
    - cli/search.go contains `fs.Bool("json"`
    - cli/search.go contains `printJSON(results)`
    - cli/stubs.go does NOT contain `func runSearch`
    - cli/search_test.go contains `func TestRunSearch_MissingArgs`
    - cli/search_test.go contains `func TestRunSearch_WithResults`
    - cli/search_test.go contains `func TestRunSearch_JSON`
    - `go test ./cli/... -run TestRunSearch -v -count=1` exits 0
  </acceptance_criteria>
  <done>storcat search command works with table output (file, path, type, size, catalog), JSON mode, cwd default, error handling. All search tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement storcat create command with tests</name>
  <files>cli/create.go, cli/create_test.go, cli/stubs.go</files>
  <read_first>
    - cli/stubs.go (current runCreate stub — will be removed after this task; check what remains)
    - cli/output.go (printJSON from Plan 01)
    - cli/list.go (reference pattern for flag setup and dir resolution)
    - internal/catalog/service.go (CreateCatalog signature at line 28, copyToDirectory behavior at lines 55-68)
    - pkg/models/catalog.go (CreateCatalogResult struct)
  </read_first>
  <behavior>
    - TestRunCreate_MissingArg: `storcat create` exits 2, stderr contains "directory argument required"
    - TestRunCreate_NonExistentDir: `storcat create /nonexistent` exits 1, stderr contains error
    - TestRunCreate_Success: create temp dir with a file, run `storcat create <dir>`, exits 0, stdout contains "Created catalog", JSON and HTML files exist in the source dir
    - TestRunCreate_WithFlags: `storcat create <dir> --title "My Title" --name mycat`, exits 0, creates mycat.json and mycat.html
    - TestRunCreate_WithOutput: `storcat create <dir> --output <outdir>`, exits 0, files exist in both source dir and outdir
    - TestRunCreate_JSON: `storcat create <dir> --json`, exits 0, stdout is valid JSON with jsonPath, htmlPath, fileCount, totalSize
    - TestRunCreate_Help: `storcat create --help` exits 0
  </behavior>
  <action>
1. Create `cli/create.go` with package `cli`:
   - Imports: `"flag"`, `"fmt"`, `"os"`, `"path/filepath"`, `"storcat-wails/internal/catalog"`
   - `func runCreate(args []string) int`:
     a. `fs := flag.NewFlagSet("create", flag.ContinueOnError)`
     b. Flags:
        - `title := fs.String("title", "", "Catalog title (default: directory name)")`
        - `name := fs.String("name", "", "Output filename stem (default: directory name)")`
        - `output := fs.String("output", "", "Output directory for copies")`
        - `jsonFlag := fs.Bool("json", false, "Output result as JSON")`
     c. Set `fs.Usage` to print:
        ```
        Usage: storcat create <directory> [flags]

        Create a JSON and HTML catalog from a directory.

        Flags:
          --title    Catalog title (default: directory name)
          --name     Output filename stem (default: directory name)
          --output   Copy catalog files to this directory
          --json     Output result as JSON
        ```
     d. Parse: `if err := fs.Parse(args); err != nil { if err == flag.ErrHelp { return 0 }; return 2 }`
     e. Require directory arg: `if fs.NArg() < 1 { fmt.Fprintf(os.Stderr, "storcat create: directory argument required\nRun 'storcat create --help' for usage.\n"); return 2 }`
     f. `dir, err := filepath.Abs(fs.Arg(0))` — on error: print to stderr, return 1.
     g. Validate dir exists and is a directory: `info, err := os.Stat(dir)` — if err or `!info.IsDir()`: `fmt.Fprintf(os.Stderr, "storcat create: %s is not a directory\n", dir)` return 1.
     h. Default title: `if *title == "" { *title = filepath.Base(dir) }`
     i. Default name: `if *name == "" { *name = filepath.Base(dir) }`
     j. Resolve output dir if provided: `outputDir := ""` then `if *output != "" { outputDir, err = filepath.Abs(*output); if err != nil { stderr, return 1 } }`
     k. `svc := catalog.NewService()`
     l. `result, err := svc.CreateCatalog(*title, dir, *name, outputDir, nil)` — on error: `fmt.Fprintf(os.Stderr, "storcat create: %v\n", err)` return 1.
        Note: `onProgress` is nil — CLI does not show progress (per REQUIREMENTS.md: "Progress bar on create" is out of scope).
        Note: `copyToDirectory` = `outputDir` — when `--output` is specified, files are written to source dir AND copied to output dir (using existing service behavior, no service modification).
     m. If `*jsonFlag`: `return printJSON(result)`
     n. Else: print human-readable summary to stdout:
        ```go
        fmt.Fprintf(os.Stdout, "Created catalog: %s\n", result.JsonPath)
        fmt.Fprintf(os.Stdout, "  HTML:  %s\n", result.HtmlPath)
        fmt.Fprintf(os.Stdout, "  Files: %d\n", result.FileCount)
        fmt.Fprintf(os.Stdout, "  Size:  %s\n", formatBytes(result.TotalSize))
        if result.CopyJsonPath != "" {
            fmt.Fprintf(os.Stdout, "  Copied to: %s\n", filepath.Dir(result.CopyJsonPath))
        }
        ```
        return 0.

2. Edit `cli/stubs.go`: Remove the `runCreate` function. After this, stubs.go should contain ONLY `runShow` and `runOpen`. If the imports become unused (e.g., if only `runShow` and `runOpen` remain and they still use `"flag"`, `"fmt"`, `"os"`), keep the necessary imports.

3. Create `cli/create_test.go` with package `cli_test`:
   - Tests as described in behavior section.
   - For TestRunCreate_Success: `dir := t.TempDir()`, write a test file `os.WriteFile(filepath.Join(dir, "hello.txt"), []byte("hello world"), 0644)`, run `cli.Run([]string{"create", dir}, "2.0.0")`, assert exit 0, assert stdout contains "Created catalog", assert `filepath.Join(dir, filepath.Base(dir)+".json")` exists, assert `filepath.Join(dir, filepath.Base(dir)+".html")` exists.
   - For TestRunCreate_WithFlags: same setup, run with `"--title", "Test Title", "--name", "mycat"`, assert `filepath.Join(dir, "mycat.json")` exists and `filepath.Join(dir, "mycat.html")` exists.
   - For TestRunCreate_WithOutput: `dir := t.TempDir()` (source), `outDir := t.TempDir()` (output), write test file in dir, run with `"--output", outDir`, assert files exist in both dir and outDir.
   - For TestRunCreate_JSON: same setup with `"--json"` flag, unmarshal stdout into `models.CreateCatalogResult`, verify `FileCount > 0` and `JsonPath != ""`.
   - IMPORTANT: Tests that call CreateCatalog write real files. `t.TempDir()` auto-cleans. Always use temp dirs.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat && go test ./cli/... -run TestRunCreate -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - cli/create.go contains `func runCreate(args []string) int`
    - cli/create.go contains `svc.CreateCatalog(*title, dir, *name, outputDir, nil)`
    - cli/create.go contains `fs.String("title"` and `fs.String("name"` and `fs.String("output"`
    - cli/create.go contains `fs.Bool("json"`
    - cli/create.go contains `printJSON(result)`
    - cli/create.go contains `fmt.Fprintf(os.Stdout, "Created catalog:`
    - cli/stubs.go contains ONLY `func runShow` and `func runOpen` (no runCreate, runSearch, runList)
    - cli/create_test.go contains `func TestRunCreate_MissingArg`
    - cli/create_test.go contains `func TestRunCreate_Success`
    - cli/create_test.go contains `func TestRunCreate_WithFlags`
    - cli/create_test.go contains `func TestRunCreate_WithOutput`
    - cli/create_test.go contains `func TestRunCreate_JSON`
    - `go test ./cli/... -run TestRunCreate -v -count=1` exits 0
    - `go test ./cli/... -v -count=1` exits 0 (full cli suite including list, search, create, and Phase 8 tests)
  </acceptance_criteria>
  <done>storcat create command works with --title/--name/--output/--json flags, writes JSON+HTML to source dir, copies to --output when specified, prints human-readable summary or JSON. All create tests pass, full CLI suite green.</done>
</task>

</tasks>

<verification>
1. `go test ./cli/... -v -count=1` — all tests pass (Phase 8 existing + list + search + create)
2. `go test ./... -count=1` — full suite green
3. `go vet ./...` — no issues
4. `go build ./...` — compiles cleanly
5. Manual smoke test (executor should run): create a temp dir with files, then:
   - `go run . create /tmp/test-storcat`
   - `go run . list /tmp/test-storcat`
   - `go run . search "some-file" /tmp/test-storcat`
   - `go run . list /tmp/test-storcat --json | head`
</verification>

<success_criteria>
- `storcat search <term> <dir>` prints a table of matching files
- `storcat search <term> <dir> --json` outputs valid JSON array
- `storcat search <term>` (no dir) defaults to cwd
- `storcat search` (no args) prints usage error, exits 2
- `storcat create <dir>` creates JSON+HTML files, prints confirmation
- `storcat create <dir> --title T --name N --output O` respects all flags
- `storcat create <dir> --json` outputs JSON result object
- `storcat create` (no dir) prints usage error, exits 2
- All Phase 8 tests still pass (no regression)
- stubs.go contains only show and open stubs
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-subcommands-create-list-search/09-02-SUMMARY.md`
</output>
