---
phase: 07-verification-merge
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified: []
autonomous: false
requirements:
  - REL-01

must_haves:
  truths:
    - "All three tabs (Create, Search, Browse) complete their primary operations on macOS"
    - "The merged main branch contains the Go/Wails codebase with no bloat"
    - "A v2.0.0 tag marks the merge point on main"
  artifacts:
    - path: "build/bin/StorCat.app"
      provides: "Built macOS application for smoke testing"
  key_links:
    - from: "main branch"
      to: "feature/go-refactor-2.0.0-clean"
      via: "git merge"
      pattern: "v2\\.0\\.0"
---

<objective>
Smoke test the built application on macOS, then merge the verified feature branch to main with a version tag.

Purpose: Human verification of the three core tabs confirms feature parity before the final merge. The merge itself completes the v2.0.0 migration milestone.
Output: main branch updated with Go/Wails codebase, tagged v2.0.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-verification-merge/07-RESEARCH.md
@.planning/phases/07-verification-merge/07-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Three-tab smoke test on macOS</name>
  <files>build/bin/StorCat.app</files>
  <read_first>
    - .planning/phases/07-verification-merge/07-RESEARCH.md (smoke test pattern in "Three-Tab Smoke Test Pattern" section)
  </read_first>
  <action>
    Launch the built StorCat.app and have the user verify all three tabs work correctly.
    The app was built in Plan 01 at build/bin/StorCat.app with version injection.
    Present the user with the verification steps below and wait for their confirmation.

    **Create Tab:**
    1. Switch to the Create tab
    2. Select a test directory (any directory with files)
    3. Select an output directory
    4. Click Create
    5. Verify: catalog .json and .html files appear in output dir, fileCount > 0 displayed in UI

    **Search Tab:**
    1. Switch to the Search tab
    2. Select the directory containing the catalog created above
    3. Enter a search term that matches files in the catalog
    4. Click Search
    5. Verify: results table populates with matching file entries

    **Browse Tab:**
    1. Switch to the Browse tab
    2. Select the directory containing the catalog
    3. Verify: file list loads with size and date columns populated
    4. Click a catalog entry to view its HTML representation

    **Additional checks:**
    - Drag the window by its header bar (macOS drag region works)
    - Check version string in settings/about (should show "2.0.0" or "dev" depending on build)
    - Resize window, quit, relaunch — window size should persist
  </action>
  <verify>
    <automated>test -d /Users/ken/dev/storcat/build/bin/StorCat.app && echo "App exists for testing"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms Create tab produces catalog files and displays fileCount
    - User confirms Search tab returns results in the table
    - User confirms Browse tab shows catalogs with size and date columns
    - User confirms window drag works on macOS header
  </acceptance_criteria>
  <done>User has approved all three tabs as working correctly on macOS. Feature parity with Electron v1.2.3 confirmed by human tester.</done>
</task>

<task type="auto">
  <name>Task 2: Merge feature branch to main and tag v2.0.0</name>
  <files></files>
  <read_first>
    - .planning/phases/07-verification-merge/07-RESEARCH.md (merge strategy section — fast-forward recommended)
  </read_first>
  <action>
    Execute the merge sequence. Use merge commit (--no-ff) to create a clear milestone marker in git history:

    ```bash
    # 1. Ensure we're on the feature branch and everything is committed
    cd /Users/ken/dev/storcat
    git status

    # 2. Switch to main
    git checkout main

    # 3. Pull to ensure main is current
    git pull origin main

    # 4. Merge with a merge commit for clear milestone marker
    git merge --no-ff feature/go-refactor-2.0.0-clean -m "feat: StorCat v2.0.0 — Go/Wails migration

    Complete backend rewrite from Electron/Node.js to Go/Wails.
    Full feature parity with Electron v1.2.3.
    97% binary size reduction (415MB -> ~10MB).

    Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"

    # 5. Tag the release
    git tag -a v2.0.0 -m "StorCat v2.0.0 — Go/Wails migration"

    # 6. Verify merge succeeded
    git log --oneline -5
    git tag -l "v2.0.0"
    ```

    Do NOT push to remote — the user will push when ready.
    If the merge fails for any reason, stop and report the error.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat && git log --oneline -1 | grep -q "v2.0.0" && git tag -l "v2.0.0" | grep -q "v2.0.0" && echo "MERGE AND TAG VERIFIED"</automated>
  </verify>
  <acceptance_criteria>
    - git branch --show-current returns "main"
    - git log --oneline -1 contains "v2.0.0"
    - git tag -l "v2.0.0" returns "v2.0.0"
    - git status shows a clean working tree (no uncommitted changes beyond untracked files)
  </acceptance_criteria>
  <done>Feature branch merged to main with a merge commit. v2.0.0 tag applied. Ready for push when user decides.</done>
</task>

</tasks>

<verification>
- Three-tab smoke test passes (human verified)
- `git branch --show-current` returns `main`
- `git log --oneline -1` shows the merge commit with "v2.0.0"
- `git tag -l "v2.0.0"` returns `v2.0.0`
- `git status` shows clean working tree
</verification>

<success_criteria>
The Go/Wails v2.0.0 codebase is merged to main with a version tag. All three tabs verified working on macOS by human tester. The milestone is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/07-verification-merge/07-02-SUMMARY.md`
</output>
