---
phase: 27-catalog-actions-watch
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - go.mod
  - go.sum
  - internal/catalog/duplicate.go
  - internal/catalog/duplicate_test.go
  - internal/osutil/trash.go
  - internal/osutil/trash_test.go
  - app.go
  - frontend/src/services/wailsAPI.ts
  - frontend/wailsjs/go/main/App.d.ts
  - frontend/wailsjs/go/main/App.js
autonomous: true
requirements: [ACT-03, ACT-04, ACT-05]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Duplicating a catalog produces a new .json whose filename root is the original's plus -copy, and -copy-2, -copy-3 and onward when earlier suffixes are taken."
    - "Duplicating a catalog that has an .html copies the .html too, under the same suffixed root; a catalog with no .html produces only a .json."
    - "A duplicate is byte-identical to its source — the title is inherited verbatim and only the filename root changes."
    - "A suffix candidate is only taken when NEITHER the .json nor the .html at that root already exists, so a duplicate can never clobber an orphaned .html."
    - "Delete moves files to the OS Trash and never permanently deletes: no code path, error path, or retry path in this phase performs an unrecoverable removal."
    - "A failed trash operation returns the real underlying error verbatim, and the catalog files are left where they were."
    - "Delete confirmation names both file paths: DeleteCatalog receives the .json path and a deleteHtml flag, and derives the .html by the same-directory/same-basename convention already used in internal/search/service.go and app.go."
    - "Re-invoking delete with the same paths after a partial failure re-attempts only the paths still on disk and returns success once none remain — no what-already-succeeded bookkeeping exists."
    - "A catalog whose filename contains non-ASCII characters is handed to the trash helper as the byte-identical path os.ReadDir produced, never a re-encoded or shell-escaped form."
    - "DeleteCatalog with an empty jsonPath, or with a path resolving outside the configured catalog directory, returns an error and trashes nothing."
    - "Rename, Duplicate and Delete each reject a path outside the configured catalog directory before any read, write, or trash call."
    - statement: "Two concurrent DeleteCatalog calls for the same catalog cannot both report success while leaving a file behind — the second call's already-missing paths skip through the not-exist branch before the trash seam is reached."
      verification: backstop
  artifacts:
    - path: "internal/catalog/duplicate.go"
      provides: "DuplicateCatalog with the -copy / -copy-N collision loop"
      exports: ["DuplicateCatalog"]
    - path: "internal/osutil/trash.go"
      provides: "containment-gated OS Trash helper wrapping wastebasket.Trash behind a swappable package seam"
      exports: ["TrashPaths"]
    - path: "internal/osutil/trash_test.go"
      provides: "ACT-04/ACT-05 tests that never touch a real OS Trash"
      contains: "func TestTrashPaths"
    - path: "go.mod"
      provides: "wastebasket/v2 v2.0.3 pinned"
      contains: "github.com/Bios-Marcel/wastebasket/v2 v2.0.3"
  key_links:
    - from: "app.go"
      to: "internal/osutil/trash.go"
      via: "App.DeleteCatalog calls osutil.TrashPaths after resolving and containment-checking every path"
      pattern: "osutil\\.TrashPaths"
    - from: "internal/osutil/trash.go"
      to: "internal/osutil/reveal.go"
      via: "TrashPaths reuses ContainsPath and allowedRevealExtensions rather than a second containment implementation"
      pattern: "ContainsPath"
    - from: "app.go"
      to: "internal/catalog/duplicate.go"
      via: "App.DuplicateCatalog calls catalog.DuplicateCatalog after osutil.ContainsPath"
      pattern: "catalog\\.DuplicateCatalog"
  prohibitions:
    - statement: "MUST NOT permanently delete a catalog file under any code path, error path, or retry path; a Trash failure must surface the real OS error and stop, and no control, link, or copy anywhere in the delete flow may offer permanent deletion as an alternative."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT report a destructive action as succeeded when the underlying filesystem operation returned an error; the real system error string is surfaced verbatim, never replaced by a generic or reassuring message."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT overwrite or clobber an existing file when duplicating — a suffix candidate is free only when every file at that root is absent."
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Expand the tracer slice sideways into the two remaining catalog operations: duplicate a catalog to a suffixed filename root, and move a catalog to the OS Trash behind a containment gate that never falls back to permanent deletion.

Purpose: these are ACT-03, ACT-04 and ACT-05's backend halves. Both reuse the seams the tracer proved — `WriteFileAtomic` as the one write primitive, `osutil.ContainsPath` in front of every path-taking binding, the regenerated bridge, the `wailsAPI` wrapper shape — so the only genuinely new surface is the collision-suffix loop and the third-party Trash call.

Output: `catalog.DuplicateCatalog`, `osutil.TrashPaths`, the `App.DuplicateCatalog`/`App.DeleteCatalog` bindings, their `wailsAPI` wrappers, and `github.com/Bios-Marcel/wastebasket/v2 v2.0.3` in `go.mod`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-catalog-actions-watch/27-CONTEXT.md
@.planning/phases/27-catalog-actions-watch/27-RESEARCH.md
@.planning/phases/27-catalog-actions-watch/27-PATTERNS.md
@.planning/phases/27-catalog-actions-watch/27-VALIDATION.md
@.planning/phases/27-catalog-actions-watch/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DuplicateCatalog — the -copy / -copy-N collision loop</name>

  <read_first>
    - `internal/catalog/service.go` lines 606-623 — `copyFile`, which reads the source fully and routes the write through `WriteFileAtomic`, with the doc comment explaining why `os.Create` + `io.Copy` was rejected. This is the exact pattern to reuse.
    - `internal/catalog/atomicwrite.go` — the post-`27-02` hardened `WriteFileAtomic` this task writes through.
    - `internal/catalog/rename.go` (from `27-01`) — the file-naming and error-wrapping conventions established one plan earlier: `fmt.Errorf("<verb> %s: %w", path, err)`.
    - `internal/search/service.go` line 189 — `strings.TrimSuffix(filePath, ".json") + ".html"`, the repo's one `.json`/`.html` pairing convention.
  </read_first>

  <files>internal/catalog/duplicate.go, internal/catalog/duplicate_test.go</files>

  <behavior>
    Write `internal/catalog/duplicate_test.go` first, red, then green. Table-driven, `package catalog`, `t.TempDir()`.

    - `TestDuplicateCatalog_FirstCopy`: source `photos.json` with no existing copies produces `photos-copy.json`, and the returned path is that file.
    - `TestDuplicateCatalog_SecondCopy`: with `photos-copy.json` already present, produces `photos-copy-2.json`.
    - `TestDuplicateCatalog_ThirdCopy`: with `photos-copy.json` and `photos-copy-2.json` present, produces `photos-copy-3.json`.
    - `TestDuplicateCatalog_SkipsRootWithOrphanHTML`: with only `photos-copy.html` present (no `photos-copy.json`), the next free root is `photos-copy-2` — an orphaned HTML must never be clobbered.
    - `TestDuplicateCatalog_CopiesHTMLWhenPresent`: source has `photos.html`; afterwards `photos-copy.html` exists and is byte-identical to the source HTML.
    - `TestDuplicateCatalog_NoHTMLCopiesOnlyJSON`: source has no `.html`; afterwards no `photos-copy.html` exists and no error is returned.
    - `TestDuplicateCatalog_IsByteIdentical`: the duplicated `.json` bytes equal the source `.json` bytes exactly, including any root `title` key — the title is inherited verbatim, only the filename root changes.
    - `TestDuplicateCatalog_LeavesSourceUntouched`: the source `.json` and `.html` are byte-identical before and after.
    - `TestDuplicateCatalog_RejectsNonJSON`: a path whose extension is not `.json` returns an error and creates nothing.
    - `TestDuplicateCatalog_RejectsMissingSource`: a nonexistent source returns an error and creates nothing.
  </behavior>

  <action>
Create `internal/catalog/duplicate.go`, package `catalog`, exporting exactly one function:

`DuplicateCatalog(jsonPath string) (string, error)` — returns the new `.json` path.

Sequence:
- Reject a `jsonPath` whose `filepath.Ext` is not `.json` with a wrapped error, before touching the filesystem.
- `dir := filepath.Dir(jsonPath)`; `root := strings.TrimSuffix(filepath.Base(jsonPath), ".json")`.
- Find the free destination root with an unexported helper `nextCopyRoot(dir, root string) (string, error)`. Candidates in order: `root + "-copy"`, then `root + "-copy-2"`, `root + "-copy-3"`, and so on — this is `27-CONTEXT.md`'s locked suffix scheme, chosen for readability over a bare numeric suffix. A candidate is **free only when both `<candidate>.json` and `<candidate>.html` are absent**, checked with `os.Stat` and `os.IsNotExist`. Checking both extensions is what stops a duplicate from clobbering an orphaned HTML left behind by a partial delete. A `os.Stat` error that is not "not exists" aborts with a wrapped error rather than being treated as free. Cap the search at 1000 candidates and return a wrapped error past the cap, so a pathological directory cannot spin forever.
- Copy the `.json` with the same read-then-`WriteFileAtomic` shape `service.go`'s `copyFile` uses: `os.ReadFile(src)` then `WriteFileAtomic(dst, data, 0644)`. Do not call the unexported `copyFile` method — it hangs off `*Service` and `DuplicateCatalog` is a package-level function with no service dependency; write the four-line equivalent inline rather than restructuring `Service`.
- Derive the source HTML as `strings.TrimSuffix(jsonPath, ".json") + ".html"`. `os.Stat` it; `os.IsNotExist` → done, return the new JSON path with a nil error. Otherwise copy it the same way to `<dir>/<candidate>.html`.
- If the HTML copy fails after the JSON copy succeeded, return the wrapped error **and** the new JSON path, so the caller can report exactly what landed. Do not attempt a rollback: removing a file the user can already see would be a second, unasked-for destructive act.

**The title is inherited verbatim.** This is `27-CONTEXT.md`'s post-research resolution: ACT-03 speaks to the filename, not the title, and a duplicate genuinely is an identical copy. Do not read, parse, or rewrite the JSON — this is a byte copy. A user who wants a different title renames afterwards, which is exactly what `27-01` shipped.
  </action>

  <verify>
    <automated>go test ./internal/catalog/... -run TestDuplicateCatalog -race -count=1 -v && go test ./... -race -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/catalog/... -run TestDuplicateCatalog -race -count=1` passes with all ten cases from `<behavior>` present.
    - `grep -Eq 'func DuplicateCatalog\(jsonPath string\) \(string, error\)' internal/catalog/duplicate.go` succeeds.
    - `grep -q 'func nextCopyRoot' internal/catalog/duplicate.go` succeeds.
    - Every write in the file routes through the atomic primitive: `grep -c 'WriteFileAtomic' internal/catalog/duplicate.go` is at least `2`, and `! grep -Eq 'os\.WriteFile|os\.Create' internal/catalog/duplicate.go` succeeds. <!-- planner-discipline-allow: os\.WriteFile|os\.Create -->
    - The file does not parse the catalog JSON: `! grep -Eq 'json\.Unmarshal|json\.Marshal' internal/catalog/duplicate.go` succeeds.
    - `TestDuplicateCatalog_SkipsRootWithOrphanHTML` is present and green — the both-extensions freeness check is real, not assumed.
    - `go test ./... -race -count=1` is green.
  </acceptance_criteria>

  <done>A catalog duplicates to the next free `-copy`/`-copy-N` root, copies its `.html` when one exists, is byte-identical to its source, and never overwrites anything.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TrashPaths — containment-gated OS Trash with a swappable seam and no removal fallback</name>

  <read_first>
    - `internal/osutil/reveal.go` — the whole file. `ContainsPath` (its `filepath.Abs` + `EvalSymlinks` + `filepath.Rel` contract and why `strings.HasPrefix` is wrong), `allowedRevealExtensions`, and `RevealInFileManager`'s validate-everything-before-acting ordering are all reused here.
    - `internal/osutil/reveal_test.go` — the table-driven conventions in this package, including how containment cases are constructed without spawning a process.
    - `internal/osutil/openexternal.go` — `ResolveContainedFileURL`, Phase 26's second consumer of the same containment control, for the error-wrapping style.
    - `27-RESEARCH.md` Common Pitfalls #1 and #3, and Assumption A4 — the macOS AppleScript-interpolation finding and the partial-multi-path-failure semantics that make the retry UX free.
    - `go.mod` — the current `require` blocks and the existing `golang.org/x/sys v0.30.0` indirect entry.
  </read_first>

  <files>go.mod, go.sum, internal/osutil/trash.go, internal/osutil/trash_test.go</files>

  <behavior>
    Write `internal/osutil/trash_test.go` first, red, then green. `package osutil` (in-package, so the seam is directly assignable), `t.TempDir()`, and a fake trash function recording the paths it received. **No test may reach a real OS Trash.**

    - `TestTrashPaths_PassesResolvedPathsToSeam`: two real files inside the catalog directory produce exactly those two resolved paths at the seam, in the order supplied.
    - `TestTrashPaths_RejectsOutsideCatalogDir`: a real `.json` in a sibling directory returns an error mentioning the configured catalog directory, and the seam is never called.
    - `TestTrashPaths_RejectsSiblingPrefixDirectory`: a catalog directory of `<tmp>/catalogs` and a file in `<tmp>/catalogs-evil` is rejected — the `filepath.Rel` behavior a naive prefix check would get wrong.
    - `TestTrashPaths_RejectsDisallowedExtension`: a `.txt` file inside the catalog directory is rejected and the seam is never called.
    - `TestTrashPaths_RejectsDirectory`: a directory path inside the catalog directory is rejected.
    - `TestTrashPaths_SkipsMissingPath`: two paths where one no longer exists — only the surviving one reaches the seam, and the call returns the seam's result. This is the retry case.
    - `TestTrashPaths_AllMissingReturnsNil`: every supplied path is already gone — the seam is never called and the result is `nil`. Idempotency: a repeated delete of an already-trashed catalog succeeds.
    - `TestTrashPaths_EmptyCatalogDirIsAnError`: an empty `catalogDir` returns an error and the seam is never called.
    - `TestTrashPaths_PropagatesSeamErrorVerbatim`: a seam returning a sentinel error yields an error for which `errors.Is` matches that sentinel — the real OS error must survive to the UI, not be replaced.
    - `TestTrashPaths_NonASCIIPathReachesSeamByteIdentical`: a file named with non-ASCII and space characters arrives at the seam as the exact resolved path, with no escaping, quoting, or re-encoding applied by this code.
    - `TestTrashPaths_NoPathsIsNil`: calling with zero paths returns `nil` without invoking the seam.
  </behavior>

  <action>
Add the dependency, then build a thin, heavily-gated wrapper.

**1. Add the module.** Run `go get github.com/Bios-Marcel/wastebasket/v2@v2.0.3` and `go mod tidy`. `27-RESEARCH.md` verified this exact version against the Go module proxy (`go list -m -versions` returned `v2.0.0 v2.0.1 v2.0.2 v2.0.3`), read all four platform backends in full, and recorded a **Package Legitimacy Audit** verdict of OK/Approved — the package is also named by the ROADMAP as this milestone's one pre-approved new Go dependency. It is neither `[ASSUMED]` nor `[SUS]`, so no blocking human checkpoint is inserted. Note in the SUMMARY that the automated `package-legitimacy` seam covers npm/pypi/crates only and Go verification was therefore manual. `go.mod` must end up with the exact pinned version, not a range.

**2. Create `internal/osutil/trash.go`, package `osutil`.**

Declare the test seam as a package-level variable so `trash_test.go` (same package) can swap it: a variable of type `func(paths ...string) error` initialized to `wastebasket.Trash`. Keep it unexported — the seam exists for this package's own tests, and exporting it would create a public switch for turning off the Trash guarantee.

Export exactly one function: `TrashPaths(catalogDir string, paths ...string) error`.

Sequence, mirroring `RevealInFileManager`'s validate-everything-before-acting ordering:
- `catalogDir == ""` → wrapped error, nothing attempted. Fail closed, exactly as `GetCatalogHtmlPath` does.
- For each supplied path, in order:
  - `os.Lstat` it. `os.IsNotExist` → **skip this path and continue**, do not error. This is what makes a retry after a partial failure correct with no extra machinery: `27-RESEARCH.md` verified that every `wastebasket` backend already `continue`s past a missing path, and re-invoking with the same path set therefore re-attempts only what is still there. Any other stat error → wrapped error.
  - `filepath.Abs`, then `filepath.EvalSymlinks`.
  - Reject a non-regular file (this rejects directories too) with a wrapped error.
  - Reject an extension outside `allowedRevealExtensions` (`.json`, `.html`) — reuse the existing map, do not declare a second one.
  - `ContainsPath(catalogDir, resolved)`; a resolve error and a `false` result each get their own wrapped error naming the configured catalog directory. Reuse the existing function; a second containment implementation is forbidden by `27-PATTERNS.md`'s shared-pattern rule.
  - Append `resolved` to the list to hand over.
- If the list is empty → return `nil`. Everything was already gone; there is nothing left to do and nothing failed.
- Call the seam with the resolved list and wrap its error with `%w` so `errors.Is`/`errors.As` still reach the original. The error text the frontend renders is this OS error verbatim.

**This file performs no filesystem removal of any kind.** The only deletion mechanism it may reach is the injected seam. `27-RESEARCH.md` verified by direct source read that `wastebasket.Trash` never falls back to permanent deletion on any of its three platform backends — every failure path returns an error — so ACT-05 is satisfied by the library itself *and* by the absence of any local fallback. An acceptance grep below enforces the latter.

**Why the containment gate is doubly important here.** `wastebasket`'s macOS backend shells out to `osascript` with a hand-built AppleScript string, escaping only literal double-quote characters in the path before interpolating it. That is weaker than this codebase's own convention of passing every path as its own `exec.Command` argv element, and it is third-party code this phase cannot change. The gate is what bounds the worst case to "a `.json`/`.html` file the app itself already resolved inside the configured catalog directory." Record this in the file's doc comment as an accepted, upstream-owned residual risk — not as something local code has fully closed.

Document at the top of the file that the paths this helper receives always originate from `BrowseCatalogs`'s own `os.ReadDir` listing, never from renderer free-text, and that a future feature which lets a user type an arbitrary filename would need this constraint revisited (`27-RESEARCH.md` Assumption A4).
  </action>

  <verify>
    <automated>go build ./... && go test ./internal/osutil/... -run TestTrashPaths -race -count=1 -v && go test ./... -race -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/osutil/... -run TestTrashPaths -race -count=1` passes with all eleven cases from `<behavior>` present.
    - `grep -Eq 'func TrashPaths\(catalogDir string, paths \.\.\.string\) error' internal/osutil/trash.go` succeeds.
    - No filesystem-removal call exists in the helper — the seam is the only deletion mechanism: `! grep -Eq 'os\.Remove|os\.RemoveAll|os\.Truncate' internal/osutil/trash.go` succeeds. <!-- planner-discipline-allow: os\.Remove|os\.RemoveAll|os\.Truncate -->
    - No shell is invoked and no command string is built here: `! grep -Eq 'exec\.Command|os/exec' internal/osutil/trash.go` succeeds. <!-- planner-discipline-allow: exec\.Command|os/exec -->
    - Containment is reused, not reimplemented: `grep -c 'ContainsPath' internal/osutil/trash.go` is at least `1`, and `! grep -q 'func ContainsPath' internal/osutil/trash.go` succeeds.
    - The extension allowlist is reused: `grep -c 'allowedRevealExtensions' internal/osutil/trash.go` is at least `1`.
    - `grep -c 'github.com/Bios-Marcel/wastebasket/v2 v2.0.3' go.mod` returns `1` (exact pin, in the direct `require` block).
    - `grep -c 'Bios-Marcel/wastebasket' internal/osutil/trash_test.go` returns `0` — the tests exercise the seam, never the real library.
    - `go build ./... && go vet ./...` green.
  </acceptance_criteria>

  <done>Every path handed to the OS Trash has been stat'd, resolved, extension-checked and containment-checked first; a missing path is skipped so retry is free; and the file contains no local removal fallback.</done>
</task>

<task type="auto">
  <name>Task 3: DuplicateCatalog and DeleteCatalog bindings, bridge regeneration, service wrappers</name>

  <read_first>
    - `app.go` lines 740-822 — `GetCatalogHtmlPath` and `OpenExternal` (the containment-gated binding shape) plus `App.RenameCatalog` as landed by `27-01`, which is the sibling this task must match exactly in error wording and check ordering.
    - `app.go` lines 755-770 — the existing `.json` → `.html` derivation, so the delete binding uses the same convention rather than inventing a second one.
    - `internal/catalog/duplicate.go` and `internal/osutil/trash.go` — the two functions this task binds, as written in Tasks 1 and 2.
    - `frontend/src/services/wailsAPI.ts` lines 1-60 and the `renameCatalog` method added by `27-01` — the import block, `wailsError`, and the wrapper shape to match.
    - `pkg/models/catalog.go` — `CatalogMetadata`, in case the duplicate binding's return type is widened later (this task returns a plain path string).
  </read_first>

  <files>app.go, frontend/src/services/wailsAPI.ts, frontend/wailsjs/go/main/App.d.ts, frontend/wailsjs/go/main/App.js</files>

  <action>
Bind both operations with the identical gate `App.RenameCatalog` already carries, regenerate the bridge, and add the two service wrappers.

**1. `app.go` — `func (a *App) DuplicateCatalog(jsonPath string, catalogDir string) (string, error)`.**
Place it directly after `App.RenameCatalog`. Sequence: empty `catalogDir` → wrapped error; `filepath.Abs`; `filepath.EvalSymlinks`; `.json` extension check; `osutil.ContainsPath(catalogDir, resolved)` with a distinct wrapped error for a resolve failure and for a `false` result; then `catalog.DuplicateCatalog(resolved)`, returning its new-path string and its error wrapped as `fmt.Errorf("duplicate %s: %w", jsonPath, err)`. Return the absolute new `.json` path so the frontend can select the new catalog if it chooses to.

**2. `app.go` — `func (a *App) DeleteCatalog(jsonPath string, catalogDir string, deleteHtml bool) error`.**
Same gate on `jsonPath`. After it passes:
- Derive the HTML companion with the repo's one convention: `strings.TrimSuffix(resolved, ".json") + ".html"`. Do not introduce a second derivation and do not accept an HTML path from the renderer — the frontend must not be able to name an arbitrary second file for deletion.
- Build the path list: always the resolved `.json`; append the derived `.html` **only when `deleteHtml` is true**. Do not stat the HTML here — `osutil.TrashPaths` already skips a path that is not there, and duplicating the check would create a second place for the two to disagree.
- Call `osutil.TrashPaths(catalogDir, paths...)` and return its error wrapped as `fmt.Errorf("delete %s: %w", jsonPath, err)`.
- `TrashPaths` re-runs its own containment gate on every path, including the derived HTML. That is deliberate belt-and-braces: the binding's gate covers the caller-supplied path, the helper's covers whatever the binding derived, and neither depends on the other having run.
- There is no permanent-deletion branch, no "force" parameter, and no fallback of any kind in this method. ACT-05 is not a default that may be softened.

**3. Regenerate the Wails bridge.** Run the same generation step `27-01` used so `frontend/wailsjs/go/main/App.d.ts` and `App.js` gain `DuplicateCatalog` and `DeleteCatalog`. Commit the generated files.

**4. `frontend/src/services/wailsAPI.ts` — two wrappers.**
Add `DuplicateCatalog` and `DeleteCatalog` to the generated-binding import block. Then:
- `duplicateCatalog: async (jsonPath: string, catalogDir: string)` → on success `{ success: true as const, jsonPath: <the returned path> }`; on catch `wailsError(error)`.
- `deleteCatalog: async (jsonPath: string, catalogDir: string, deleteHtml: boolean)` → on success `{ success: true as const }`; on catch `wailsError(error)`.
Route every catch through the existing `wailsError` helper — never a hand-written failure literal, which is the drift that helper was extracted to prevent.
  </action>

  <verify>
    <automated>go build ./... && go vet ./... && go test ./... -race -count=1 && cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -Eq 'func \(a \*App\) DuplicateCatalog\(jsonPath string, catalogDir string\) \(string, error\)' app.go` succeeds.
    - `grep -Eq 'func \(a \*App\) DeleteCatalog\(jsonPath string, catalogDir string, deleteHtml bool\) error' app.go` succeeds.
    - Both new bindings gate on containment: `awk '/func \(a \*App\) DuplicateCatalog/,/^}/' app.go | grep -c 'osutil.ContainsPath'` returns `1`, and the same command for `DeleteCatalog` returns `1`.
    - `DeleteCatalog` contains no local removal call: `awk '/func \(a \*App\) DeleteCatalog/,/^}/' app.go | grep -Ec 'os\.Remove|os\.RemoveAll'` returns `0`. <!-- planner-discipline-allow: os\.Remove|os\.RemoveAll -->
    - `grep -q 'DuplicateCatalog' frontend/wailsjs/go/main/App.d.ts && grep -q 'DeleteCatalog' frontend/wailsjs/go/main/App.d.ts` succeeds — the bridge was regenerated, not hand-edited.
    - `grep -q 'duplicateCatalog' frontend/src/services/wailsAPI.ts && grep -q 'deleteCatalog' frontend/src/services/wailsAPI.ts` succeeds.
    - `awk '/duplicateCatalog: async/,/^  },/' frontend/src/services/wailsAPI.ts | grep -c 'wailsError(error)'` returns `1`, and the same for `deleteCatalog` returns `1`.
    - `cd frontend && npx tsc --noEmit && npm run build` exit 0.
    - `go test ./... -race -count=1` green.
  </acceptance_criteria>

  <done>`wailsAPI.duplicateCatalog` and `wailsAPI.deleteCatalog` exist, each reaching a containment-gated binding, with the delete path deriving its `.html` companion in Go rather than trusting the renderer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer JS → Wails binding | `jsonPath`, `catalogDir` and `deleteHtml` all cross here; the renderer never supplies the `.html` path. |
| Go → OS Trash (third party) | `wastebasket.Trash` reaches `osascript` on macOS, `SHFileOperationW` on Windows, and the FreeDesktop trash spec on Linux. |
| Go → filesystem | Duplicate creates new files; delete removes user data (recoverably). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | `internal/osutil/trash.go` → `wastebasket`'s macOS backend, which interpolates the path into an AppleScript string escaping only literal double quotes before `osascript -e` | high | mitigate | Every path passes `os.Lstat`, regular-file check, `.json`/`.html` extension allowlist, `filepath.Abs` + `EvalSymlinks`, and `osutil.ContainsPath(catalogDir, resolved)` before reaching the library. Paths originate from `BrowseCatalogs`'s `os.ReadDir` listing, never renderer free-text. The upstream interpolation itself cannot be fixed from here; recorded in the file's doc comment as an accepted, scope-bounded residual risk (`27-RESEARCH.md` Pitfall 1, Assumption A4). |
| T-27-02 | Tampering, Elevation of Privilege | `app.go` `DuplicateCatalog` / `DeleteCatalog` — renderer-supplied path reaching a filesystem write or a trash call | high | mitigate | The same `EvalSymlinks` + `ContainsPath` + extension gate `GetCatalogHtmlPath`/`OpenExternal`/`RevealInFileManager`/`RenameCatalog` carry; empty `catalogDir` fails closed; the `.html` companion is derived in Go, never accepted from the renderer, so a second arbitrary file cannot be named for deletion. |
| T-27-09 | Repudiation | Delete reporting success while a file remains, or masking the real OS failure | medium | mitigate | `TrashPaths` wraps the seam's error with `%w` so `errors.Is` still reaches it, and the binding wraps again with the path; the frontend renders the OS error verbatim. `TestTrashPaths_PropagatesSeamErrorVerbatim` pins it. |
| T-27-14 | Tampering | Duplicate clobbering an existing `.json` or an orphaned `.html` | medium | mitigate | A candidate root is free only when both `<root>.json` and `<root>.html` are absent; a stat error that is not "not exists" aborts rather than being read as free; every write goes through `WriteFileAtomic`. Pinned by `TestDuplicateCatalog_SkipsRootWithOrphanHTML`. |
| T-27-06 | Tampering | Crash mid-write during a duplicate corrupting an existing file | high | mitigate | Both copies route through the `27-02`-hardened `WriteFileAtomic`; an acceptance grep forbids any direct file-creation call in `duplicate.go`. |
| T-27-SC | Tampering | `go get github.com/Bios-Marcel/wastebasket/v2@v2.0.3` | high | mitigate | `27-RESEARCH.md`'s Package Legitimacy Audit: version set confirmed against the Go module proxy via `go list -m -versions`; repo age (created 2018-08-29), stars and importer counts fetched from `api.github.com` and `pkg.go.dev`; all four platform backend source files read in full, not merely metadata. Verdict **OK / Approved**, neither `[ASSUMED]` nor `[SUS]`, and the package is named by the ROADMAP as this milestone's one pre-approved new Go dependency — so no blocking human checkpoint is inserted. The automated `package-legitimacy` seam supports npm/pypi/crates only; Go verification was manual and is recorded as such. Version pinned exactly in `go.mod`; checksums recorded in `go.sum`. Maintenance-pace observation (last upstream push ~16 months before research) is noted, not treated as a legitimacy flag. |
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test ./... -race -count=1` green.
- `cd frontend && npx tsc --noEmit && npm run build` green.
- `go.mod` shows `github.com/Bios-Marcel/wastebasket/v2 v2.0.3` in the direct `require` block and `go.sum` carries its checksums; `git diff go.mod` shows no other module added or bumped except `golang.org/x/sys` if `go mod tidy` promotes it.
- No test in this plan touched a real OS Trash — `grep -rn 'wastebasket' internal/` shows the import only in `internal/osutil/trash.go`.
</verification>

<success_criteria>
- A catalog duplicates to the next free `-copy`/`-copy-N` root, copying its `.html` when present, byte-identical, clobbering nothing.
- Delete moves the `.json` — and the `.html` when the caller asked for it — to the OS Trash, with every path containment-gated and no permanent-deletion path anywhere.
- A failed trash returns the real OS error through to the service layer.
- A repeat delete of an already-trashed catalog succeeds without error.
</success_criteria>

<artifacts_this_phase_produces>
Created or changed by **this plan**:

**Go — `internal/catalog` (new file `duplicate.go`)**
- `catalog.DuplicateCatalog(jsonPath string) (string, error)` — exported.
- `nextCopyRoot(dir, root string) (string, error)` — unexported collision-suffix search.

**Go — `internal/osutil` (new file `trash.go`)**
- `osutil.TrashPaths(catalogDir string, paths ...string) error` — exported.
- an unexported package-level `func(paths ...string) error` seam initialized to `wastebasket.Trash`, swapped by `trash_test.go`.

**Go — `app.go`**
- `func (a *App) DuplicateCatalog(jsonPath string, catalogDir string) (string, error)`.
- `func (a *App) DeleteCatalog(jsonPath string, catalogDir string, deleteHtml bool) error`.

**Modules**
- `github.com/Bios-Marcel/wastebasket/v2 v2.0.3` — first of this phase's two new Go dependencies, in `go.mod` + `go.sum`.

**Generated bridge**
- `frontend/wailsjs/go/main/App.d.ts`, `App.js` — `DuplicateCatalog`, `DeleteCatalog`.

**Frontend service layer**
- `wailsAPI.duplicateCatalog(jsonPath, catalogDir)` → `{success:true, jsonPath}` | `{success:false, error}`.
- `wailsAPI.deleteCatalog(jsonPath, catalogDir, deleteHtml)` → `{success:true}` | `{success:false, error}`.

**Tests**
- `internal/catalog/duplicate_test.go` — `TestDuplicateCatalog_*` (10 cases).
- `internal/osutil/trash_test.go` — `TestTrashPaths_*` (11 cases).
</artifacts_this_phase_produces>

<planner_assumptions>
Edge probes the deterministic scanner returned as `unclassified` for this plan's requirements. Carried as explicit, flagged assumptions — not dismissed, not auto-resolved with a backstop:

- **ACT-03 (`unclassified`)** — planner's reading is that the load-bearing edges are the collision sequence, the orphaned-`.html` case, and byte-identity, all covered above by explicit `must_haves.truths`.
- **ACT-05 (`unclassified`)** — planner's reading is that the load-bearing edge is the *absence* of a fallback, which is a prohibition rather than a data-shape edge; it is authored into `must_haves.prohibitions` and enforced by the removal-call grep gate rather than by a positive test.

Additionally: `TestTrashPaths_*` exercise the seam, never the real OS Trash, so the **runtime** behavior of `wastebasket`'s Windows (`SHFileOperationW`) and Linux (FreeDesktop spec) backends is unverified on this machine. That is a platform-runtime gap, not a missing dependency — plan `27-07` records it in `.planning/WINDOWS.md`, matching this project's convention of tracking unverified platform behavior rather than claiming parity.
</planner_assumptions>

<output>
Create `.planning/phases/27-catalog-actions-watch/27-03-SUMMARY.md` when done.
</output>
