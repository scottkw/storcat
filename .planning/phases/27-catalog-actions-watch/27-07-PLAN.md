---
phase: 27-catalog-actions-watch
plan: 07
type: execute
wave: 4
depends_on: ["27-04", "27-06"]
files_modified:
  - frontend/src/components/workspace/StatusBar.tsx
  - frontend/src/components/workspace/CatalogRail.tsx
  - frontend/src/components/workspace/settings/CatalogSettingsSection.tsx
  - .planning/WINDOWS.md
autonomous: false
requirements: [WATCH-01, WATCH-02, WATCH-03]

estimate:
  tokens: 47000
  raw_tokens: 47000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "User sees ● watching <catalog directory> in the status bar when watching is enabled and a catalog directory is set."
    - "The watching segment is omitted entirely — not a placeholder, not a dash, not a reserved empty slot — when either the setting is off or no catalog directory is set."
    - "The watching segment and the scan segment can be visible simultaneously, grouped in a .ws-status-right wrapper with watching first and scan last at a 16px gap."
    - "The watching segment renders in var(--fn), not var(--ac) — passive background chrome, deliberately quieter than the active, clickable scan segment."
    - "The watching segment is a span, not a button — it has no click destination."
    - "The watched directory path truncates at max-width 160px with an ellipsis, the same value and behavior the scan segment's name already uses; it is the segment's only variable-length content."
    - "The watching segment carries no aria-live region and no title attribute — matching the scan segment's own precedent rather than introducing a one-off."
    - "The watching segment's inputs are already resolved synchronously in AppContext by the time the status bar renders, so it has no loading state."
    - "User sees the rail update when catalogs are added, removed, or modified outside the app, through the existing catalogs:changed subscription re-triggering CatalogRail's own directory listing."
    - "The rail refresh reuses loadCatalogsForDirectory — the same function CatalogRail's directory-change effect already calls — with no second read path."
    - "The catalogs:changed subscription returns its unsubscribe function from the effect, so a React 18 StrictMode double-invoke cannot leak a second listener."
    - "The Settings watch-directory toggle's note describes what watching does, statically, independent of on/off state — the live on indicator is the status-bar segment itself."
    - statement: "A catalogs:changed emission that lands while a browseCatalogs call is already in flight cannot produce a torn rail listing — each SET_CATALOGS dispatch replaces the array wholesale, so the last completed listing wins. Reasoned from the reducer's replace-wholesale semantics; this project has no frontend test framework (TEST-01 deferred), so the race is not proven mechanically."
      verification: backstop
  artifacts:
    - path: "frontend/src/components/workspace/StatusBar.tsx"
      provides: "WATCH-01's watching segment inside a new right-hand group"
      contains: "ws-status-watching"
    - path: "frontend/src/components/workspace/CatalogRail.tsx"
      provides: "WATCH-02's catalogs:changed subscription re-triggering the existing listing"
      contains: "catalogs:changed"
    - path: ".planning/WINDOWS.md"
      provides: "this phase's platform-gated ledger entries and the disposition of entry #6"
      contains: "27"
  key_links:
    - from: "frontend/src/components/workspace/CatalogRail.tsx"
      to: "app.go"
      via: "EventsOn('catalogs:changed') subscribes to the payload-free event app.go emits"
      pattern: "EventsOn\\('catalogs:changed'"
    - from: "frontend/src/components/workspace/StatusBar.tsx"
      to: "frontend/src/contexts/AppContext.tsx"
      via: "the segment's visibility derives from state.settings.watchDirectory and state.catalogDir"
      pattern: "settings\\.watchDirectory"
  prohibitions:
    - statement: "MUST NOT retain an OS filesystem watch handle after watching is turned off or the app quits; 'stop delivering events' is not the same as releasing the handle."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT claim platform coverage that was not exercised; behavior that could not be run on this machine is recorded as an open ledger entry rather than asserted as verified."
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Finish the phase: show the user that watching is on, make the rail actually update when catalogs change outside the app, correct the Settings note Phase 26 left as a promise, record every platform-gated gap honestly, and verify the whole phase live.

Purpose: WATCH-01 and the frontend half of WATCH-02/WATCH-03. It is also the phase's ledger and verification pass — this plan is the sole owner of `.planning/WINDOWS.md` for Phase 27, so every platform gap the other six plans surfaced lands in one place with one set of evidence.

Output: the status-bar watching segment, the `catalogs:changed` subscription, the corrected Settings note, `.planning/WINDOWS.md` entries, and a completed phase verification matrix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-catalog-actions-watch/27-CONTEXT.md
@.planning/phases/27-catalog-actions-watch/27-UI-SPEC.md
@.planning/phases/27-catalog-actions-watch/27-VALIDATION.md
@.planning/phases/27-catalog-actions-watch/27-02-SUMMARY.md
@.planning/phases/27-catalog-actions-watch/27-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The watching status-bar segment, the rail's catalogs:changed subscription, and the Settings note correction</name>

  <precondition>`app.go` emits `catalogs:changed` and `frontend/src/workspace.css` declares `.ws-status-right`, `.ws-status-watching` and `.ws-status-watching-dir` — both landed by plans `27-06` and `27-04` respectively. Confirm with a read-only `grep`, not by re-implementing either.</precondition>

  <read_first>
    - `frontend/src/components/workspace/StatusBar.tsx` — the whole file. `.ws-status` is `justify-content: space-between` with exactly two top-level children today (`.ws-status-left` and the conditional `.ws-status-scan` button); `showScanSegment` is the conditional-render-vs-omit precedent; the `●` span carries `aria-hidden="true"`; the file has no `aria-live` anywhere.
    - `frontend/src/components/workspace/CatalogRail.tsx` lines 1-60 — `loadCatalogsForDirectory` (the `useCallback` that calls `wailsAPI.browseCatalogs` and dispatches `SET_CATALOGS`) and the `state.catalogDir` effect that already calls it. These are the existing refresh path, not something to duplicate.
    - `frontend/src/components/workspace/CreateSlideOver.tsx` lines 7 and 186-198 — the `EventsOn` import path (`'../../../wailsjs/runtime/runtime'`) and the subscribe-and-return-unsubscribe effect shape, including its comment on why returning the unsubscribe is what stops a StrictMode double-invoke leaking a listener.
    - `frontend/src/components/workspace/settings/CatalogSettingsSection.tsx` lines 128-146 — the watch-directory `ToggleRow`, whose `note` prop still carries Phase 26's placeholder text, and the neighbouring "Write HTML"/"Remember window" toggles whose notes are always-static descriptions.
    - `frontend/src/settingsStore.ts` → `AppSettings` and `frontend/src/contexts/AppContext.tsx` → `AppState` — confirming `state.settings.watchDirectory` and `state.catalogDir` are both already resolved synchronously by the time the status bar renders.
    - `27-UI-SPEC.md` "Watching Status-Bar Indicator (WATCH-01)" and the Copywriting Contract's last three rows.
  </read_first>

  <files>frontend/src/components/workspace/StatusBar.tsx, frontend/src/components/workspace/CatalogRail.tsx, frontend/src/components/workspace/settings/CatalogSettingsSection.tsx</files>

  <action>
Three small, independent frontend changes. All CSS already exists (plan `27-04`); this task adds no styles.

**1. `StatusBar.tsx` — the watching segment.**

Compute `showWatchSegment = state.settings.watchDirectory && !!state.catalogDir`. Both inputs resolve synchronously in `AppContext`, so there is no loading branch.

Restructure the return so `.ws-status` still has exactly two top-level children under its `space-between` distribution: the existing `.ws-status-left` group, and a new `<div className="ws-status-right">` wrapping the conditional segments. DOM order inside the right group is **watching first, scan last** — watching is the ambient, persistent state and sits closer to centre; scan is the transient, urgent one and keeps the outermost, most-attention-grabbing position it already occupies.

The watching segment is a `<span className="ws-status-watching">`, **not a `<button>`** — unlike the scan segment there is no click destination, because watching has no panel to reopen. It contains three children in order: `<span aria-hidden="true">●</span>`, `<span>watching</span>`, and `<span className="ws-status-watching-dir">{state.catalogDir}</span>`. That produces the literal `● watching <catalog directory path>` WATCH-01's requirement wording names.

Omit the segment entirely when `showWatchSegment` is false — no placeholder, no dash, no reserved empty slot — the same rule the scan segment already follows.

Add **no** `aria-live` region: a screen reader re-announcing this every time an external change debounces in would be noisy, and the scan segment has none either. Add **no** `title` attribute for the untruncated path on hover: this app has never used that affordance anywhere, not even on the scan segment's own name which truncates the same way, so adding it here alone would be a one-off inconsistency rather than closing a genuine gap.

Do not change `.ws-status-left`, the three existing segments, the `useMemo` totals, or the `partial`/`qualifier` logic.

**2. `CatalogRail.tsx` — the `catalogs:changed` subscription.**

Import `EventsOn` from `'../../../wailsjs/runtime/runtime'`, matching `CreateSlideOver.tsx`'s import path exactly.

Add one effect that subscribes to `'catalogs:changed'` and, in the handler, calls the existing `loadCatalogsForDirectory(state.catalogDir)` when `state.catalogDir` is set. Depend on `[state.catalogDir, loadCatalogsForDirectory]` and **return the unsubscribe function from the effect** — that is what keeps a React 18 StrictMode double-invoke from leaving a second listener behind.

This deliberately reuses the one existing refresh path rather than adding a second. `27-CONTEXT.md` locks the event as a bare, payload-free signal precisely so the rail re-lists through `browseCatalogs`, which is idempotent and the single source of truth; per-file delta patching was considered and rejected because a second code path can disagree with the first. Do not add a bespoke read, do not diff, do not patch individual rows.

Leave the existing `state.catalogDir` effect exactly as it is — the two effects are independent and both call the same function.

**3. `CatalogSettingsSection.tsx` — the note correction.**

Replace the watch-directory `ToggleRow`'s `note` prop value with the exact string `Detects catalogs added, removed, or edited outside the app`. Phase 26's placeholder wording described a future state and is now false; the replacement is a static description of what the toggle does, independent of on/off state, matching the always-static note pattern the neighbouring "Write HTML alongside JSON" and "Remember window size & position" rows already use. The live "on" indicator is the status-bar segment itself, not this note.

Keep `noteMono={false}` and every other prop on that row unchanged. The Phase 26 placeholder text must not remain anywhere in the file.
  </action>

  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit && npm run build` exit 0.
    - `grep -c 'ws-status-right' frontend/src/components/workspace/StatusBar.tsx` returns `1` and `grep -c 'ws-status-watching' frontend/src/components/workspace/StatusBar.tsx` returns `2` (the segment class and the directory class).
    - The segment is a span, not a button: `awk '/ws-status-watching"/,/\/span>/' frontend/src/components/workspace/StatusBar.tsx | grep -c '<button'` returns `0`.
    - Watching precedes scan in DOM order: `awk '/ws-status-watching"/{a=NR} /ws-status-scan"/{b=NR} END{exit !(a<b)}' frontend/src/components/workspace/StatusBar.tsx` succeeds.
    - Visibility derives from both inputs: `grep -Eq 'settings\.watchDirectory && ' frontend/src/components/workspace/StatusBar.tsx` succeeds and the same expression references `catalogDir`.
    - No one-off affordances were added: `! grep -Eq 'aria-live|title=' frontend/src/components/workspace/StatusBar.tsx` succeeds. <!-- planner-discipline-allow: aria-live|title= -->
    - `grep -c "EventsOn('catalogs:changed'" frontend/src/components/workspace/CatalogRail.tsx` returns `1`, and that effect returns an unsubscribe: `awk "/EventsOn\('catalogs:changed'/,/\}, \[/" frontend/src/components/workspace/CatalogRail.tsx | grep -c 'unsubscribe'` is at least `2`.
    - No second read path was added to the rail: `grep -c 'wailsAPI.browseCatalogs' frontend/src/components/workspace/CatalogRail.tsx` returns `1` — still only the one call inside `loadCatalogsForDirectory`.
    - `grep -c 'Detects catalogs added, removed, or edited outside the app' frontend/src/components/workspace/settings/CatalogSettingsSection.tsx` returns `1`, and `grep -c 'applies once file watching ships' frontend/src/components/workspace/settings/CatalogSettingsSection.tsx` returns `0`.
  </acceptance_criteria>

  <done>The status bar announces watching quietly and only when it is genuinely on, the rail re-lists on external change through its existing path, and the Settings note describes reality.</done>
</task>

<task type="auto">
  <name>Task 2: Record every platform-gated gap in the broken-windows ledger</name>

  <read_first>
    - `.planning/WINDOWS.md` — the whole file, including the frontmatter counters (`open_count`, `waived_count`, `fixed_count`, `total_count`, `last_updated`), the markdown table, and the trailing JSON array that must stay in sync with it. Entry #6 is Phase 25's atomic-write crash-safety item, explicitly tied to "CRT-11/Phase 27 ACT-09".
    - `.planning/phases/27-catalog-actions-watch/27-02-SUMMARY.md` — the SIGKILL harness verdict. This is the sole evidence for whether entry #6 can be marked fixed.
    - `.planning/phases/27-catalog-actions-watch/27-03-SUMMARY.md` and `27-06-SUMMARY.md` — what was and was not exercised for the two new dependencies' platform backends.
    - `27-RESEARCH.md` "Environment Availability" — the per-backend availability table (no Windows machine, no Linux machine this session) and its statement that every gap is a platform-runtime-verification gap rather than a missing dependency.
  </read_first>

  <files>.planning/WINDOWS.md</files>

  <action>
Use the ledger tooling rather than hand-editing both representations. Record entries with `gsd-tools windows add` (or the equivalent `node "$GSD_TOOLS" query …` invocation this repo's other phases used) so the markdown table, the JSON array and the frontmatter counters stay consistent; hand-editing one of the three is how they drift.

**A. Dispose of entry #6 on evidence, not on hope.**
Read `27-02-SUMMARY.md`'s verdict. If the SIGKILL test genuinely landed and passes — a real external process killed inside the write window across at least 20 iterations, with the pre-existing catalog byte-identical every time — mark entry #6 fixed. If `27-02` committed the harness with an honest `t.Skip`, **leave #6 open** and add a one-line note recording what was attempted and why it did not conclude. A false "fixed" is worse than an honest open item; this exact entry exists because a prior phase declined to fake it.

**B. Add this phase's new entries.** One per genuinely distinct platform gap:

1. **fsnotify Windows rename-release divergence** — `kind: deviation`, `file: internal/watch/watcher.go`. fsnotify documents that a watch is automatically removed when the watched path is deleted or renamed, **except on the Windows backend, which does not remove the watcher on renames**. So if the user renames the catalog directory itself while StorCat is watching it, macOS and Linux drop the watch cleanly (surfacing as silence) while Windows keeps a now-dangling watch attached to the old handle. This is a documented upstream platform difference, not something local code closes; the next explicit catalog-directory change through Settings tears down and rebuilds the watcher on every platform, which bounds the impact. Sweep before v3.0.0 ships. Record this as a divergence rather than claiming coverage.

2. **fsnotify Windows and Linux backends runtime-unverified** — `kind: deviation`, `file: internal/watch/watcher.go`. The `ReadDirectoryChangesW` and inotify backends compile for their targets but no Windows or Linux machine was available to run them; only the macOS kqueue backend was exercised. Same class as existing entries #4 and #5.

3. **wastebasket Windows and Linux backends runtime-unverified** — `kind: deviation`, `file: internal/osutil/trash.go`. The `SHFileOperationW` (Windows) and FreeDesktop-trash-spec (Linux) backends were read in full at research time and cross-build cleanly, but only the macOS `osascript`/Finder backend was actually exercised. ACT-04/ACT-05's runtime behavior on the other two platforms is unverified.

4. **`WriteFileAtomic` parent-directory fsync unsupported on Windows** — `kind: deviation`, `file: internal/catalog/atomicwrite.go`. The parent-directory `Sync()` added in `27-02` works on Linux and macOS; Windows does not expose a directory handle that can be synced this way, so the call is expected to fail there and its error is deliberately discarded. The file's own `Sync()` still provides the primary guarantee on Windows. The discard path itself is unverified on Windows.

5. **`wastebasket`'s macOS AppleScript interpolation** — `kind: deviation`, `file: internal/osutil/trash.go`. Record this as an accepted, upstream-owned residual risk rather than a defect to fix: the library's macOS backend builds an AppleScript string escaping only literal double-quote characters before `osascript -e`. This phase's mitigation is the `osutil.ContainsPath` gate on every path before the library is reached (threat T-27-01), which bounds the worst case but does not close the interpolation itself, and cannot — it is third-party code. Note that a future feature letting a user type an arbitrary filename that reaches the trash binding would need this revisited.

**C. Do not invent entries.** Only record gaps that genuinely exist and are supported by one of the prior SUMMARYs or by `27-RESEARCH.md`'s Environment Availability table. Do not add an entry for something that was actually verified this phase.

**D. Verify the three representations agree** after every change: the markdown table row count, the JSON array length, and `total_count` must match, and `open_count + waived_count + fixed_count` must equal `total_count`.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/WINDOWS.md','utf8');const fm=t.match(/^---\n([\s\S]*?)\n---/)[1];const g=k=>parseInt(fm.match(new RegExp(k+': *(\\\\d+)'))[1],10);const open=g('open_count'),waived=g('waived_count'),fixed=g('fixed_count'),total=g('total_count');const json=JSON.parse(t.slice(t.indexOf('\`\`\`\`json')+8,t.lastIndexOf('\`\`\`\`')));const rows=t.split('\n').filter(l=>/^\| *\d+ *\|/.test(l)).length;if(open+waived+fixed!==total)throw new Error('counters do not sum: '+[open,waived,fixed,total]);if(json.length!==total)throw new Error('json length '+json.length+' != total '+total);if(rows!==total)throw new Error('table rows '+rows+' != total '+total);const p27=json.filter(e=>String(e.phase)==='27');if(p27.length<5)throw new Error('expected >=5 phase-27 entries, found '+p27.length);console.log('ledger consistent: '+total+' entries, '+p27.length+' from phase 27, open='+open)"</automated>
  </verify>

  <acceptance_criteria>
    - The `<verify>` script prints a `ledger consistent:` line and exits 0 — table rows, JSON array length and `total_count` all agree, the three status counters sum to `total_count`, and at least five Phase 27 entries exist.
    - Entry #6's `status` matches `27-02-SUMMARY.md`'s actual verdict. If it is `fixed`, `resolved_at` is a non-null timestamp and the SUMMARY quotes the evidence (iteration count, hash comparison). If it is still `open`, the SUMMARY states plainly why.
    - Every new entry names a real file that exists in the repo: for each Phase 27 entry, `test -f "$(echo "$file")"` succeeds.
    - No entry claims verification that did not happen: every Phase 27 entry's description contains either "unverified", "not removed", "not supported", or "upstream" — `node -e` over the JSON asserting each phase-27 description matches `/unverified|not removed|not supported|upstream|accepted/i`.
    - `git diff .planning/WINDOWS.md` shows the frontmatter `last_updated` advanced.
  </acceptance_criteria>

  <done>Every platform gap this phase surfaced is a tracked ledger entry with an honest description, entry #6 is disposed of on real evidence, and the file's three representations agree.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 27 verification matrix — 28 rows, live at :34115</name>
  <what-built>
The complete Phase 27 feature set: a catalog actions menu on the `⋯` button; rename (JSON title authoritative, both HTML title sites patched, escaping fixed on the read side); duplicate with `-copy`/`-copy-N`; delete to the OS Trash behind a confirmation that names both paths and never offers permanent deletion; crash-safe writes with a real SIGKILL proof; and live directory watching with a status-bar indicator, a debounced rail refresh, and genuine watcher release.
  </what-built>
  <how-to-verify>
Run `wails dev` and verify against **`:34115` only** — `:5173` exposes no `window.go`, so binding assertions pass vacuously there. Confirm the binary is fresh first: `Object.keys(window.go.main.App)` must include `RenameCatalog`, `DuplicateCatalog` and `DeleteCatalog`.

**No host-OS GUI automation** — no `osascript`, System Events, `cliclick`, or synthetic keystrokes (`STATE.md` records a prior-phase incident where a stray keystroke landed in an unrelated app). Drive the webview; where a native dialog is unavoidable, call the binding directly, which is the same code path a real click takes.

Work through the matrix and record an actual result for every row, not a reasoned expectation:

| # | Req | Check | Expected |
|---|-----|-------|----------|
| 1 | ACT-01 | Click `⋯` in the details panel | Menu opens with Rename catalog… / Duplicate catalog / divider / Delete catalog… |
| 2 | ACT-01 | Click `⋯` again while open | Menu closes |
| 3 | ACT-01 | ArrowDown past the last item, ArrowUp past the first | Focus wraps at both ends |
| 4 | ACT-01 | Press Escape | Menu closes and focus returns to the `⋯` button |
| 5 | ACT-01 | Reopen, click elsewhere in the app | Menu closes, focus returns to the trigger |
| 6 | ACT-01 | Open the menu while a scan is running | All three items remain enabled |
| 7 | ACT-02 | Rename a catalog to `Tom & Jerry <2024>` | Rail row and details panel show it immediately; neither shows `&amp;` |
| 8 | ACT-02 | `grep -c 'Tom &amp; Jerry &lt;2024&gt;' <the .html>` | Returns `2` — `<title>` and `<h1>` |
| 9 | ACT-02 | Compare both filenames before and after | Unchanged |
| 10 | ACT-02 | Rename a catalog that has no `.html` | Succeeds, no `.html` is created |
| 11 | ACT-03 | Duplicate a catalog with an `.html`, twice | `<root>-copy.{json,html}` then `<root>-copy-2.{json,html}` |
| 12 | ACT-03 | Compare the duplicate's `.json` bytes to its source | Byte-identical |
| 13 | ACT-04 | Delete on a catalog with an `.html` | Two full path boxes, checkbox checked, button reads `Move both to Trash` |
| 14 | ACT-04 | Uncheck the checkbox | Button reads `Move to Trash` |
| 15 | ACT-04 | Delete on a catalog without an `.html` | One path box, no checkbox, `Move to Trash` |
| 16 | ACT-04 | Confirm the delete on a disposable catalog | Both files appear in the OS Trash |
| 17 | ACT-05 | Induce a real trash failure (e.g. make the target undeletable) | Error sub-state shows the real system message |
| 18 | ACT-05 | Inspect every control and string in that error state | Only `Keep catalog` and `Try moving to Trash again`; no permanent-deletion affordance anywhere |
| 19 | ACT-09 | `go test ./internal/catalog/... -run TestWriteFileAtomic -count=1 -v` | Green, including the kill cases (or an honest documented skip) |
| 20 | WATCH-01 | Enable watching in Settings with a catalog directory set | `● watching <dir>` appears in the status bar, in the quiet `--fn` color |
| 21 | WATCH-01 | Disable watching | The segment disappears entirely — no placeholder, no dash |
| 22 | WATCH-01 | Start a scan while watching is on | Both segments render together, watching left of scan |
| 23 | WATCH-02 | With watching on, `cp` a `.json` into the catalog directory from a terminal | Rail shows it within ~1s |
| 24 | WATCH-02 | `rm` a `.json` from a terminal | Rail drops it within ~1s |
| 25 | WATCH-02 | `cp` 10 files in at once | Rail refreshes roughly once, not ten times |
| 26 | WATCH-03 | Toggle watching off, then `touch` a `.json` | Rail does not refresh |
| 27 | WATCH-03 | Quit the app entirely while watching was on | No error on quit; relaunch works normally |
| 28 | SET-04 | Open Settings and read the watch-directory note | Describes what watching detects; no leftover placeholder wording |

For row 27, if a handle-level check is available on this machine (`lsof -p <pid>` before quitting versus after), record it; otherwise record that release rests on `TestWatcher_Close` plus the `OnShutdown` wiring and say so plainly rather than implying a stronger proof.

Write the completed matrix, with real observed results, into this plan's SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which rows did not match.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wails event → React state | `catalogs:changed` arrives unsolicited from Go and triggers a filesystem listing. |
| config value → status-bar display | `state.catalogDir` is rendered as text in the status bar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-23 | Spoofing | The status bar claiming an active watch when the watcher failed to start (e.g. permission denied on the directory) | low | accept | `27-UI-SPEC.md`'s E4 resolution makes this segment deliberately optimistic — tied to the setting rather than a confirmed running watcher — because `27-CONTEXT.md`'s integration points define only a bare `catalogs:changed` signal with no watcher-health channel, and this app has no toast system to surface a background failure through. Adding a health event was explicitly identified as scope creep beyond WATCH-01 and beyond the approved UI contract. Accepted, recorded, not silently glossed. |
| T-27-24 | Denial of Service | A leaked `catalogs:changed` listener under React 18 StrictMode's development double-invoke, causing duplicated listings | low | mitigate | The effect returns `EventsOn`'s unsubscribe function, the same shape `CreateSlideOver.tsx`'s `scan:progress` subscription uses and documents; an acceptance grep asserts the unsubscribe is present. |
| T-27-25 | Information Disclosure | The catalog directory path rendered in the status bar | low | accept | The path is the user's own configured directory, already displayed in the rail's directory chip and in Settings; the status bar adds no new disclosure. It is rendered as a JSX text child, never as an HTML string. |
| T-27-05 | Denial of Service | Watcher handle not released on quit | medium | mitigate | Verified behaviorally by matrix row 27 on top of `27-06`'s `OnShutdown` wiring and `TestWatcher_Close`; where a handle-level check is unavailable on this machine, the verification records that limitation rather than overclaiming. |
| T-27-26 | Repudiation | The broken-windows ledger claiming platform coverage that was never exercised | medium | mitigate | Task 2 records five explicit deviation entries for the unexercised Windows and Linux backends and the upstream AppleScript risk; an acceptance check asserts every Phase 27 entry's description carries unverified/not-supported/upstream/accepted language, and entry #6's disposition is driven by `27-02-SUMMARY.md`'s actual verdict rather than by convenience. |
| T-27-SC | Tampering | Package installs | high | mitigate | No module or npm package is added in this plan; `go.mod`, `go.sum` and `frontend/package.json` are untouched. The phase's two Go dependencies were audited and pinned in `27-03` and `27-06`. |
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test ./... -race -count=1` green.
- `cd frontend && npx tsc --noEmit && npm run build` green.
- The ledger consistency script in Task 2 exits 0.
- The 28-row matrix in Task 3 is completed with real observed results and recorded in the SUMMARY.

## UI Considerations lifted from `27-UI-SPEC.md`

E4 (watching status-bar segment) is this plan's surface: 6 applicable considerations, all 6 resolved explicit (empty, error, populated, overflow, long-text, partial), authored above as plain `must_haves.truths`. E1/E2 belong to plan `27-04` and E3 to `27-05`. Across the phase: 20 applicable, 18 explicit truths and 2 `verification: backstop` markers, matching the UI checker's own arithmetic.
</verification>

<success_criteria>
- `● watching <dir>` appears exactly when watching is on and a directory is set, in the quiet tertiary color, alongside the scan segment when both apply.
- The rail updates within about a second when catalogs change outside the app, once per burst rather than once per file.
- Turning watching off stops the refreshes and releases the handle; quitting releases it too.
- The Settings note describes what watching does rather than promising it.
- Every platform gap is a tracked ledger entry; entry #6 is disposed of on real evidence.
- All 28 verification rows are recorded with observed results.
</success_criteria>

<artifacts_this_phase_produces>
## This plan

**React components (changed)**
- `frontend/src/components/workspace/StatusBar.tsx` — new `.ws-status-right` wrapper and the `.ws-status-watching` segment (`●` + `watching` + `.ws-status-watching-dir`).
- `frontend/src/components/workspace/CatalogRail.tsx` — `EventsOn('catalogs:changed')` subscription re-triggering `loadCatalogsForDirectory`, with unsubscribe on cleanup.
- `frontend/src/components/workspace/settings/CatalogSettingsSection.tsx` — the watch-directory toggle's `note` corrected to a static description.

**Planning artifacts**
- `.planning/WINDOWS.md` — five new Phase 27 deviation entries plus the disposition of entry #6.

---

## Phase 27 roll-up — every symbol this phase creates

**Go — `pkg/models`**
- `models.CatalogItem.Title string` (`json:"title,omitempty"`) — `27-01`.

**Go — `internal/catalog`**
- `catalog.RenameCatalog(jsonPath, newTitle string) error`; unexported `setRootStringField`, `rewriteHTMLTitle` — `27-01`.
- `catalog.DuplicateCatalog(jsonPath string) (string, error)`; unexported `nextCopyRoot` — `27-03`.
- unexported `syncDir(dir string) error`; `WriteFileAtomic` behavior change (temp `Sync()` + best-effort parent-directory `Sync()`) — `27-02`.

**Go — `internal/search`**
- unexported `extractJSONTitle(data []byte) string`; `BrowseCatalogs` three-tier title resolution with `html.UnescapeString` — `27-01`.

**Go — `internal/osutil` (new file `trash.go`)**
- `osutil.TrashPaths(catalogDir string, paths ...string) error`; unexported package-level trash seam — `27-03`.

**Go — `internal/watch` (new package)**
- `watch.DefaultDebounce`, `watch.Watcher`, `watch.New(dir, debounce, onChange)`, `(*Watcher).Close()`; unexported `coalescer` with `newCoalescer`/`trigger`/`fireNow`/`stop` — `27-06`.

**Go — `app.go`**
- `App.RenameCatalog(jsonPath, catalogDir, newTitle string) error` — `27-01`.
- `App.DuplicateCatalog(jsonPath, catalogDir string) (string, error)` — `27-03`.
- `App.DeleteCatalog(jsonPath, catalogDir string, deleteHtml bool) error` — `27-03`.
- `App.watchMu`, `App.watcher`, `App.emitCatalogsChanged()`, `App.applyWatchState()`, `App.shutdown(ctx)` — `27-06`.

**Go — `main.go`**
- `OnShutdown: app.shutdown` in the `wails.Run` options — `27-06`.

**Wails events**
- `catalogs:changed` — payload-free; emitted only from `app.go`, subscribed in `CatalogRail.tsx`.

**Go modules**
- `github.com/Bios-Marcel/wastebasket/v2 v2.0.3` — `27-03`.
- `github.com/fsnotify/fsnotify v1.10.1` — `27-06` (a **second** new dependency beyond the one the ROADMAP anticipated, accepted as an explicit user decision).
- `golang.org/x/sys v0.30.0` promoted from indirect to direct, version unchanged.

**Frontend service layer** — `wailsAPI.renameCatalog`, `wailsAPI.duplicateCatalog`, `wailsAPI.deleteCatalog`.

**React components (new)** — `Menu.tsx` (`Menu`, `MenuItemSpec`, `MenuProps`), `DialogShell.tsx` (`DialogShell`, `DialogShellProps`), `RenameDialog.tsx` (`RenameDialog`, `RenameDialogProps`), `DeleteConfirmDialog.tsx` (`DeleteConfirmDialog`, `DeleteConfirmDialogProps`) — all under `frontend/src/components/workspace/`.

**React components (changed)** — `DetailsPanel.tsx` (new in-file `CatalogActions`; `Footer` takes `error`/`onError`; hoisted `actionError`), `StatusBar.tsx`, `CatalogRail.tsx`, `settings/CatalogSettingsSection.tsx`.

**CSS custom properties** — `--danger` `#e5534b`, `--ondanger` `#ffffff`, `--z-menu` `150`.

**CSS keyframe** — `ws-dialog-scrim-in`.

**CSS classes** — `.ws-menu`, `.ws-menu-item`, `.ws-menu-item-danger`, `.ws-menu-divider`, `.ws-dialog-scrim`, `.ws-dialog-panel`, `.ws-dialog-header`, `.ws-dialog-title`, `.ws-dialog-close-x`, `.ws-dialog-body`, `.ws-dialog-footer`, `.ws-dialog-btn`, `.ws-dialog-btn-primary`, `.ws-dialog-btn-danger`, `.ws-dialog-error`, `.ws-dialog-label`, `.ws-rename-input`, `.ws-delete-lead`, `.ws-delete-path-label`, `.ws-delete-path-box`, `.ws-delete-row`, `.ws-delete-check`, `.ws-status-right`, `.ws-status-watching`, `.ws-status-watching-dir`.

**DOM ids** — `ws-catalog-actions-menu`.

**New file paths** — `internal/catalog/rename.go`, `internal/catalog/rename_test.go`, `internal/catalog/duplicate.go`, `internal/catalog/duplicate_test.go`, `internal/catalog/atomicwrite_sigkill_test.go`, `internal/catalog/testdata/killtarget/main.go`, `internal/osutil/trash.go`, `internal/osutil/trash_test.go`, `internal/watch/watcher.go`, `internal/watch/watcher_test.go`, `frontend/src/components/workspace/Menu.tsx`, `frontend/src/components/workspace/DialogShell.tsx`, `frontend/src/components/workspace/RenameDialog.tsx`, `frontend/src/components/workspace/DeleteConfirmDialog.tsx`, `.planning/phases/27-catalog-actions-watch/COVERAGE.md`.
</artifacts_this_phase_produces>

<planner_assumptions>
- **WATCH-01 and WATCH-03 (`unclassified` edge probes)** — carried as explicit flagged assumptions, not dismissed and not auto-resolved with backstops. Planner's reading: WATCH-01's load-bearing edges are the two-input visibility rule, the omit-entirely behavior, the colour divergence from the scan segment, and the 160px truncation — all covered above by explicit `must_haves.truths`, and independently marked `not applicable` for `loading` and `zero-one-many` by `27-UI-SPEC.md`'s E4 table. WATCH-03's is release-versus-ignore, covered by `27-06`'s truths and tests plus matrix rows 26-27.
- **WATCH-02's `concurrency` edge is a `backstop`, not an explicit truth.** The reasoning (each `SET_CATALOGS` replaces the array wholesale, so the last completed listing wins) is sound but unprovable here: this project has no frontend test framework by design (TEST-01 is an explicitly deferred milestone item). It is authored as a structured `verification: backstop` marker so the verifier abstains to `human_needed` rather than passing it silently.
- **The watching indicator is optimistic by design.** It reflects the setting, not a confirmed-alive watcher. This is `27-UI-SPEC.md`'s E4 resolution, recorded as threat T-27-23 with disposition `accept`, and it is a deliberate contract rather than an unnoticed gap.
- **Windows and Linux runtime behavior is unverified for both new dependencies.** Recorded as ledger entries in Task 2 rather than claimed. This matches the project's standing convention (`.planning/WINDOWS.md` entries #1, #2, #4, #5, #7).
</planner_assumptions>

<output>
Create `.planning/phases/27-catalog-actions-watch/27-07-SUMMARY.md` when done.
</output>
