---
phase: 08-cli-foundation-and-platform-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/cli.go
  - cli/cli_test.go
  - cli/version.go
  - cli/version_test.go
  - cli/stubs.go
autonomous: true
requirements: [CLIP-02, CLIP-03, CLIP-04, CLIP-05, CLIP-06, CLCM-06]

must_haves:
  truths:
    - "cli.Run(['version'], '1.0.0') prints 'storcat 1.0.0' to stdout and returns 0"
    - "cli.Run(['version', '--help'], '1.0.0') prints usage to stderr and returns 0"
    - "cli.Run(['--help'], '1.0.0') prints global usage to stdout and returns 0"
    - "cli.Run(['create', '--help'], '1.0.0') prints create usage and returns 0"
    - "cli.Run(['badcmd'], '1.0.0') prints error to stderr and returns 2"
    - "cli.Run(['version'], '1.0.0') writes nothing to stderr; cli.Run(['badcmd'], '1.0.0') writes nothing to stdout"
    - "All stub commands (create, search, list, show, open) return 1 with 'not yet implemented' on stderr"
  artifacts:
    - path: "cli/cli.go"
      provides: "Run() entry point, printUsage(), subcommand dispatch switch"
      exports: ["Run"]
    - path: "cli/version.go"
      provides: "runVersion() implementation"
      contains: "func runVersion"
    - path: "cli/stubs.go"
      provides: "Stub handlers for create, search, list, show, open"
      contains: "func runCreate"
    - path: "cli/cli_test.go"
      provides: "Dispatch, help, exit code, stdout/stderr routing tests"
      contains: "TestRun"
    - path: "cli/version_test.go"
      provides: "Version command tests"
      contains: "TestRunVersion"
  key_links:
    - from: "cli/cli.go"
      to: "cli/version.go"
      via: "switch dispatch calls runVersion()"
      pattern: "runVersion\\(args"
    - from: "cli/cli.go"
      to: "cli/stubs.go"
      via: "switch dispatch calls runCreate/runSearch/etc"
      pattern: "runCreate\\(args"
---

<objective>
Create the `cli/` Go package with the Run() entry point, subcommand dispatch, version command, and stub handlers for all Phase 9-10 commands.

Purpose: This is the core CLI package that all future commands build on. It establishes the dispatch pattern, exit code contract, stdout/stderr routing, and --help behavior.
Output: `cli/` package with cli.go, version.go, stubs.go, and comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cli-foundation-and-platform-compatibility/08-RESEARCH.md

@version.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cli/ package -- dispatch, version command, and stubs</name>
  <files>cli/cli.go, cli/version.go, cli/stubs.go, cli/cli_test.go, cli/version_test.go</files>
  <read_first>
    - version.go (understand Version var pattern -- it's in package main, passed as parameter)
    - main.go (understand current entry point -- cli/ must NOT import main or wails)
    - .planning/phases/08-cli-foundation-and-platform-compatibility/08-RESEARCH.md (Pattern 2 and Pattern 3 sections for exact signatures)
  </read_first>
  <behavior>
    - TestRunVersion_PrintsVersion: cli.Run(["version"], "2.0.0") writes "storcat 2.0.0\n" to stdout, returns 0
    - TestRunVersion_Help: cli.Run(["version", "--help"], "2.0.0") writes usage to stderr, returns 0
    - TestRunVersion_Help_H: cli.Run(["version", "-h"], "2.0.0") returns 0
    - TestRunHelp_Global: cli.Run(["--help"], "2.0.0") writes usage containing "Commands:" to stdout, returns 0
    - TestRunHelp_HFlag: cli.Run(["-h"], "2.0.0") returns 0
    - TestRunHelp_HelpCmd: cli.Run(["help"], "2.0.0") returns 0
    - TestRun_UnknownCommand: cli.Run(["badcmd"], "2.0.0") writes error to stderr containing "unknown command", returns 2
    - TestRun_StdoutStderrSeparation: version writes to stdout only (stderr empty); unknown cmd writes to stderr only (stdout empty)
    - TestRun_StubCreate: cli.Run(["create"], "2.0.0") writes "not yet implemented" to stderr, returns 1
    - TestRun_StubSearch: cli.Run(["search"], "2.0.0") returns 1 with "not yet implemented"
    - TestRun_StubList: cli.Run(["list"], "2.0.0") returns 1 with "not yet implemented"
    - TestRun_StubShow: cli.Run(["show"], "2.0.0") returns 1 with "not yet implemented"
    - TestRun_StubOpen: cli.Run(["open"], "2.0.0") returns 1 with "not yet implemented"
    - TestRun_StubHelp: cli.Run(["create", "--help"], "2.0.0") returns 0 (help works on stubs)
    - TestRun_NoCobra: import path "github.com/spf13/cobra" does NOT appear in any cli/*.go file
  </behavior>
  <action>
    Create the cli/ directory with three source files and two test files.

    **cli/cli.go** (package cli):
    - `func Run(args []string, version string) int` -- the single public entry point
    - Exit code contract: 0 = success, 1 = runtime error, 2 = usage error
    - Switch dispatch on args[0]: "version" -> runVersion(args[1:], version), "create" -> runCreate(args[1:]), "search" -> runSearch(args[1:]), "list" -> runList(args[1:]), "show" -> runShow(args[1:]), "open" -> runOpen(args[1:]), "help"/"--help"/"-h" -> printUsage(version) + return 0, default -> fmt.Fprintf(os.Stderr, "storcat: unknown command %q\nRun 'storcat --help' for usage.\n", args[0]) + return 2
    - If len(args) == 0: printUsage(version) and return 0
    - `func printUsage(version string)` -- prints to os.Stdout: "storcat {version}\n\nUsage:\n  storcat [command]\n\nCommands:\n  create    Create a catalog from a directory\n  search    Search catalogs for a term\n  list      List catalogs in a directory\n  show      Display a catalog's tree structure\n  open      Open a catalog's HTML in the default browser\n  version   Print the version\n\nFlags:\n  -h, --help   Show help for a command\n\nRun 'storcat <command> --help' for command-specific help.\n"
    - Imports: ONLY "flag", "fmt", "os" -- NO cobra, NO wails

    **cli/version.go** (package cli):
    - `func runVersion(args []string, version string) int`
    - Creates `flag.NewFlagSet("version", flag.ContinueOnError)`
    - Sets fs.Usage to print "Usage: storcat version\n\nPrint the StorCat version.\n" to os.Stderr
    - Calls fs.Parse(args) -- if err == flag.ErrHelp return 0, if other err return 2
    - Prints `fmt.Fprintf(os.Stdout, "storcat %s\n", version)` and returns 0

    **cli/stubs.go** (package cli):
    - Five functions: runCreate(args []string) int, runSearch(args []string) int, runList(args []string) int, runShow(args []string) int, runOpen(args []string) int
    - Each function: creates flag.NewFlagSet with ContinueOnError, sets Usage with command-specific help text, calls fs.Parse(args), handles flag.ErrHelp -> return 0, then prints fmt.Fprintf(os.Stderr, "storcat %s: not yet implemented\n", cmdName) and returns 1
    - Help text per stub:
      - create: "Usage: storcat create <directory> [flags]\n\nCreate a catalog from a directory.\n\nFlags:\n  --title    Catalog title\n  --name     Catalog filename\n  --output   Output directory\n  --json     Output result as JSON\n"
      - search: "Usage: storcat search <term> <directory> [flags]\n\nSearch catalogs for a term.\n\nFlags:\n  --json   Output results as JSON\n"
      - list: "Usage: storcat list <directory> [flags]\n\nList catalogs in a directory.\n\nFlags:\n  --json   Output results as JSON\n"
      - show: "Usage: storcat show <catalog.json> [flags]\n\nDisplay a catalog's tree structure.\n\nFlags:\n  --depth     Limit tree depth\n  --json      Output raw catalog JSON\n  --no-color  Disable color output\n"
      - open: "Usage: storcat open <catalog.json>\n\nOpen the catalog's HTML report in the default browser.\n"

    **cli/cli_test.go** and **cli/version_test.go**:
    - Use a test helper that captures stdout and stderr by temporarily replacing os.Stdout and os.Stderr with os.Pipe() -- pattern: create pipe, set os.Stdout = writer, call function, close writer, read from reader
    - Implement all behaviors listed above as table-driven tests where practical
    - Test that no cli/*.go file contains "github.com/spf13/cobra" by reading file contents with os.ReadDir + os.ReadFile in a test

    Per CLIP-03: use ONLY stdlib flag.FlagSet -- this is a locked project decision, not Cobra.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat && go test ./cli/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - cli/cli.go exists and contains `func Run(args []string, version string) int`
    - cli/cli.go contains `func printUsage(version string)`
    - cli/cli.go switch statement contains cases: "version", "create", "search", "list", "show", "open", "help", "--help", "-h"
    - cli/version.go contains `func runVersion(args []string, version string) int`
    - cli/version.go contains `flag.NewFlagSet("version", flag.ContinueOnError)`
    - cli/stubs.go contains `func runCreate(args []string) int`
    - cli/stubs.go contains `func runSearch(args []string) int`
    - cli/stubs.go contains `func runList(args []string) int`
    - cli/stubs.go contains `func runShow(args []string) int`
    - cli/stubs.go contains `func runOpen(args []string) int`
    - cli/stubs.go contains "not yet implemented"
    - cli/cli_test.go exists and contains `func Test`
    - cli/version_test.go exists and contains `func Test`
    - `go test ./cli/... -v -count=1` exits 0 with all tests passing
    - No file in cli/ contains "github.com/spf13/cobra"
    - No file in cli/ contains "wailsapp"
  </acceptance_criteria>
  <done>
    cli/ package compiles, all tests pass, Run() dispatches to version (working) and 5 stubs (return 1 with "not yet implemented"), --help works on all commands (returns 0), unknown commands return 2 with error to stderr, stdout/stderr separation is correct.
  </done>
</task>

</tasks>

<verification>
- `go test ./cli/... -v -count=1` -- all tests pass
- `go vet ./cli/...` -- no issues
- `go build ./cli/...` -- compiles cleanly
- `golangci-lint run ./cli/...` -- no lint issues
- No imports of cobra or wails in cli/ package
</verification>

<success_criteria>
- cli/ package exists with Run() entry point
- `storcat version` logic prints version to stdout, returns 0
- `storcat --help` prints usage listing all 6 commands, returns 0
- `storcat <stub>` returns 1 with "not yet implemented" on stderr
- `storcat <stub> --help` returns 0 with usage text
- `storcat badcmd` returns 2 with error on stderr
- All behaviors verified by automated tests
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-foundation-and-platform-compatibility/08-01-SUMMARY.md`
</output>
