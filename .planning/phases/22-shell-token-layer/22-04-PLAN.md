---
phase: 22-shell-token-layer
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - frontend/src/components/workspace/CatalogRail.tsx
  - frontend/src/components/workspace/StatusBar.tsx
autonomous: true
requirements: [SHELL-01, THEME-02, THEME-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The catalog rail shows a header block with an uppercase CATALOGS label and a literal count of 0, a New pill, a directory chip, and a filter input, above a scrollable region holding the empty state (SHELL-01)."
    - "The New pill renders accent text on an --acs tint and inverts on hover to a full --ac fill with --onac text, staying legible on light accents such as Gruvbox orange and Monokai green and on dark accents such as GitHub blue (THEME-02)."
    - "The rail row padding reads --rp, so flipping density between Compact and Comfortable changes it without any component-level change (THEME-04)."
    - "The status bar is a fixed 26px band showing exactly three mono segments: 0 catalogs, 0 files indexed, 0.0 GB (SHELL-01)."
    - "The rail's empty state is unconditionally on: the heading No catalogs here yet, the two-sentence body, and the inert Catalog a volume link, all verbatim from the Copywriting Contract (UI-SPEC E2 empty)."
    - "The rail has no loading state — Phase 22 makes no BrowseCatalogs call, so it is unconditionally empty; rail loading is Phase 23's contract (UI-SPEC E2 loading)."
    - "The rail has no error surface, because nothing can fail to load; the unreadable-catalog surface is Phase 23 (UI-SPEC E2 error)."
    - statement: "Populated rail rows are out of scope by design; verified by confirming pane width, tokens, borders and scroll behaviour match spec at all three tiers rather than by any populated-row check."
      verification: backstop
    - statement: "The filter input and the directory chip render but are inert; verified by confirming they render per spec and that interacting with them neither throws nor produces a console error, with no stub filtering behind them."
      verification: backstop
    - "The rail scroll region is flex 1, overflow-y auto, min-height 0, scrollbar-gutter stable; row titles set white-space nowrap, overflow hidden and text-overflow ellipsis (UI-SPEC E2 overflow)."
    - "Only the zero case applies: the header count renders literally as CATALOGS 0, with singular and plural handling deferred to Phase 23 (UI-SPEC E2 zero-one-many)."
    - "The directory chip ellipsizes with overflow hidden, text-overflow ellipsis and white-space nowrap; the empty-state body wraps at rail width with line-height 1.5 and is authored copy, never user-generated (UI-SPEC E2 long-text)."
    - "The status bar is a fixed 26px height that never wraps and never scrolls; its segments are flex-shrink 0 and the right-aligned watch segment is omitted entirely rather than reserved as empty space (UI-SPEC E5 overflow)."
    - "All three status segments are formatted numbers using the handoff's own formatting, bounded in width by construction, with no user text (UI-SPEC E5 long-text)."
  artifacts:
    - "frontend/src/components/workspace/CatalogRail.tsx — CatalogRail with header, New pill, directory chip, filter input, empty state"
    - "frontend/src/components/workspace/StatusBar.tsx — StatusBar with three mono zero-state segments"
  key_links:
    - "New pill --acs background and --onac hover text -> themeTokens applyTokens -> THEME-02's legibility guarantee; this pill is one of only two accent-filled surfaces in Phase 22 and therefore one of only two places the --onac bug can be seen"
    - "rail row padding var(--rp) -> density branch in computeTokens -> THEME-04; hardcoding a pixel padding here silently breaks the density toggle"
    - ".pane-scroll on the rail's list region -> scrollbar-gutter stable -> the fixed 268/236/200px pane widths measuring the same on Windows and macOS"
  prohibitions:
    - "Do not fabricate a working filter, a stub catalog list, or a fake catalog directory path to make the rail look alive. The rail is honestly empty in this phase; inventing data would misrepresent what the app can do."
    - "Do not give the inert Catalog a volume link a pointer cursor or a click handler. An affordance that does nothing when clicked is a broken promise; the affordance appears when Phase 25 wires it."
---

<objective>
Fill the catalog rail and status bar seams the tracer created: the rail's header, accent New pill, directory chip, filter input and unconditional empty state, and the status bar's three literal zero-state segments — all copy transcribed verbatim from the Copywriting Contract.

Purpose: the New pill is one of only two accent-filled surfaces in Phase 22, which makes this plan half of THEME-02's visible evidence.
Output: two complete, always-empty skeleton regions at their specified dimensions and tokens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-shell-token-layer/22-UI-SPEC.md
@.planning/phases/22-shell-token-layer/22-PATTERNS.md
@.planning/phases/22-shell-token-layer/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the catalog rail header, chips, filter input and empty state</name>
  <files>frontend/src/components/workspace/CatalogRail.tsx</files>
  <read_first>
    - `.planning/phases/22-shell-token-layer/22-UI-SPEC.md` — "Copywriting Contract" (the rail header count, directory chip, filter placeholder, New pill, and all three rail empty-state rows — every string is transcribed, none invented), "Typography" (the 10.5/11/11.5/12/12.5px roles), "Spacing Scale", "Color / Accent reserved for", "Icons" (folder and plus glyphs and the accessible-name table), "UI Considerations / E2"
    - `.planning/phases/22-shell-token-layer/22-PATTERNS.md` — the `components/workspace/{CatalogRail,…}` section (single plain function declaration, not the deleted `.Sidebar`/`.Content` object-export pattern) and the `.pane-scroll` convention
    - `frontend/src/components/workspace/CatalogRail.tsx` and `frontend/src/workspace.css` as written by plan 22-01
    - `design_handoff_storcat_ui/designs/StorCat 1a Demo.dc.html` lines 98-121 — the rail markup to transcribe
  </read_first>
  <action>
Rewrite `CatalogRail.tsx` as a plain function declaration with a default export at the bottom and no props. Use inline style objects reading `var(--…)`; `workspace.css` already owns `.ws-rail`, `.pane-scroll` and `.mono`. No Ant Design imports.

Keep the root as the `div.ws-rail` the tracer created. Inside it:

**Header block.** A div with `padding: '12px 12px 10px'`, `display: 'flex'`, `flexDirection: 'column'`, `gap: 10`, `borderBottom: '1px solid var(--l)'`, `flex: 'none'`. It contains three rows.

Row one is a flex row with `alignItems: 'center'`, `justifyContent: 'space-between'`. On the left, a span at `fontSize: 12`, `fontWeight: 600`, `letterSpacing: '0.04em'`, `textTransform: 'uppercase'`, `color: 'var(--dm)'` whose markup text is `Catalogs ` followed by a nested span at `color: 'var(--fn)'` containing the literal `0`. Uppercasing is done by the CSS property, not by typing capitals — the rendered result is `CATALOGS 0`. On the right, the New pill: a `button` with `type="button"`, no `onClick` (the create slide-over is Phase 25), styled `display: 'flex'`, `alignItems: 'center'`, `gap: 5`, `fontSize: 12`, `fontWeight: 600`, `color: 'var(--ac)'`, `background: 'var(--acs)'`, `border: 'none'`, `borderRadius: 6`, `padding: '3px 8px'`, `cursor: 'pointer'`. Its contents are a 10px plus svg (`viewBox="0 0 12 12"`, `stroke="currentColor"`, `strokeWidth={1.8}`, a vertical line 6,1.5 to 6,10.5 and a horizontal line 1.5,6 to 10.5,6) carrying `aria-hidden="true"` and `focusable="false"`, followed by the text `New`. The visible text is its accessible name, so it takes no `aria-label`. Give the button `className="ws-new-pill"`: its hover inversion to a `var(--ac)` fill with `var(--onac)` text — the THEME-02 evidence surface, which cannot be expressed as an inline style — comes from the rule plan 22-01 already declared.

Row two is the directory chip: a div with `display: 'flex'`, `alignItems: 'center'`, `gap: 7`, `className="mono"`, `fontSize: 11`, `color: 'var(--dm)'`, `background: 'var(--ch)'`, `border: '1px solid var(--l)'`, `borderRadius: 6`, `padding: '5px 8px'`. It contains an 11px folder svg (`viewBox="0 0 14 14"`, `fill="none"`, `stroke="var(--fn)"`, `strokeWidth={1.4}`, a rect at 1,3 sized 12 by 9 with rx 1.5 and a horizontal line from 1,5.5 to 13,5.5) carrying `aria-hidden="true"` and `focusable="false"`, followed by a span reading exactly `No catalog directory set` with `overflow: 'hidden'`, `textOverflow: 'ellipsis'`, `whiteSpace: 'nowrap'`. The chip is non-interactive this phase: render it as a div, not a button, and give it no click handler and no pointer cursor — no catalog directory exists to change until Phase 26.

Row three is the filter input: a wrapper div with `height: 26`, `borderRadius: 6`, `background: 'var(--bg)'`, `border: '1px solid var(--l)'`, `display: 'flex'`, `alignItems: 'center'`, `padding: '0 8px'`, containing an `input` with `aria-label="Filter catalogs"`, `placeholder` set to exactly `Filter catalogs…`, `style` of `width: '100%'`, `fontSize: 12`, `color: 'var(--tx)'`, `background: 'transparent'`, `border: 'none'`, `outline: 'none'`. Leave it uncontrolled with no `onChange` — filtering is Phase 23's RAIL-02, and typing into it must neither throw nor produce a console error.

**Scroll region.** A div with `className="pane-scroll"` plus inline `padding: 6`, `display: 'flex'`, `flexDirection: 'column'`, `gap: 1`.

**Empty state**, rendered unconditionally (Phase 22 has no data source, so this is not a conditional branch). A div with `padding: '16px 10px'`, `display: 'flex'`, `flexDirection: 'column'`, `gap: 10`, containing three elements in order: a span at `fontSize: 12.5`, `fontWeight: 500` reading exactly `No catalogs here yet`; a span at `fontSize: 11.5`, `lineHeight: 1.5`, `color: 'var(--dm)'` reading exactly `This folder has no .json catalogs. Point StorCat somewhere else, or catalog a volume to create the first one.`; and a span at `fontSize: 12`, `fontWeight: 600`, `color: 'var(--ac)'` reading exactly `Catalog a volume →`. That third element is a span, not a button, with no click handler and no pointer cursor — the create slide-over it will open is Phase 25, and the affordance is added when the behaviour is.

The rail row padding contract is already declared as `.ws-rail-row` in `workspace.css` by plan 22-01, reading `padding: var(--rp)`, so Phase 23's rows inherit the density contract rather than reinventing it. Do not hardcode `6px 8px` or `10px 10px` anywhere in this file, and do not edit `workspace.css` — plans 22-03 and 22-05 run in the same wave and that stylesheet has a single owner.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && npx tsc --noEmit && npm run build && test $(grep -c "No catalogs here yet" src/components/workspace/CatalogRail.tsx) -eq 1 && test $(grep -c "Filter catalogs…" src/components/workspace/CatalogRail.tsx) -eq 1 && test $(grep -c "antd" src/components/workspace/CatalogRail.tsx) -eq 0 && test $(grep -vc "^ *//" src/components/workspace/CatalogRail.tsx >/dev/null; grep -c "6px 8px\|10px 10px" src/components/workspace/CatalogRail.tsx) -eq 0</automated>
    <human-check>Run `wails dev` at a window width of 1280px or more. The rail measures 268px wide with a 1px `--l` right border. The header shows `CATALOGS 0` in uppercase with the zero in the dimmer `--fn` colour, and the New pill sits flush right. Hover the New pill: it inverts to a solid accent fill with legible text. Cycle all 11 themes with Ctrl+Alt+T and confirm the hovered pill's text stays legible in every one, especially Gruvbox Dark, Monokai and GitHub Light. Type into the filter input: characters appear and the console stays clean. The empty state reads exactly as the Copywriting Contract specifies across three lines.</human-check>
  </verify>
  <acceptance_criteria>
    - Each of these strings appears exactly once and character-identically in `CatalogRail.tsx`: `No catalogs here yet`, `This folder has no .json catalogs. Point StorCat somewhere else, or catalog a volume to create the first one.`, `Catalog a volume →`, `No catalog directory set`, `Filter catalogs…`, `New`.
    - `grep -c "6px 8px\|10px 10px" frontend/src/components/workspace/CatalogRail.tsx` returns 0 — density padding comes from `var(--rp)`, never a literal.
    - `grep -c "onClick\|onChange" frontend/src/components/workspace/CatalogRail.tsx` returns 0 — nothing is wired to a Phase 23/25/26 target.
    - `grep -c "cursor: *'pointer'\|cursor: *\"pointer\"" frontend/src/components/workspace/CatalogRail.tsx` returns exactly 1 — the New pill only; the empty-state link and the directory chip have none.
    - `grep -c 'aria-label' frontend/src/components/workspace/CatalogRail.tsx` returns exactly 1 — the filter input.
    - Every `svg` carries `aria-hidden="true"` and `focusable="false"`: `grep -c 'focusable="false"'` equals `grep -c "<svg"`.
    - `grep -c "antd" frontend/src/components/workspace/CatalogRail.tsx` returns 0 and `grep -c "z-index\|zIndex"` returns 0.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The rail renders its header, New pill, directory chip, filter input and empty state at spec dimensions and tokens; the New pill's hover inversion is legible in all 11 themes.</done>
</task>

<task type="auto">
  <name>Task 2: Build the 26px status bar with literal zero-state segments</name>
  <files>frontend/src/components/workspace/StatusBar.tsx</files>
  <read_first>
    - `.planning/phases/22-shell-token-layer/22-UI-SPEC.md` — "Copywriting Contract" (the status bar row, including the instruction to omit the right-aligned watch segment entirely rather than fabricate an idle string), "UI Considerations / E5", "Typography"
    - `frontend/src/components/workspace/StatusBar.tsx` and `frontend/src/workspace.css` (`.ws-status`, `.mono`) as written by plan 22-01
    - `design_handoff_storcat_ui/designs/StorCat 1a Demo.dc.html` lines 238-240 — the status bar markup to transcribe
  </read_first>
  <action>
Rewrite `StatusBar.tsx` as a plain function declaration with a default export at the bottom and no props. No Ant Design imports.

Keep the root as the `div.ws-status` the tracer created; the tracer already gave it `height: 26px`, `flex: none`, `display: flex`, `alignItems: center`, `gap: 16px`, `padding: 0 14px`, `background: var(--p2)`, `borderTop: 1px solid var(--l)`, `fontSize: 11px` and `color: var(--fn)`. Add `className="mono"` to the root so all three segments render in IBM Plex Mono.

Render exactly three sibling spans, each with inline `flexShrink: 0`, reading exactly `0 catalogs`, `0 files indexed`, and `0.0 GB` in that order. These are literal zero-state values using the handoff's own number formatting, not dash placeholders and not a loading treatment.

Render no fourth segment. The right-aligned watching status the handoff shows belongs to Phase 27's directory watching; omit it entirely rather than reserving empty space or fabricating an idle string. Add no `marginLeft: auto` anywhere, since nothing is right-aligned in this phase.

Live counts are Phase 23's SHELL-06 requirement and are explicitly out of scope here — do not call any Wails binding and do not import from `services/wailsAPI`.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && npx tsc --noEmit && npm run build && test $(grep -c "0 catalogs" src/components/workspace/StatusBar.tsx) -eq 1 && test $(grep -c "0 files indexed" src/components/workspace/StatusBar.tsx) -eq 1 && test $(grep -c "0.0 GB" src/components/workspace/StatusBar.tsx) -eq 1 && test $(grep -c "wailsAPI\|marginLeft\|antd" src/components/workspace/StatusBar.tsx) -eq 0</automated>
    <human-check>Run `wails dev`. The status bar measures exactly 26px tall with a 1px `--l` top border, sits below the middle grid, and shows the three segments left-aligned in monospace at 11px in the `--fn` colour, separated by 16px gaps. Shrink the window to below 1040px: the bar still shows all three segments on one line with no wrap and no scrollbar. Nothing appears on the right-hand side.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "0 catalogs" frontend/src/components/workspace/StatusBar.tsx` returns 1, `grep -c "0 files indexed"` returns 1, `grep -c "0.0 GB"` returns 1.
    - `grep -c "marginLeft" frontend/src/components/workspace/StatusBar.tsx` returns 0 — no right-aligned segment is reserved.
    - `grep -c "wailsAPI\|BrowseCatalogs\|LoadCatalog" frontend/src/components/workspace/StatusBar.tsx` returns 0 — no backend call in this phase.
    - `grep -c "flexShrink" frontend/src/components/workspace/StatusBar.tsx` returns 3.
    - `grep -c "antd" frontend/src/components/workspace/StatusBar.tsx` returns 0.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The status bar renders as a fixed 26px monospace band with exactly three literal zero-state segments and nothing on the right.</done>
</task>

</tasks>

<flagged_planner_assumptions>
None land in this plan. The edge probe classified no rows against the rail or status bar; both surfaces are covered entirely by the UI-SPEC `## UI Considerations` items above (E2 and E5), all of which are resolved rather than unclassified. This is recorded so the phase-wide no-silent-drop count stays auditable.
</flagged_planner_assumptions>

<artifacts_this_phase_produces>
**Created by this plan**

| Artifact | Kind |
|---|---|
| `CatalogRail` component body — header block, `CATALOGS 0` label, New pill button, directory chip, filter input, unconditional empty state | React component (file created by 22-01) |
| `StatusBar` component body — three literal mono zero-state segments | React component (file created by 22-01) |
| Plus svg (10px, stroke 1.8) and folder svg (11px, stroke 1.4) | inline SVG, no icon library |
| `.ws-new-pill` and `.ws-rail-row` applied to the New pill and the row contract | class application only; both rules were declared by 22-01 |

**Consumed from other plans in this phase**

`.ws-rail`, `.ws-status`, `.pane-scroll`, `.mono` and the token set from `workspace.css` (22-01); `--acs`, `--ac`, `--onac`, `--rp` from `applyTokens` in `themeTokens.ts` (22-01); the `IBM Plex Mono` faces (22-02). The rail's `order` and border-side swap under a right-side rail setting is added by 22-06 in CSS, not here.

**Not produced here** — no new file, no new exported symbol, no new CSS custom property, no reducer action, no Go change. The full phase-wide artifact index lives in `22-01-PLAN.md`.
</artifacts_this_phase_produces>

<coverage_counts>
- **UI-SPEC `## UI Considerations`:** 32 applicable phase-wide; this plan carries **10** — E2 (rail): 8 of 8 (empty, loading, error, populated, partial, overflow, zero-one-many, long-text; 6 explicit and 2 backstop, the two backstop items authored as flat `{ statement, verification: backstop }` scalars) plus E5 (status bar): 2 of 2 (overflow, long-text, both explicit). Phase-wide split: 22-01 (E6) 4 + 22-03 (E1) 6 + 22-04 (E2, E5) 10 + 22-05 (E3, E4) 12 = 32. Zero dropped.
- **Spec-less edge probe:** 34 applicable phase-wide; this plan carries **0** — the probe classified no rows against these two surfaces. Phase-wide: 26 authored (22-01 12, 22-02 7, 22-06 2, 22-07 5) + 8 flagged (22-01 3, 22-03 2, 22-06 3) = 34.
- **Prohibitions:** 2 survivors authored descriptor-less. Dropped during precision-drop: "do not hardcode density padding" and "do not import antd" (routine engineering hygiene, both enforced as acceptance criteria); no canon-security survivor applies — the filter input is inert and renders no user string.
</coverage_counts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystrokes -> rail filter input | The only user-supplied text this phase accepts, and it is uncontrolled and unread |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-04 | Tampering (injection) | `CatalogRail.tsx`, `StatusBar.tsx` | medium | mitigate | All rail and status copy is authored string literals rendered as React text children; the filter input is uncontrolled and its value is never read, stored, or rendered anywhere. No `dangerouslySetInnerHTML` and no `innerHTML` assignment in either file. <!-- planner-discipline-allow: dangerouslySetInnerHTML --> |
| T-22-09 | Information disclosure | rail directory chip | low | mitigate | The chip renders the literal placeholder `No catalog directory set` rather than a real filesystem path. No path is read, resolved, or displayed until Phase 26 configures one, so no local directory structure leaks through a screenshot or screen share in this phase. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run build` exits 0.
- Every Copywriting Contract string for the rail and status bar appears exactly once and character-identically.
- No density pixel literal, no `onClick`/`onChange`, no `antd` import, no `zIndex`, and no Wails binding call in either file.
- Manual matrix row THEME-02 from `22-VALIDATION.md` passes across all 11 themes on the New pill's hover state.
</verification>

<success_criteria>
The rail and status bar render at their specified dimensions and tokens with copy transcribed verbatim from the Copywriting Contract, the New pill's accent inversion stays legible across all 11 themes, and nothing in either region is wired to a capability that does not exist yet.
</success_criteria>

<output>
Create `.planning/phases/22-shell-token-layer/22-04-SUMMARY.md` when done.
</output>
