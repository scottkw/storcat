---
phase: 22-shell-token-layer
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - frontend/src/assets/fonts/ibm-plex-sans-latin-400-normal.woff2
  - frontend/src/assets/fonts/ibm-plex-sans-latin-500-normal.woff2
  - frontend/src/assets/fonts/ibm-plex-sans-latin-600-normal.woff2
  - frontend/src/assets/fonts/ibm-plex-mono-latin-400-normal.woff2
  - frontend/src/assets/fonts/ibm-plex-mono-latin-500-normal.woff2
  - frontend/src/assets/fonts/IBM-Plex-OFL.txt
  - frontend/src/style.css
autonomous: false
requirements: [THEME-05]
user_setup: []

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "IBM Plex Sans renders for all UI chrome text and IBM Plex Mono renders for every path, filename, size, count and timestamp, from files embedded in the binary (THEME-05)."
    - "A normal load issues zero external font requests; with the network fully disconnected the app still renders Plex, not a system fallback (THEME-05)."
    - "A weight requested but not vendored falls back to synthetic emphasis of the nearest vendored weight within the same family, never to a different family; only Sans 400/500/600 and Mono 400/500 are declared and no Phase 22 element requests a weight outside that set (THEME-05 boundary)."
    - "Weights 400 and 500 are declared as separate @font-face blocks with distinct src files, so a 500 element and a 400 element render from different files rather than the same one (THEME-05 adjacency)."
    - "No @font-face block carries a unicode-range; a codepoint outside the latin subset falls through to the next family in the stack rather than rendering a missing glyph from Plex (THEME-05 empty)."
    - "The non-ASCII characters in the authored copy — the ellipsis, the right arrow, the middle dot, and the command glyph — are confirmed to render either from Plex or from an explicit fallback family, never as a replacement box (THEME-05 encoding)."
    - "The five @font-face blocks are declared in style.css, which main.tsx imports before workspace.css, so no font-family rule can resolve against an undeclared face (THEME-05 ordering)."
    - "font-display: swap is set on all five faces, so a slow first paint shows fallback metrics rather than invisible text; no numeric font-size is altered to compensate (THEME-05 precision)."
    - "The five faces load in parallel from the embedded asset server, and the shell renders correctly if any subset has not finished loading, because every font-family declaration ends in a generic family (THEME-05 concurrency)."
    - "IBM Plex's own OFL copyright and licence text ships in the repo as a file separate from Nunito's existing OFL.txt."
    - "@fontsource packages appear in neither dependencies nor devDependencies of frontend/package.json after this plan."
  artifacts:
    - "frontend/src/assets/fonts/ibm-plex-sans-latin-400-normal.woff2 (22588 bytes)"
    - "frontend/src/assets/fonts/ibm-plex-sans-latin-500-normal.woff2 (24184 bytes)"
    - "frontend/src/assets/fonts/ibm-plex-sans-latin-600-normal.woff2 (24252 bytes)"
    - "frontend/src/assets/fonts/ibm-plex-mono-latin-400-normal.woff2 (14708 bytes)"
    - "frontend/src/assets/fonts/ibm-plex-mono-latin-500-normal.woff2 (14888 bytes)"
    - "frontend/src/assets/fonts/IBM-Plex-OFL.txt"
    - "frontend/src/style.css — five @font-face blocks"
  key_links:
    - "style.css @font-face url() -> Vite default asset pipeline -> frontend/dist/assets/ -> //go:embed all:frontend/dist -> the shipped binary; this is the chain that makes THEME-05's no-network guarantee true"
    - "workspace.css .ws-root font-family and .mono -> the five declared faces; a family-name typo silently degrades to the system fallback and is invisible without the offline check"
  prohibitions:
    - "Do not ship the vendored IBM Plex files without IBM's own OFL copyright and licence text. The existing OFL.txt covers Nunito's authors, not IBM's, and reusing it would misattribute the licence."
    - "Do not reintroduce a CDN or webfont-service link for typography. The requirement is no network access, and a remote font also discloses app usage to a third party."
---

<objective>
Vendor the five latin-subset IBM Plex woff2 files the shell's type scale needs, declare them as `@font-face`, and ship IBM's OFL licence text alongside them — closing THEME-05's "no network access" requirement with files embedded in the Go binary.

Purpose: the tracer declared the font-family contract; this plan supplies the faces that satisfy it.
Output: five committed woff2 files, an IBM Plex OFL text file, and five `@font-face` declarations in the codebase's existing font stylesheet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-shell-token-layer/22-RESEARCH.md
@.planning/phases/22-shell-token-layer/22-PATTERNS.md
@.planning/phases/22-shell-token-layer/22-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm @fontsource package legitimacy before any registry fetch</name>
  <what-built>Nothing yet. This gate precedes the one-time `npm pack` registry fetch in Task 2. 22-RESEARCH.md's Package Legitimacy Audit rates both source packages `[SUS]` (`too-new` — a routine version publish on 2026-07-19) and tags the package names themselves `[ASSUMED]` (recalled from training knowledge, not read from an official IBM or Fontsource document). Neither package is added to `package.json`; only static `.woff2` files and a `LICENSE` file are extracted. Per GSD's package-legitimacy gate, an `[ASSUMED]`/`[SUS]` package still earns a blocking human check before the fetch, and this checkpoint is never auto-approvable.</what-built>
  <how-to-verify>
    1. Open `https://www.npmjs.com/package/@fontsource/ibm-plex-sans` and confirm: the package exists, the repository field points at `github.com/fontsource/font-files`, weekly downloads are in the hundreds of thousands, and the publish history goes back years rather than weeks.
    2. Open `https://www.npmjs.com/package/@fontsource/ibm-plex-mono` and confirm the same four facts.
    3. Confirm on either page that the declared licence is OFL-1.1.
    4. If either package looks wrong, reply with `rejected` — the fallback path in 22-RESEARCH.md's Environment Availability table (sourcing the same latin-subset woff2 files by inspecting a webfont service's CSS response, or subsetting IBM's own `IBM/plex` release) is used instead, and Task 2's file list and byte sizes are re-derived from that source.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the one-time `npm pack` fetch, or "rejected" plus which alternate source to use.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Vendor five IBM Plex woff2 files, IBM's OFL text, and five @font-face declarations</name>
  <files>frontend/src/assets/fonts/ibm-plex-sans-latin-400-normal.woff2, frontend/src/assets/fonts/ibm-plex-sans-latin-500-normal.woff2, frontend/src/assets/fonts/ibm-plex-sans-latin-600-normal.woff2, frontend/src/assets/fonts/ibm-plex-mono-latin-400-normal.woff2, frontend/src/assets/fonts/ibm-plex-mono-latin-500-normal.woff2, frontend/src/assets/fonts/IBM-Plex-OFL.txt, frontend/src/style.css</files>
  <precondition>Network access to the npm registry is available and `npm` and `tar` are on PATH; the fetch is a one-time developer-machine step, not a build-time dependency. If offline, halt and use the alternate source named in 22-RESEARCH.md's Environment Availability table.</precondition>
  <read_first>
    - `.planning/phases/22-shell-token-layer/22-RESEARCH.md` — "IBM Plex Font Vendoring" (the exact `npm pack` commands, the exact five source paths inside the tarballs, the measured byte sizes, the five `@font-face` blocks already written out verbatim, and the Vite/`go:embed` zero-config finding)
    - `.planning/phases/22-shell-token-layer/22-RESEARCH.md` — "Common Pitfalls / OFL license text must accompany the vendored fonts"
    - `.planning/phases/22-shell-token-layer/22-PATTERNS.md` — the `frontend/src/assets/fonts/*` section and the `style.css` `@font-face` analog
    - `frontend/src/style.css` — the existing Nunito `@font-face` block, which is the shape to copy
    - `.planning/phases/22-shell-token-layer/22-UI-SPEC.md` — "Typography" (the three-weights-not-two exception, and which roles use Sans versus Mono)
  </read_first>
  <action>
Work in a scratch directory outside the repo. Run `npm pack @fontsource/ibm-plex-sans@5.3.0 --silent` and `npm pack @fontsource/ibm-plex-mono@5.3.0 --silent`, extract both tarballs, then copy exactly these five files from `package/files/` into `frontend/src/assets/fonts/`, keeping their filenames unchanged: `ibm-plex-sans-latin-400-normal.woff2`, `ibm-plex-sans-latin-500-normal.woff2`, `ibm-plex-sans-latin-600-normal.woff2`, `ibm-plex-mono-latin-400-normal.woff2`, `ibm-plex-mono-latin-500-normal.woff2`. Copy either tarball's `package/LICENSE` to `frontend/src/assets/fonts/IBM-Plex-OFL.txt`. Do not overwrite, merge into, or delete the existing `OFL.txt` — that file is Nunito's copyright holder text and IBM's is a different copyright block. Delete the scratch tarballs and extracted trees. Do not add either package to `frontend/package.json`; the deliverable is static binary assets, not a dependency.

Before declaring the faces, verify each copied file's byte size against the measured values in 22-RESEARCH.md's vendoring table (22588, 24184, 24252, 14708, 14888). A mismatch means the upstream files changed and the sizes in the artifact list must be re-derived and recorded in the summary rather than silently accepted.

Append five `@font-face` blocks to `frontend/src/style.css`, immediately after the existing Nunito block. Each declares `font-family` of either `IBM Plex Sans` or `IBM Plex Mono`, `font-style: normal`, the matching `font-weight` (400, 500, 600 for Sans; 400, 500 for Mono), `font-display: swap`, and `src: url("assets/fonts/<filename>") format("woff2")` — note the path is relative to `style.css` and therefore has no leading `./` segment beyond what the existing Nunito rule uses; match that rule's exact form. Do not add a `unicode-range` descriptor: fontsource pre-splits by subset at the file level, so there is no sibling subset for the browser to choose between, and adding a range would suppress fallback for out-of-range codepoints.

Do not change `frontend/vite.config.ts`. 22-RESEARCH.md verifies that Vite's default asset pipeline already fingerprints `url()`-referenced files under `src/` into `dist/assets/`, which `//go:embed all:frontend/dist` then bakes into the binary — the exact mechanism the existing Nunito file already uses in this repo.

Finally, confirm the four non-ASCII characters the Copywriting Contract uses — the horizontal ellipsis in "Search every catalog…", the right arrow in "Catalog a volume →", the middle dot in the dev readout, and the command glyph in the "⌘K" badge — render as real glyphs rather than replacement boxes. The command glyph in particular is unlikely to be in a latin subset; if it does not render, keep the badge's `font-family` stack ending in a generic family so the system supplies it, and record which characters resolve from a fallback rather than from Plex in the summary.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && ls -l src/assets/fonts/ibm-plex-sans-latin-400-normal.woff2 src/assets/fonts/ibm-plex-sans-latin-500-normal.woff2 src/assets/fonts/ibm-plex-sans-latin-600-normal.woff2 src/assets/fonts/ibm-plex-mono-latin-400-normal.woff2 src/assets/fonts/ibm-plex-mono-latin-500-normal.woff2 src/assets/fonts/IBM-Plex-OFL.txt src/assets/fonts/OFL.txt && test $(grep -c "IBM Plex" src/style.css) -eq 5 && test $(grep -c "unicode-range" src/style.css) -eq 0 && test $(grep -c "fontsource" package.json) -eq 0 && npx tsc --noEmit && npm run build && ls dist/assets/ | grep -c "ibm-plex" | grep -qx 5</automated>
    <human-check>Run `wails dev`. In DevTools, Network tab, reload and confirm every font request resolves to a same-origin `assets/` path with no third-party host. Then switch the Network tab to Offline and reload: the toolbar "StorCat" wordmark and the status bar's "0 catalogs" must still render in Plex Sans and Plex Mono respectively, not in the system UI font. Inspect a Mono element and confirm `font-family` resolves to IBM Plex Mono in the Computed panel.</human-check>
  </verify>
  <acceptance_criteria>
    - All six new files exist under `frontend/src/assets/fonts/` and the pre-existing `OFL.txt` and `nunito-v16-latin-regular.woff2` are still present and unmodified.
    - Each woff2's byte size matches 22-RESEARCH.md's measured table, or the deviation is recorded in the summary with the observed size.
    - `grep -c "IBM Plex" frontend/src/style.css` returns exactly 5.
    - `grep -c "font-display: *swap" frontend/src/style.css` returns at least 5.
    - `grep -c "unicode-range" frontend/src/style.css` returns 0. <!-- planner-discipline-allow: unicode-range -->
    - `grep -c "fontsource" frontend/package.json` returns 0 and `git status --porcelain frontend/package.json frontend/package-lock.json` shows no modification. <!-- planner-discipline-allow: fontsource -->
    - `grep -ci "http" frontend/src/style.css` returns 0 — no remote font source of any kind.
    - After `npm run build`, exactly five files matching `ibm-plex` exist under `frontend/dist/assets/`.
    - `frontend/vite.config.ts` is unmodified.
  </acceptance_criteria>
  <done>The five latin-subset Plex faces render in the running app from embedded assets, IBM's OFL text ships beside them as a distinct file from Nunito's, `package.json` is untouched, and an offline reload still renders Plex.</done>
  <reversibility rating="reversible">Vendored static assets and five CSS declarations; deleting the files and the blocks restores the prior state with no downstream migration.</reversibility>
</task>

</tasks>

<flagged_planner_assumptions>
None land in this plan. THEME-05's seven probe items are all classified and all authored explicitly into `must_haves.truths`; none is `unclassified`, so none becomes a flagged assumption here.
</flagged_planner_assumptions>

<artifacts_this_phase_produces>
**Created by this plan**

| Artifact | Kind | Note |
|---|---|---|
| `frontend/src/assets/fonts/ibm-plex-sans-latin-400-normal.woff2` | binary asset | 22588 bytes |
| `frontend/src/assets/fonts/ibm-plex-sans-latin-500-normal.woff2` | binary asset | 24184 bytes |
| `frontend/src/assets/fonts/ibm-plex-sans-latin-600-normal.woff2` | binary asset | 24252 bytes |
| `frontend/src/assets/fonts/ibm-plex-mono-latin-400-normal.woff2` | binary asset | 14708 bytes |
| `frontend/src/assets/fonts/ibm-plex-mono-latin-500-normal.woff2` | binary asset | 14888 bytes |
| `frontend/src/assets/fonts/IBM-Plex-OFL.txt` | licence text | separate from Nunito's `OFL.txt` |
| `IBM Plex Sans` @font-face x3, `IBM Plex Mono` @font-face x2 | CSS declarations in `style.css` | no `unicode-range` |

**Consumed from other plans in this phase**

`workspace.css`'s `.ws-root` `font-family` stack and its `.mono` class (created by 22-01) are the only consumers of these family names in Phase 22. The five region components created by 22-01 and filled out by 22-03/04/05 inherit from `.ws-root` or opt into `.mono`.

**Not produced here** — no new exported symbol, no new CSS custom property, no reducer action, no Go change. The full phase-wide artifact index lives in `22-01-PLAN.md`.
</artifacts_this_phase_produces>

<coverage_counts>
- **UI-SPEC `## UI Considerations`:** 32 applicable phase-wide; **0 carried by this plan** — the probe declared six surfaces (toolbar, rail, tree pane, details panel, status bar, token layer) and typography is not one of them. E1 goes to 22-03, E2 and E5 to 22-04, E3 and E4 to 22-05, E6 to 22-01. 6 + 10 + 12 + 4 = 32. Zero dropped.
- **Spec-less edge probe:** 34 applicable phase-wide; this plan carries **7** — all of THEME-05's boundary, adjacency, empty, encoding, ordering, precision and concurrency rows, each authored explicitly into `must_haves.truths`. Zero flagged assumptions here. Phase-wide: 26 authored + 8 flagged = 34.
- **Prohibitions:** 2 survivors authored descriptor-less. Dropped during precision-drop: "verify byte sizes before committing" and "do not add to package.json" (both routine engineering hygiene, and both already enforced as acceptance criteria).
</coverage_counts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> developer machine -> repo | A one-time `npm pack` pulls third-party tarballs whose contents are committed as binary assets |
| embedded asset server -> WebView | Font bytes are served from the Go binary, not the network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-SC | Tampering | `npm pack @fontsource/ibm-plex-sans` / `@fontsource/ibm-plex-mono` | high | mitigate | Blocking human checkpoint (Task 1) before any registry fetch, per the package-legitimacy gate for `[ASSUMED]`/`[SUS]` packages; never auto-approvable. Post-fetch, each extracted file's byte size is verified against the values measured during research, and only five `.woff2` files plus one `LICENSE` are copied — no scripts, no lifecycle hooks, and nothing added to `package.json` or the lockfile. |
| T-22-02 | Information disclosure | typography delivery | low | mitigate | Fonts are self-hosted and embedded; a remote webfont would disclose app launches and usage timing to a third party and would break the no-network requirement. Enforced by an acceptance criterion that `style.css` contains no `http` substring and by an offline reload check. |
| T-22-06 | Repudiation (licence attribution) | vendored font files | low | mitigate | IBM's OFL copyright and licence text ships as `IBM-Plex-OFL.txt`, distinct from Nunito's `OFL.txt`, so each font's obligations travel with its files. |
</threat_model>

<verification>
- All six new font-directory files exist; the two pre-existing Nunito files are untouched.
- `frontend/package.json`, `frontend/package-lock.json` and `frontend/vite.config.ts` are unmodified.
- `cd frontend && npx tsc --noEmit && npm run build` exits 0 and emits exactly five `ibm-plex` assets into `dist/assets/`.
- Manual matrix row THEME-05 from `22-VALIDATION.md` passes, including the offline reload.
</verification>

<success_criteria>
IBM Plex Sans and Mono render throughout the shell from files embedded in the binary, with zero external font requests on a normal load and correct rendering with the network disconnected; IBM's OFL text ships in the repo; and `package.json` gained no dependency.
</success_criteria>

<output>
Create `.planning/phases/22-shell-token-layer/22-02-SUMMARY.md` when done.
</output>
