---
phase: 22-shell-token-layer
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - frontend/src/components/workspace/Toolbar.tsx
autonomous: true
requirements: [SHELL-01, SHELL-07, SHELL-08]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The 46px toolbar shows, left to right: reserved traffic-light inset on macOS, the app mark at 16px, the wordmark StorCat at 13px weight 600, a centred search field, the current theme name as a chip, and a gear (SHELL-01)."
    - "Dragging from empty toolbar background moves the window, while clicks on the search field, the theme chip and the gear all register and are not swallowed (SHELL-07)."
    - "On macOS the real traffic lights sit inside the 46px band with roughly 78px of reserved left inset; on Windows and Linux the native title bar sits above the band and no inset is reserved (SHELL-08)."
    - "Every control whose entire label is an icon carries an explicit aria-label and its svg carries aria-hidden and focusable false, so icon geometry is never announced."
    - "The toolbar has no data-driven content; all controls always render. The search field's empty state is its hint text in --dm, and no separate empty treatment exists (UI-SPEC E1 empty)."
    - "The toolbar has no loading state and renders synchronously on mount; no spinner or skeleton appears anywhere in the shell (UI-SPEC E1 loading)."
    - "The toolbar has no error state and performs no async work beyond the one platform query, whose failure leaves the inset at its zero default; no error boundary beyond React's default is added (UI-SPEC E1 error)."
    - "Partial rendering by tier is intentional and specified: the Details chip renders only below 1280px and every other control renders at all three tiers (UI-SPEC E1 partial)."
    - "The toolbar never wraps and never scrolls; the search field is flex 1 with min-width 0 and the app mark, theme chip and gear are flex-shrink 0, so at the narrowest tier the search field absorbs all shrink (UI-SPEC E1 overflow)."
    - "Theme names are a fixed 11-item enum at most 15 characters long; the chip is sized for the longest name at every tier with no truncation and no ellipsis (UI-SPEC E1 long-text)."
  artifacts:
    - "frontend/src/components/workspace/Toolbar.tsx — Toolbar with app mark, wordmark, search button, theme chip, gear"
    - "--toolbar-inset-left set to 78px on darwin at runtime (declared with a 0px default by 22-01)"
  key_links:
    - "Toolbar root --wails-draggable drag -> Wails dragTest reading getComputedStyle on the mousedown target -> native window drag; a control missing .no-drag inherits drag and silently loses its click"
    - "Environment() from frontend/wailsjs/runtime/runtime -> platform string equal to Go's GOOS -> --toolbar-inset-left; the only runtime platform branch in the frontend"
    - "themes.ts theme.name -> theme chip label; the chip is the only place the theme name is rendered in Phase 22"
  prohibitions:
    - "Do not hand-roll mock traffic-light circles or click handlers for them. On macOS Wails renders the real ones; painting fake buttons that do nothing would be a lie to the user about what is clickable."
    - "Do not fabricate a Settings surface behind the gear or the theme chip to make them feel functional. The real Settings modal is Phase 26; an inert control is honest, a stub panel is not."
---

<objective>
Fill out the toolbar seam the tracer created: the app mark, wordmark, centred search field with its command-key badge, theme chip, and gear — every one of them explicitly opted out of the window drag region, with accessible names, and with the macOS traffic-light inset applied at runtime.

Purpose: SHELL-07's click-swallowing bug and SHELL-08's inset are the two highest-risk details in this phase, and both live entirely in this one file.
Output: a complete, pixel-accurate 46px toolbar.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-shell-token-layer/22-UI-SPEC.md
@.planning/phases/22-shell-token-layer/22-RESEARCH.md
@.planning/phases/22-shell-token-layer/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the toolbar controls with explicit drag opt-out and accessible names</name>
  <files>frontend/src/components/workspace/Toolbar.tsx</files>
  <read_first>
    - `.planning/phases/22-shell-token-layer/22-UI-SPEC.md` — "Window Chrome & Drag Regions", "Icons" (including the accessible-name table), "Copywriting Contract" (the search placeholder, the command badge, and the theme chip rows), "Typography", "Color / Accent reserved for"
    - `.planning/phases/22-shell-token-layer/22-RESEARCH.md` — "Pattern 3: --wails-draggable — exact mechanism, not folklore" (the opt-out is CSS custom-property shadowing, not a Wails keyword) and the "Details chip specifically" pitfall
    - `.planning/phases/22-shell-token-layer/22-PATTERNS.md` — the `Toolbar.tsx` section (the `as React.CSSProperties & { '--wails-draggable'?: string }` cast, the only drag-region precedent in the codebase)
    - `frontend/src/components/workspace/Toolbar.tsx` and `frontend/src/workspace.css` as written by plan 22-01
    - `design_handoff_storcat_ui/designs/StorCat 1a Demo.dc.html` lines 68-93 — the toolbar markup to transcribe
  </read_first>
  <action>
Rewrite `Toolbar.tsx` as a plain function declaration with a default export at the bottom, using inline style objects reading `var(--…)` (the project's established styling convention; `workspace.css` owns only tokens, layout and shared utilities). No Ant Design imports.

Keep the root `div.ws-toolbar` and its inline `--wails-draggable` value of `drag` using the cast the tracer already applied. Inside it, in order:

**Traffic-light inset.** Nothing is rendered. The reserved space comes from `.ws-toolbar`'s `padding-left: calc(14px + var(--toolbar-inset-left))`, which the tracer already declared. Do not render decorative circles — the prototype's three coloured dots exist only because it runs in a browser without a native title bar.

**App mark and wordmark.** A flex row, `gap: 8px`, `flexShrink: 0`. The mark is `frontend/src/storcat-icon.svg` imported as a URL and rendered as an `img` at exactly 16 by 16 pixels with `borderRadius: 4`, `alt=""` and `aria-hidden="true"` (the adjacent wordmark is the accessible text). The wordmark is a span reading `StorCat` at `fontSize: 13`, `fontWeight: 600`, `letterSpacing: '-0.01em'`.

**Search field.** A wrapper div with `flex: 1`, `minWidth: 0`, `display: flex`, `justifyContent: center`. Inside it a `button` element with `type="button"`, `className="no-drag ws-search"`, `aria-label="Search every catalog"`, and no `onClick` — the palette it will open is Phase 24, and an inert button is the honest representation. Style: `width: '100%'`, `maxWidth: 460`, `minWidth: 0`, `height: 30`, `borderRadius: 8`, `background: 'var(--bg)'`, `border: '1px solid var(--l)'`, `display: flex`, `alignItems: center`, `gap: 9`, `padding: '0 10px'`, `cursor: 'pointer'`, `color: 'inherit'`, `font: 'inherit'`. The hover and focus-visible accent border — the only accent use permitted on the search field per the UI-SPEC's accent list — comes from the `.ws-search` rule plan 22-01 already declared in `workspace.css`. Apply the class; do not edit that stylesheet, because plans 22-04 and 22-05 run in the same wave and `workspace.css` has a single owner. Contents, in order: a 13px magnifier svg (`viewBox="0 0 16 16"`, `fill="none"`, `stroke="var(--dm)"`, `strokeWidth={1.6}`, a circle at centre 7,7 radius 4.5 and a line from 10.5,10.5 to 14,14) carrying `aria-hidden="true"` and `focusable="false"`; a span reading exactly `Search every catalog…` at `fontSize: 12.5`, `color: 'var(--dm)'`; and a badge span reading exactly `⌘K` with `marginLeft: 'auto'`, `className="mono"`, `fontSize: 11`, `color: 'var(--fn)'`, `border: '1px solid var(--l)'`, `borderRadius: 4`, `padding: '1px 5px'`, and a font-family stack ending in a generic monospace family so the command glyph resolves from a system font if the latin Plex subset lacks it.

**Right cluster.** A flex row, `gap: 10px`, `flexShrink: 0`, containing the theme chip and the gear. Do not add a Details chip here — plan 22-07 adds it together with the drawer behaviour it toggles.

**Theme chip.** A `button` with `type="button"`, `className="no-drag ws-chip"`, no `onClick`, no `aria-label` (its visible text is its accessible name per the UI-SPEC table). Label is the current theme's `name`, read by calling `readPersistedPrefs().theme.name` — Phase 26 replaces this read with reducer state, and a `themeChange` window listener is not needed here because `App.tsx` already holds one. Style: `fontSize: 11.5`, `color: 'var(--dm)'`, `border: '1px solid var(--l)'`, `borderRadius: 6`, `padding: '3px 8px'`, `background: 'transparent'`, `cursor: 'pointer'`, `whiteSpace: 'nowrap'`. Its accent hover comes from the `.ws-chip` rule plan 22-01 already declared. Do not truncate or ellipsize: the longest of the 11 names is `Solarized Light` at 15 characters and the chip must fit it at every tier.

**Gear.** A `button` with `type="button"`, `className="no-drag"`, `aria-label="Settings"`, no `onClick`, `background: 'transparent'`, `border: 'none'`, `padding: 0`, `display: 'flex'`, `cursor: 'pointer'`. It contains a 15px svg (`viewBox="0 0 16 16"`, `fill="none"`, `stroke="var(--dm)"`, `strokeWidth={1.4}`, two concentric circles at centre 8,8 with radii 2.4 and 6) carrying `aria-hidden="true"` and `focusable="false"`.

Every interactive descendant of the toolbar carries `className="no-drag"`. 22-RESEARCH.md verifies that Wails reads the inherited computed value of the drag custom property on the exact mousedown target, so any descendant without the opt-out inherits `drag` and loses its click — most visibly on Windows.

Do not edit `frontend/src/workspace.css` in this plan. Every rule this toolbar needs — `.ws-toolbar`, `.no-drag`, `.mono`, `.ws-search` and `.ws-chip` — was declared by plan 22-01, precisely so that this plan and plans 22-04 and 22-05 can run in parallel without contending for that file. If a rule is genuinely missing, record it in the summary and add it in plan 22-07's wave rather than editing the stylesheet here.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && npx tsc --noEmit && npm run build && test $(grep -c 'className="no-drag"' src/components/workspace/Toolbar.tsx) -eq 3 && test $(grep -c 'aria-hidden="true"' src/components/workspace/Toolbar.tsx) -eq 3 && test $(grep -c 'aria-label' src/components/workspace/Toolbar.tsx) -eq 2 && test $(grep -c "antd" src/components/workspace/Toolbar.tsx) -eq 0</automated>
    <human-check>Run `wails dev`. Press and drag from empty toolbar background between the wordmark and the search field: the window moves. Click the search field, the theme chip, and the gear in turn: each shows its pressed/hover state and none moves the window. Measure the toolbar band at exactly 46px and confirm the wordmark renders at 13px weight 600. Tab through the toolbar and confirm three focus stops in order: search field, theme chip, gear.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'className="no-drag"' frontend/src/components/workspace/Toolbar.tsx` returns exactly 3 — search field, theme chip, gear.
    - `grep -c "aria-label" frontend/src/components/workspace/Toolbar.tsx` returns exactly 2 — the search field and the gear; the theme chip is named by its visible text.
    - Every `svg` element in the file carries both `aria-hidden="true"` and `focusable="false"`: `grep -c 'focusable="false"' frontend/src/components/workspace/Toolbar.tsx` equals `grep -c "<svg" frontend/src/components/workspace/Toolbar.tsx`.
    - `grep -c "Search every catalog…" frontend/src/components/workspace/Toolbar.tsx` returns 1 and the string is character-identical to the UI-SPEC Copywriting Contract row, ellipsis character included.
    - `grep -c "antd" frontend/src/components/workspace/Toolbar.tsx` returns 0.
    - `grep -c "z-index\|zIndex" frontend/src/components/workspace/Toolbar.tsx` returns 0.
    - `grep -c "onClick" frontend/src/components/workspace/Toolbar.tsx` returns 0 — no control is wired to a target that does not exist yet.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The toolbar renders all five regions at their specified sizes and tokens, the window drags from its background, and every control clicks and is keyboard reachable with an accessible name.</done>
</task>

<task type="auto">
  <name>Task 2: Reserve the macOS traffic-light inset from the runtime platform</name>
  <files>frontend/src/components/workspace/Toolbar.tsx</files>
  <precondition>`frontend/wailsjs/runtime/runtime.ts` exists with a generated `Environment()` export; it is generated by the Wails toolchain and is already committed in this repo.</precondition>
  <read_first>
    - `.planning/phases/22-shell-token-layer/22-RESEARCH.md` — "Pattern 2: Wails macOS title bar" (the `Environment()` binding, the fact that `platform` is populated Go-side as `GOOS` so the values are exactly `darwin`, `windows`, `linux`, and Open Question 2's ruling that this may be async because it moves a few pixels of padding rather than repainting coloured content)
    - `frontend/wailsjs/runtime/runtime.d.ts` — the `EnvironmentInfo` shape
    - `frontend/src/workspace.css` — the `--toolbar-inset-left` declaration and its `0px` default, written by plan 22-01
  </read_first>
  <action>
In `Toolbar.tsx`, add one `useEffect` with an empty dependency array that calls `Environment()` imported from `../../../wailsjs/runtime/runtime` (adjust the relative depth to the actual file location) and, when the resolved `platform` is exactly the string `darwin`, calls `document.documentElement.style.setProperty('--toolbar-inset-left', '78px')`. Do nothing on any other platform value — the tracer already declared the `0px` default, which is what Windows and Linux need since their native title bar sits above the band.

Guard the promise with a `catch` that leaves the inset at its default rather than throwing; a failed platform query must degrade to "no inset reserved", not to an unhandled rejection. Guard against the effect running after unmount by ignoring the resolution if a cleanup flag has been set — React StrictMode double-invokes effects in dev.

Do not sniff `navigator.userAgent` or `navigator.platform`. Do not add a Go-side `runtime.GOOS` branch: Go's `GOOS` is a compile-time constant and the inset is a CSS concern, and 22-RESEARCH.md names UA sniffing explicitly as the thing not to hand-roll here.

Record in the summary that the 78px value is inherited from CONTEXT.md as a locked decision, is not derivable from any Wails API, and is the one number in this phase that needs empirical adjustment against a real macOS build if the traffic lights visibly clip or float.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && npx tsc --noEmit && npm run build && test $(grep -c "navigator.userAgent\|navigator.platform" src/components/workspace/Toolbar.tsx) -eq 0 && test $(grep -c "toolbar-inset-left" src/components/workspace/Toolbar.tsx) -eq 1</automated>
    <human-check>Build and launch on macOS: the three real traffic lights sit inside the 46px band, vertically centred, and the app mark begins clear of the rightmost one with no overlap and no more than a small gap. Build and launch on Windows or Linux: the native title bar sits above the 46px band, the band starts flush at 14px of left padding, and no empty inset is reserved. On macOS, confirm in DevTools that `:root` resolves `--toolbar-inset-left` to `78px`; on Windows or Linux, that it resolves to `0px`.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "Environment" frontend/src/components/workspace/Toolbar.tsx` returns at least 2 — the import and the call.
    - `grep -c "'darwin'" frontend/src/components/workspace/Toolbar.tsx` returns 1 and the comparison is a strict equality against that exact string.
    - `grep -c "navigator.userAgent\|navigator.platform" frontend/src/components/workspace/Toolbar.tsx` returns 0.
    - `grep -c "catch" frontend/src/components/workspace/Toolbar.tsx` returns at least 1 — the platform query has a failure path.
    - `grep -c "78px" frontend/src/components/workspace/Toolbar.tsx` returns 1; the value appears once and nowhere else in the frontend.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>On a real macOS build the traffic lights render inside the toolbar band with the inset reserved; on Windows and Linux the native title bar sits above the band with no reserved inset.</done>
  <reversibility rating="reversible">One effect and one CSS custom-property write; removing it returns the toolbar to a zero inset with no other consequence.</reversibility>
</task>

</tasks>

<flagged_planner_assumptions>
Two of this phase's eight unresolved edge-probe rows land in this plan:

- **SHELL-07 (unclassified).** Assumption: "without drag regions swallowing clicks" is verified by clicking each toolbar control on the developer's own platform. 22-VALIDATION.md and 22-RESEARCH.md both state the failure is most visible on Windows, and no Windows build machine is available in this environment. This plan therefore cannot fully discharge SHELL-07; the phase gate must record whether a Windows build was tested or the requirement was accepted on macOS/Linux evidence alone.
- **SHELL-08 (unclassified).** Assumption: the 78px inset is correct. It is a locked CONTEXT.md value, not derivable from the Wails API, and 22-RESEARCH.md rates its correctness LOW/MEDIUM confidence (Assumptions Log A2). If the real traffic lights clip or float on macOS, the number is adjusted empirically rather than by re-reading a spec — and this plan's structure makes that a one-line change.
</flagged_planner_assumptions>

<artifacts_this_phase_produces>
**Created by this plan**

| Artifact | Kind |
|---|---|
| `Toolbar` component body — app mark img, `StorCat` wordmark, search button, `⌘K` badge, theme chip button, gear button | React component (file created by 22-01) |
| Magnifier svg (13px, stroke 1.6) and gear svg (15px, stroke 1.4) | inline SVG, no icon library |
| Runtime write of `--toolbar-inset-left` to `78px` on darwin | CSS custom property (declared with a `0px` default by 22-01) |
| `.ws-search` and `.ws-chip` applied to the search field and theme chip | class application only; both rules were declared by 22-01 |

**Consumed from other plans in this phase**

`.ws-toolbar`, `.no-drag`, `.mono` and the token set from `workspace.css` (22-01); `readPersistedPrefs` from `themeTokens.ts` (22-01); `IBM Plex Sans`/`IBM Plex Mono` faces (22-02). The `Details` chip that will also live in this toolbar is added by 22-07, together with the drawer state it toggles.

**Not produced here** — no new file, no new exported symbol, no reducer action, no Go change. The full phase-wide artifact index lives in `22-01-PLAN.md`.
</artifacts_this_phase_produces>

<coverage_counts>
- **UI-SPEC `## UI Considerations`:** 32 applicable phase-wide; this plan carries **E1 (toolbar): 6 of 6** — empty, loading, error, partial, overflow, long-text, all resolved explicit, all present in `must_haves.truths`. Phase-wide split: 22-01 (E6) 4 + 22-03 (E1) 6 + 22-04 (E2, E5) 10 + 22-05 (E3, E4) 12 = 32. Zero dropped.
- **Spec-less edge probe:** 34 applicable phase-wide; this plan carries **2**, both surfaced as flagged planner assumptions (SHELL-07, SHELL-08 — both `unclassified`, neither auto-dismissed). Phase-wide: 26 authored + 8 flagged = 34.
- **Prohibitions:** 2 survivors authored descriptor-less. Dropped during precision-drop: "keep every svg aria-hidden" and "do not import antd here" (routine engineering hygiene, both enforced as acceptance criteria); no canon-security survivor applies to a static toolbar with no input handling.
</coverage_counts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wails native host -> WebView | The window-drag decision is made natively from a CSS value the WebView computes; the platform string is supplied by Go |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-07 | Denial of service (input) | toolbar drag region | medium | mitigate | Every interactive descendant carries `.no-drag`; a missing opt-out makes a control permanently unclickable, which is a functional denial of the control to the user. Enforced by an acceptance criterion counting exactly three `.no-drag` occurrences against three interactive controls, plus a manual click check on each. |
| T-22-08 | Spoofing (platform) | `Environment()` platform branch | low | accept | The platform string originates from Go's `GOOS` inside the same binary, not from a network peer or user input; there is no adversary who can supply a false value that the app would not already trust. The only consequence of a wrong value is a wrong left padding. |
| T-22-04 | Tampering (injection) | `Toolbar.tsx` | medium | mitigate | All toolbar copy is authored string literals rendered as React text children; no `dangerouslySetInnerHTML` and no `innerHTML` assignment. <!-- planner-discipline-allow: dangerouslySetInnerHTML --> |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run build` exits 0.
- Exactly three `.no-drag` occurrences and exactly two `aria-label` occurrences in `Toolbar.tsx`; every `svg` carries `aria-hidden` and `focusable="false"`.
- No `onClick`, no `zIndex`, no `antd` import, no UA sniff in `Toolbar.tsx`.
- Manual matrix rows SHELL-07 and SHELL-08 from `22-VALIDATION.md` attempted; the Windows result recorded as tested or as untested-and-accepted.
</verification>

<success_criteria>
The 46px toolbar is complete and pixel-accurate, the window drags from its background while all three controls stay clickable and keyboard reachable with accessible names, and the macOS traffic lights sit inside the band with the native title bar remaining above it on Windows and Linux.
</success_criteria>

<output>
Create `.planning/phases/22-shell-token-layer/22-03-SUMMARY.md` when done.
</output>
