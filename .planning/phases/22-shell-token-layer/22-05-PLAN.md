---
phase: 22-shell-token-layer
plan: 05
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - frontend/src/components/workspace/TreePane.tsx
  - frontend/src/components/workspace/DetailsPanel.tsx
autonomous: true
requirements: [SHELL-01, THEME-02, THEME-04]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The tree pane shows a centred empty state: a dashed 46px placeholder square, the heading Nothing catalogued yet, a body capped at 420px, and two buttons — an accent-filled Catalog a volume and an outlined Choose catalog folder… (SHELL-01)."
    - "The accent-filled Catalog a volume button is the single visual focal point of the workspace and the only large accent fill on screen; its label uses --onac so it stays legible on light accents such as Gruvbox orange and Monokai green and on dark accents such as GitHub blue (THEME-02)."
    - "The details panel shows an uppercase DETAILS label and a centred no-selection placeholder, with no menu button and no footer action buttons (SHELL-01)."
    - "The details key/value row padding contract reads --mp and the tree row height and font size read --rh and --fs, so a density flip changes them without any component-level change (THEME-04)."
    - "The tree pane's empty state is unconditionally on, with both CTAs rendered and inert, verbatim from the Copywriting Contract (UI-SPEC E3 empty)."
    - "The tree pane has no loading state — no LoadCatalog call is made in Phase 22; tree loading is Phase 23 (UI-SPEC E3 loading)."
    - "The tree pane has no error surface and adds no error boundary beyond React's default (UI-SPEC E3 error)."
    - statement: "Tree rows never render this phase because the prototype's populated branch is unreachable without data; verified by confirming the pane's 1fr sizing, its scroll region and its empty-state centring rather than by row rendering."
      verification: backstop
    - statement: "The two tree CTAs render but are inert; verified by confirming they render per spec and that clicking is a no-op which neither throws nor opens a fabricated stub surface."
      verification: backstop
    - "The tree scroll region is flex 1, overflow-y auto, min-height 0, scrollbar-gutter stable; the fixed --rh row height contract is set now with no dynamic measuring (UI-SPEC E3 overflow)."
    - "Only the zero case applies; the tree empty state is unconditional rather than data-driven in Phase 22 (UI-SPEC E3 zero-one-many)."
    - "The tree empty-state body is capped at max-width 420px, centred, line-height 1.6 — it wraps and never truncates, and the copy is final and authored (UI-SPEC E3 long-text)."
    - "The details panel has no loading state; it renders its no-selection placeholder immediately because it has no data source this phase (UI-SPEC E4 loading)."
    - "The details panel has no error state and cannot fail; catalog-load failures are Phase 23 (UI-SPEC E4 error)."
    - "The details scroll region is flex 1, overflow-y auto, min-height 0, scrollbar-gutter stable; as a drawer below 1280px it keeps its fixed 288px width and scrolls internally while the backdrop never scrolls (UI-SPEC E4 overflow)."
    - "Metadata values are IBM Plex Mono, single-line and ellipsized; the no-selection placeholder wraps within the 288px pane. The value-row overflow contract is set now and exercised in Phase 23 (UI-SPEC E4 long-text)."
  artifacts:
    - "frontend/src/components/workspace/TreePane.tsx — TreePane with centred empty state and two inert CTAs"
    - "frontend/src/components/workspace/DetailsPanel.tsx — DetailsPanel with DETAILS label and no-selection placeholder"
  key_links:
    - "Catalog a volume button --ac fill with --onac label -> themeTokens applyTokens -> THEME-02's legibility guarantee; this button and the rail's New pill are the only two accent-filled surfaces in Phase 22"
    - "DetailsPanel's single component identity -> reused as pane at >=1280px and as drawer below it (wired by 22-07); duplicating it into a mobile-only component here would break that plan"
    - ".pane-scroll on the tree and details regions -> scrollbar-gutter stable -> the fixed 288px details width measuring the same on Windows and macOS"
  prohibitions:
    - "Do not fabricate a stub modal, a placeholder settings surface, or a fake file picker behind either tree CTA to make it appear to work. Both targets are later phases; a button that opens a lie is worse than a button that waits."
    - "Do not invent details-panel metadata rows, a catalog name, or example values to make the panel look populated. The panel is honestly empty until Phase 23 supplies a selection."
---

<objective>
Fill the tree pane and details panel seams the tracer created: the tree's centred empty state carrying the workspace's single visual focal point, and the details panel's no-selection placeholder — built as one component that plan 22-07 will render as either an inline pane or a drawer.

Purpose: the accent-filled "Catalog a volume" button is the other half of THEME-02's visible evidence, and the details panel's single-component identity is a hard precondition for the responsive drawer.
Output: two complete, always-empty skeleton regions at their specified dimensions and tokens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-shell-token-layer/22-UI-SPEC.md
@.planning/phases/22-shell-token-layer/22-PATTERNS.md
@.planning/phases/22-shell-token-layer/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the tree pane's centred empty state and two inert CTAs</name>
  <files>frontend/src/components/workspace/TreePane.tsx</files>
  <read_first>
    - `.planning/phases/22-shell-token-layer/22-UI-SPEC.md` — "Copywriting Contract" (the primary CTA, secondary CTA, tree empty-state heading and body rows, each marked rendered-but-inert), "Layout & Responsive Contract" (the primary-visual-focal-point paragraph and the hierarchy ordering), "Typography", "Color / Accent reserved for", "UI Considerations / E3"
    - `.planning/phases/22-shell-token-layer/22-PATTERNS.md` — the `components/workspace/{…,TreePane,…}` section and the `.pane-scroll` convention
    - `frontend/src/components/workspace/TreePane.tsx` and `frontend/src/workspace.css` as written by plan 22-01
    - `design_handoff_storcat_ui/designs/StorCat 1a Demo.dc.html` lines 136-149 — the tree empty-state markup to transcribe
  </read_first>
  <action>
Rewrite `TreePane.tsx` as a plain function declaration with a default export at the bottom and no props. Inline style objects reading `var(--…)`; no Ant Design imports.

Keep the root as the `div.ws-tree` the tracer created (`order: 2`, `display: flex`, `flexDirection: column`, `minHeight: 0`, `background: var(--bg)`).

Inside it render a single scroll region div with `className="pane-scroll"` and inline `display: 'flex'`, `flexDirection: 'column'` — the scroll contract is set now even though nothing overflows, because Phase 23's virtualized rows mount into exactly this element.

Inside the scroll region render the empty state unconditionally (Phase 22 has no data source, so this is not a conditional branch): a div with `flex: 1`, `display: 'flex'`, `flexDirection: 'column'`, `alignItems: 'center'`, `justifyContent: 'center'`, `gap: 16`, `padding: 40`. It contains three children in order.

First, the placeholder mark: a span sized 46 by 46 with `borderRadius: 10`, `border: '1px dashed var(--l)'`, `display: 'block'`, and `aria-hidden="true"`. It is decorative only and carries no text.

Second, a text column: a div with `display: 'flex'`, `flexDirection: 'column'`, `alignItems: 'center'`, `gap: 7`, `maxWidth: 420`. It contains a heading span at `fontSize: 16`, `fontWeight: 600` reading exactly `Nothing catalogued yet`, and a body span at `fontSize: 12.5`, `lineHeight: 1.6`, `color: 'var(--dm)'`, `textAlign: 'center'` reading exactly `Insert a card or plug in a drive and StorCat will offer it. Every catalog is a plain .json plus a browsable .html — nothing is stored in a database you can lose.` The em dash and the sentence structure are transcribed, not paraphrased. The 420px cap belongs on this column, not on the outer container, so the body wraps rather than truncating.

Third, a button row: a div with `display: 'flex'`, `gap: 10` containing two `button` elements, each `type="button"` with no `onClick`. The primary is `height: 32`, `padding: '0 15px'`, `borderRadius: 8`, `background: 'var(--ac)'`, `color: 'var(--onac)'`, `border: 'none'`, `fontSize: 12.5`, `fontWeight: 600`, `cursor: 'pointer'`, reading exactly `Catalog a volume`. The secondary is `height: 32`, `padding: '0 15px'`, `borderRadius: 8`, `background: 'transparent'`, `color: 'var(--tx)'`, `border: '1px solid var(--l)'`, `fontSize: 12.5`, `cursor: 'pointer'`, reading exactly `Choose catalog folder…`. Both are visible text buttons and need no `aria-label`.

The primary button is the workspace's single visual focal point and the only large accent fill on screen in this phase. No other element in this file may carry an accent fill, an accent border, or a shadow that competes with it.

The tree row contract is already declared as `.ws-tree-row` in `workspace.css` by plan 22-01, reading `height: var(--rh)` and `font-size: var(--fs)` with nowrap, hidden overflow and ellipsis, so Phase 23's virtualizer inherits a fixed-height, ellipsizing row rather than measuring dynamically. Do not hardcode `27px`, `34px`, `12px` or `13px` as a row dimension anywhere, and do not edit `workspace.css` — plans 22-03 and 22-04 run in the same wave and that stylesheet has a single owner.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && npx tsc --noEmit && npm run build && test $(grep -c "Nothing catalogued yet" src/components/workspace/TreePane.tsx) -eq 1 && test $(grep -c "Catalog a volume" src/components/workspace/TreePane.tsx) -eq 1 && test $(grep -c "Choose catalog folder…" src/components/workspace/TreePane.tsx) -eq 1 && test $(grep -c "onClick\|antd\|zIndex" src/components/workspace/TreePane.tsx) -eq 0</automated>
    <human-check>Run `wails dev`. The tree pane fills the `1fr` middle column with a vertically and horizontally centred empty state. The body text wraps inside a 420px measure and never reaches the pane edges. The accent-filled `Catalog a volume` button is unmistakably the loudest element on screen. Cycle all 11 themes with Ctrl+Alt+T and confirm the button label stays legible in every one, especially Gruvbox Dark, Monokai and GitHub Light. Click both buttons: nothing happens, no console error, no surface opens.</human-check>
  </verify>
  <acceptance_criteria>
    - Each of these strings appears exactly once and character-identically in `TreePane.tsx`: `Nothing catalogued yet`, `Insert a card or plug in a drive and StorCat will offer it. Every catalog is a plain .json plus a browsable .html — nothing is stored in a database you can lose.`, `Catalog a volume`, `Choose catalog folder…`.
    - `grep -c "onClick" frontend/src/components/workspace/TreePane.tsx` returns 0.
    - `grep -c "maxWidth: *420" frontend/src/components/workspace/TreePane.tsx` returns 1.
    - `grep -c "var(--onac)" frontend/src/components/workspace/TreePane.tsx` returns 1 — the primary CTA's label colour.
    - `grep -c "27px\|34px" frontend/src/components/workspace/TreePane.tsx` returns 0 — row height comes from `var(--rh)`, never a literal.
    - `grep -c "className=\"pane-scroll\"" frontend/src/components/workspace/TreePane.tsx` returns 1.
    - `grep -c "antd" frontend/src/components/workspace/TreePane.tsx` returns 0 and `grep -c "z-index\|zIndex"` returns 0.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The tree pane renders its centred empty state at spec dimensions with copy transcribed verbatim; the accent CTA is the screen's focal point and its label is legible in all 11 themes; both CTAs are inert.</done>
</task>

<task type="auto">
  <name>Task 2: Build the details panel as one component with a no-selection placeholder</name>
  <files>frontend/src/components/workspace/DetailsPanel.tsx</files>
  <read_first>
    - `.planning/phases/22-shell-token-layer/22-UI-SPEC.md` — "Copywriting Contract" (the details-panel no-selection row, marked Claude's Discretion, including the explicit instruction that this state carries no menu button and no footer action buttons), "Layout & Responsive Contract" (the same-component pane-or-drawer rule), "Typography", "Density Tokens", "UI Considerations / E4"
    - `.planning/phases/22-shell-token-layer/22-PATTERNS.md` — the `components/workspace/{…,DetailsPanel,…}` section and the `.pane-scroll` convention
    - `frontend/src/components/workspace/DetailsPanel.tsx` and `frontend/src/workspace.css` as written by plan 22-01
    - `design_handoff_storcat_ui/designs/StorCat 1a Demo.dc.html` lines 206-212 — the details pane shell, for the padding and gap values only (its populated contents are Phase 23)
  </read_first>
  <action>
Rewrite `DetailsPanel.tsx` as a plain function declaration with a default export at the bottom. Inline style objects reading `var(--…)`; no Ant Design imports.

Declare an exported props interface named `DetailsPanelProps` immediately above the component, per the project's `{ComponentName}Props` convention. Give it no members yet — plan 22-07 adds a `variant` member of `'pane' | 'drawer'` to this same interface when it introduces the drawer. Declaring the interface now, empty, is what keeps 22-07 an additive edit rather than a signature change; do not pre-add the member here, because nothing renders a drawer yet and an unused union would be dead code.

Keep the root as the `div.ws-details` the tracer created (`order: 3`, `background: var(--p)`, `display: flex`, `flexDirection: column`, `minHeight: 0`, `borderLeft: 1px solid var(--l)`). Add inline `padding: 14`, `gap: 16` — the details-panel padding and vertical-gap values from the Spacing Scale.

Inside it render two children.

First, the heading label: a span at `fontSize: 12`, `fontWeight: 600`, `letterSpacing: '0.04em'`, `textTransform: 'uppercase'`, `color: 'var(--dm)'`, `flex: 'none'` whose markup text is `Details`; the CSS property does the uppercasing, so the rendered result is `DETAILS`. Render no adjacent menu button — there is no catalog context for actions until Phase 23.

Second, the body: a div with `className="pane-scroll"` and inline `display: 'flex'`, `alignItems: 'center'`, `justifyContent: 'center'`, containing a single span at `fontSize: 12.5`, `color: 'var(--dm)'`, `textAlign: 'center'`, `lineHeight: 1.5` reading exactly `Nothing selected. Pick a catalog in the rail, or catalog a volume to get started.` Render no footer action buttons in this state — they require a catalog context that does not exist yet.

The key/value row contract is already declared in `workspace.css` by plan 22-01 as `.ws-meta-row` (vertical padding reading `var(--mp)`) and `.ws-meta-value` (nowrap, hidden overflow, ellipsis), so Phase 23's paths, sizes and timestamps ellipsize on one line and follow density without a component change. Do not hardcode `6px` or `10px` as the row's vertical padding, and do not edit `workspace.css` in this plan.

This component must remain the single details implementation. Do not add a second mobile-only variant, do not branch on window width inside it, and do not read a media query here — plan 22-07 wires the pane-versus-drawer decision from `WorkspaceShell` using a `useMediaQuery` hook and passes it down as the `variant` prop.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/storcat/frontend && npx tsc --noEmit && npm run build && test $(grep -c "Nothing selected. Pick a catalog in the rail, or catalog a volume to get started." src/components/workspace/DetailsPanel.tsx) -eq 1 && test $(grep -c "DetailsPanelProps" src/components/workspace/DetailsPanel.tsx) -ge 1 && test $(grep -c "matchMedia\|useMediaQuery\|innerWidth\|antd\|zIndex" src/components/workspace/DetailsPanel.tsx) -eq 0</automated>
    <human-check>Run `wails dev` at a window width of 1280px or more. The details panel occupies the third column at 288px with a 1px `--l` left border, 14px padding, and a 16px gap between the DETAILS label and the body. The label renders uppercase in `--dm`. The placeholder text is centred both ways and wraps inside the 288px measure. There is no menu button beside the label and no buttons at the bottom of the panel.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "Nothing selected. Pick a catalog in the rail, or catalog a volume to get started." frontend/src/components/workspace/DetailsPanel.tsx` returns 1.
    - `grep -c "DetailsPanelProps" frontend/src/components/workspace/DetailsPanel.tsx` returns at least 1 and the interface is exported.
    - `grep -c "matchMedia\|useMediaQuery\|innerWidth" frontend/src/components/workspace/DetailsPanel.tsx` returns 0 — the tier decision lives in `WorkspaceShell`, not here.
    - `grep -c "onClick" frontend/src/components/workspace/DetailsPanel.tsx` returns 0 — no menu button and no footer actions in the no-selection state.
    - `grep -c "className=\"pane-scroll\"" frontend/src/components/workspace/DetailsPanel.tsx` returns 1.
    - `grep -c "textTransform" frontend/src/components/workspace/DetailsPanel.tsx` returns 1 — the label is uppercased by CSS, and the markup text is mixed case.
    - `grep -c "antd" frontend/src/components/workspace/DetailsPanel.tsx` returns 0 and `grep -c "z-index\|zIndex"` returns 0.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The details panel renders as one component with an uppercase DETAILS label and a centred no-selection placeholder, no menu button, no footer actions, and no internal tier branching — ready for plan 22-07 to render it as either a pane or a drawer.</done>
</task>

</tasks>

<flagged_planner_assumptions>
None land in this plan. The edge probe classified no rows against the tree pane or details panel; both surfaces are covered entirely by the UI-SPEC `## UI Considerations` items above (E3 and E4), all of which are resolved rather than unclassified. This is recorded so the phase-wide no-silent-drop count stays auditable.
</flagged_planner_assumptions>

<artifacts_this_phase_produces>
**Created by this plan**

| Artifact | Kind |
|---|---|
| `TreePane` component body — centred empty state, dashed 46px placeholder mark, heading, 420px-capped body, accent CTA and outlined secondary CTA | React component (file created by 22-01) |
| `DetailsPanel` component body — `DETAILS` label and no-selection placeholder | React component (file created by 22-01) |
| `DetailsPanelProps` | exported TypeScript interface, declared empty; 22-07 adds its `variant: 'pane' \| 'drawer'` member |
| `.ws-tree-row`, `.ws-meta-row` and `.ws-meta-value` as the row and metadata contracts Phase 23 inherits | class references only; all three rules were declared by 22-01 |

**Consumed from other plans in this phase**

`.ws-tree`, `.ws-details`, `.pane-scroll`, `.mono` and the token set from `workspace.css` (22-01); `--ac`, `--onac`, `--dm`, `--l`, `--rh`, `--fs`, `--mp` from `applyTokens` in `themeTokens.ts` (22-01); the `IBM Plex Sans`/`IBM Plex Mono` faces (22-02). The details panel's drawer rendering, backdrop, Escape handling and the toolbar Details chip are added by 22-07.

**Not produced here** — no new file, no new CSS custom property, no reducer action, no Go change. The full phase-wide artifact index lives in `22-01-PLAN.md`.
</artifacts_this_phase_produces>

<coverage_counts>
- **UI-SPEC `## UI Considerations`:** 32 applicable phase-wide; this plan carries **12** — E3 (tree pane): 8 of 8 (empty, loading, error, populated, partial, overflow, zero-one-many, long-text; 6 explicit and 2 backstop, the two backstop items authored as flat `{ statement, verification: backstop }` scalars) plus E4 (details panel): 4 of 4 (loading, error, overflow, long-text, all explicit). Phase-wide split: 22-01 (E6) 4 + 22-03 (E1) 6 + 22-04 (E2, E5) 10 + 22-05 (E3, E4) 12 = 32. Zero dropped.
- **Spec-less edge probe:** 34 applicable phase-wide; this plan carries **0** — the probe classified no rows against these two surfaces. Phase-wide: 26 authored (22-01 12, 22-02 7, 22-06 2, 22-07 5) + 8 flagged (22-01 3, 22-03 2, 22-06 3) = 34.
- **Prohibitions:** 2 survivors authored descriptor-less. Dropped during precision-drop: "do not hardcode row height" and "do not import antd" (routine engineering hygiene, both enforced as acceptance criteria); no canon-security survivor applies — neither surface accepts or renders any user-supplied string in this phase.
</coverage_counts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Neither surface accepts input, calls a binding, or renders any value originating outside this repo in Phase 22 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-04 | Tampering (injection) | `TreePane.tsx`, `DetailsPanel.tsx` | medium | mitigate | All copy is authored string literals rendered as React text children; neither file assigns `innerHTML` nor uses `dangerouslySetInnerHTML`. Phase 23 introduces the first externally-sourced strings (catalog titles, filenames, paths) into these same components and must re-check this. <!-- planner-discipline-allow: dangerouslySetInnerHTML --> |
| T-22-10 | Information disclosure | details panel placeholder | low | mitigate | The no-selection state renders a fixed placeholder sentence with no filesystem path, no catalog name and no example metadata, so nothing about the local machine leaks through a screenshot or screen share in this phase. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run build` exits 0.
- Every Copywriting Contract string for the tree pane and details panel appears exactly once and character-identically.
- No `onClick`, no density or row-height pixel literal, no `antd` import, no `zIndex`, and no media query in either file.
- Manual matrix row THEME-02 from `22-VALIDATION.md` passes across all 11 themes on the `Catalog a volume` button.
</verification>

<success_criteria>
The tree pane and details panel render at their specified dimensions and tokens with copy transcribed verbatim from the Copywriting Contract; the accent-filled CTA is the workspace's focal point and its label is legible in all 11 themes; the details panel is a single component with no internal tier branching, ready to be rendered as pane or drawer.
</success_criteria>

<output>
Create `.planning/phases/22-shell-token-layer/22-05-SUMMARY.md` when done.
</output>