---
phase: 16-secrets-certificate-procurement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbooks/credential-rotation.md
autonomous: true
requirements:
  - CRED-05
  - CRED-06

must_haves:
  truths:
    - "GitHub release environment exists with deployment branch policy restricting to v*.*.* tags"
    - "Credential rotation runbook exists documenting expiry dates and renewal steps for all 9 secrets"
  artifacts:
    - path: "docs/runbooks/credential-rotation.md"
      provides: "Credential rotation runbook"
      contains: "APPLE_CERTIFICATE"
  key_links:
    - from: "docs/runbooks/credential-rotation.md"
      to: "GitHub release environment"
      via: "Documents all 9 secret names that will be stored"
      pattern: "ES_USERNAME|APPLE_CERTIFICATE"
---

<objective>
Create the GitHub Actions `release` environment with deployment branch policy, and write the credential rotation runbook document.

Purpose: The release environment is a prerequisite for storing any secrets (Plans 02 and 03). The runbook satisfies CRED-06 and serves as a reference during the credential-gathering tasks.
Output: GitHub `release` environment configured; `docs/runbooks/credential-rotation.md` committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-secrets-certificate-procurement/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub release environment with deployment branch policy</name>
  <files>n/a (GitHub API operation, no files modified)</files>
  <read_first>
    - .planning/phases/16-secrets-certificate-procurement/16-RESEARCH.md (environment creation commands in "Create GitHub release Environment" section)
  </read_first>
  <action>
Create the GitHub `release` environment for scottkw/storcat using the gh CLI:

1. Create the environment with custom branch policy enabled:
```bash
gh api --method PUT repos/scottkw/storcat/environments/release \
  --field wait_timer=0 \
  --field prevent_self_review=false \
  --field 'reviewers=[]' \
  --field 'deployment_branch_policy={"protected_branches":false,"custom_branch_policies":true}'
```

2. Add tag deployment policy restricting to semantic version tags only:
```bash
gh api --method POST \
  repos/scottkw/storcat/environments/release/deployment-branch-policies \
  --field name="v*.*.*" \
  --field type="tag"
```

3. Verify the environment was created:
```bash
gh api repos/scottkw/storcat/environments/release
```

Per CRED-05: The environment must exist before any secrets can be stored. No reviewer requirement is set (solo developer project -- tag pattern restriction is sufficient protection).

Do NOT set required reviewers -- per research Pitfall 5, this would block CI from running automatically on tags for a solo developer.
  </action>
  <verify>
    <automated>gh api repos/scottkw/storcat/environments/release --jq '.name' 2>/dev/null | grep -q "release" && echo "PASS: release environment exists" || echo "FAIL: release environment not found"</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/scottkw/storcat/environments/release --jq '.name'` returns `release`
    - `gh api repos/scottkw/storcat/environments/release/deployment-branch-policies --jq '.branch_policies[0].name'` returns `v*.*.*`
    - `gh api repos/scottkw/storcat/environments/release --jq '.deployment_branch_policy.custom_branch_policies'` returns `true`
  </acceptance_criteria>
  <done>GitHub release environment exists with v*.*.* tag deployment policy, ready to receive secrets</done>
</task>

<task type="auto">
  <name>Task 2: Write credential rotation runbook</name>
  <files>docs/runbooks/credential-rotation.md</files>
  <read_first>
    - .planning/phases/16-secrets-certificate-procurement/16-RESEARCH.md (secret names, expiry info, vendor details)
  </read_first>
  <action>
Create `docs/runbooks/credential-rotation.md` with the following exact content structure:

**Title:** `# Credential Rotation Runbook`

**Section 1: Overview**
- Purpose: documents all 9 signing secrets in the GitHub `release` environment
- When to use: when any certificate expires or is compromised
- Environment: `release` in `scottkw/storcat`

**Section 2: Apple Credentials (5 secrets)**

For each of the 5 Apple secrets, document:
| Secret | What It Contains | Expiry | Renewal Steps |
|--------|-----------------|--------|---------------|
| `APPLE_CERTIFICATE` | base64-encoded .p12 (Developer ID Application cert + private key) | Certificate expires 2027-02-01 | Re-export from Keychain Access: login keychain > My Certificates > select cert + private key > Export 2 Items as .p12 > `base64 -i file.p12 \| pbcopy` > `gh secret set APPLE_CERTIFICATE --env release --repo scottkw/storcat` |
| `APPLE_CERTIFICATE_PASSWORD` | Password used during .p12 export | Same as APPLE_CERTIFICATE (re-created on each export) | Set new password during .p12 export > `gh secret set APPLE_CERTIFICATE_PASSWORD --env release --repo scottkw/storcat` |
| `APPLE_CERTIFICATE_NAME` | `Developer ID Application: Ken Scott (S2K7P43927)` | Same as certificate | Only changes if identity name changes (unlikely); verify with `security find-identity -v -p codesigning` |
| `APPLE_NOTARIZATION_PASSWORD` | App-specific password for Apple ID | Does not expire unless revoked | Generate at appleid.apple.com > Sign-In and Security > App-Specific Passwords > name it "StorCat CI" > `gh secret set APPLE_NOTARIZATION_PASSWORD --env release --repo scottkw/storcat` |
| `APPLE_TEAM_ID` | `S2K7P43927` | Does not expire | Only changes if Apple Developer account is recreated |

**Section 3: Windows Credentials (4 secrets, SSL.com eSigner)**

| Secret | What It Contains | Expiry | Renewal Steps |
|--------|-----------------|--------|---------------|
| `ES_USERNAME` | SSL.com account email | Does not expire | Update if SSL.com account email changes |
| `ES_PASSWORD` | SSL.com account password | Per SSL.com password policy | Update in both SSL.com and `gh secret set ES_PASSWORD --env release --repo scottkw/storcat` |
| `CREDENTIAL_ID` | Signing credential ID from SSL.com dashboard | When OV certificate expires (typically 1-3 years) | Purchase new OV cert > enroll in eSigner > copy new credential ID |
| `ES_TOTP_SECRET` | TOTP seed for eSigner MFA | Same as CREDENTIAL_ID (re-enrollment generates new TOTP) | CRITICAL: shown only once during eSigner enrollment -- save immediately or must re-enroll |

**Section 4: Verification Commands**
```bash
# Check Apple cert validity on local machine
security find-identity -v -p codesigning | grep "Developer ID Application: Ken Scott"

# Check Apple cert expiry
security find-certificate -c "Developer ID Application: Ken Scott" -p | openssl x509 -noout -dates

# List all secrets in release environment (expect 9)
gh secret list --env release --repo scottkw/storcat

# Verify specific secret exists (name only, values are hidden)
gh secret list --env release --repo scottkw/storcat | grep APPLE_CERTIFICATE
```

**Section 5: Emergency Procedures**
- If Apple cert is compromised: revoke at developer.apple.com, generate new cert, re-export .p12, update `APPLE_CERTIFICATE` and `APPLE_CERTIFICATE_PASSWORD`
- If SSL.com credentials are compromised: log into SSL.com dashboard, revoke certificate, re-enroll, update all 4 ES_* secrets
- If any secret is accidentally exposed in logs: rotate immediately using the renewal steps above

**Section 6: Future Improvements**
- SIGN-07: Migrate to App Store Connect API key for notarization (more robust than app-specific password)
- WSIGN-05: Evaluate Azure Artifact Signing if business eligibility is met
- SIGN-09: Scheduled GitHub Actions to monitor certificate expiry dates

Per CRED-06: This document must exist and cover all 9 secrets.
  </action>
  <verify>
    <automated>test -f docs/runbooks/credential-rotation.md && grep -q "APPLE_CERTIFICATE" docs/runbooks/credential-rotation.md && grep -q "ES_USERNAME" docs/runbooks/credential-rotation.md && grep -q "ES_TOTP_SECRET" docs/runbooks/credential-rotation.md && grep -c "gh secret set" docs/runbooks/credential-rotation.md | grep -q "[5-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/runbooks/credential-rotation.md` exists
    - File contains all 5 Apple secret names: `APPLE_CERTIFICATE`, `APPLE_CERTIFICATE_PASSWORD`, `APPLE_CERTIFICATE_NAME`, `APPLE_NOTARIZATION_PASSWORD`, `APPLE_TEAM_ID`
    - File contains all 4 Windows secret names: `ES_USERNAME`, `ES_PASSWORD`, `CREDENTIAL_ID`, `ES_TOTP_SECRET`
    - File contains `gh secret set` commands for each secret
    - File contains expiry date `2027-02-01` for Apple certificate
    - File contains `security find-identity` verification command
    - File contains emergency procedures section
  </acceptance_criteria>
  <done>Credential rotation runbook exists at docs/runbooks/credential-rotation.md covering all 9 secrets with expiry dates, renewal steps, verification commands, and emergency procedures</done>
</task>

</tasks>

<verification>
- GitHub `release` environment exists: `gh api repos/scottkw/storcat/environments/release --jq '.name'` returns `release`
- Environment has tag policy: `gh api repos/scottkw/storcat/environments/release/deployment-branch-policies --jq '.branch_policies[0].name'` returns `v*.*.*`
- Runbook exists with all 9 secrets documented: `grep -c "ES_\|APPLE_\|CREDENTIAL_ID" docs/runbooks/credential-rotation.md` returns 20+
</verification>

<success_criteria>
- GitHub release environment is accessible and configured with custom branch policy for v*.*.* tags
- Credential rotation runbook is committed and covers all 9 signing secrets with renewal instructions
</success_criteria>

<output>
After completion, create `.planning/phases/16-secrets-certificate-procurement/16-01-SUMMARY.md`
</output>
