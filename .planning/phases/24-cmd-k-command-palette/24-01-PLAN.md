---
phase: 24-cmd-k-command-palette
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/models/catalog.go
  - internal/search/search_indexed.go
  - internal/search/search_indexed_test.go
  - cli/search_test.go
  - app.go
  - frontend/wailsjs/go/main/App.d.ts
  - frontend/wailsjs/go/main/App.js
  - frontend/wailsjs/go/models.ts
  - frontend/src/services/wailsAPI.ts
  - frontend/src/components/workspace/CommandPalette.tsx
  - frontend/src/components/workspace/WorkspaceShell.tsx
  - frontend/src/components/workspace/Toolbar.tsx
  - frontend/src/workspace.css
autonomous: true
requirements: [PLT-01, PLT-02, PLT-03]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward observable truths ---
    - "User presses ⌘K (or Ctrl+K) anywhere in the workspace and a centered search palette appears with the caret already in its input."
    - "User clicks the toolbar's existing search field and the same palette appears — the button is no longer inert."
    - "User types at least 2 characters and, ~200ms after the last keystroke, sees real file/directory hits drawn from every .json catalog in the configured catalog directory."
    - "The palette shows at most 50 rows regardless of how many nodes matched, and the true match count is always available to the UI as a separate number computed in Go."
    - "A second ⌘K press while the palette is already open is a no-op — it does not re-open, re-focus, or discard the query in progress."
    - "cli/search.go's output for a fixed fixture is identical before and after this phase's Go changes."
    # --- Probe-lifted edges (specless fallback, resolved/explicit) ---
    - "PLT-02 adjacency: two catalogs containing an identical path produce two separate result rows, each carrying its own catalog name. Results are never deduplicated across catalogs."
    - "PLT-02 empty: an empty catalog directory (no .json files) and a catalog with zero nodes both yield 0 results and total: 0 — never an error and never a rejected promise."
    - "PLT-02 encoding: the capped binding's matcher is byte-identical to the existing Service.SearchCatalogs matcher (strings.ToLower on both sides, internal/search/service.go:117-121). No Unicode normalization is added, because changing the matcher would change cli/search.go's results and violate the locked 'CLI behaves exactly as before' constraint. Proven by a Go test asserting the capped result equals SearchCatalogs(term, dir)[:min(50, len)] and total == len(SearchCatalogs(term, dir)) for the same fixture. Known limitation surfaced, not fixed: macOS APFS stores filenames NFD, so an NFC-typed query will not substring-match an NFD-stored name — pre-existing behavior inherited unchanged, not a regression introduced here."
    - "PLT-02 ordering: result order is exactly the existing service's walk order (os.ReadDir entry order across catalogs, then DFS within each catalog) and is stable across identical runs. The 50-cap takes the first 50 in that order — never a re-sort."
    - "PLT-06 concurrency: the stale-response guard is total — a slower earlier query's response can never overwrite a newer query's rendered results."
    # --- UI-SPEC ## UI Considerations lifted (E1 loading/error/overflow; E2 all six; E4 all six) ---
    - "UI-SPEC E1 (palette shell): the panel mounts synchronously on open with the input focused before any search has run; a failed search binding call degrades to the zero-result branch rather than a distinct error UI; the panel is max-height min(520px, 70vh) with the result list as the only internal scroll region while the input row and footer are flex:none and never scroll."
    - "UI-SPEC E2 (input & live search): an empty or 1-character query renders the hint branch, never the no-match branch; the debounce is 200ms and the quiet 'Searching…' readout appears only when no prior settled result set exists, so later keystrokes keep prior results visible instead of flashing; a binding failure produces no input-level error state; a pathologically long query scrolls natively inside the input with no custom truncation and no wrapping; the input is never cleared, disabled, or visually degraded while a request is in flight."
    - "UI-SPEC E4 (toolbar trigger): the button's visual footprint is byte-unchanged from Phase 22 — only an onClick is added; it is never disabled or hidden for an empty library; it has no loading, error, count, badge, or dot state in any data condition; it renders only fixed literals so no truncation rule applies beyond Phase 22's existing max-width 460px / min-width 0."
  prohibitions:
    - "MUST NOT present the capped 50-row result set as if it were the complete answer: the count the UI renders must be the true backend total, never the rendered row count and never a client-side estimate."
    - "MUST NOT let a catalog that could not be read or parsed make the cross-catalog result set look exhaustive — the silent per-file skip inherited from Service.SearchCatalogs must not be presented to the user as a complete search of the library."
  artifacts:
    - path: "pkg/models/catalog.go"
      provides: "SearchIndexResult transport struct for the capped GUI search"
      contains: "type SearchIndexResult struct"
    - path: "internal/search/search_indexed.go"
      provides: "Service.SearchIndexed — capped wrapper over the unmodified SearchCatalogs walk"
      contains: "func (s *Service) SearchIndexed"
    - path: "internal/search/search_indexed_test.go"
      provides: "Boundary coverage for the 50-cap and total count (0 / 50 / 51 matches) plus parity with SearchCatalogs"
      contains: "func TestSearchIndexed"
    - path: "app.go"
      provides: "App.SearchIndexed Wails binding"
      contains: "func (a *App) SearchIndexed"
    - path: "frontend/src/services/wailsAPI.ts"
      provides: "searchIndexed wrapper routed through wailsError/extractErrorMessage"
      contains: "searchIndexed:"
    - path: "frontend/src/components/workspace/CommandPalette.tsx"
      provides: "Palette overlay: scrim, panel, input, 200ms debounce, request-id stale guard, result rows"
      contains: "export default CommandPalette"
      min_lines: 120
    - path: "frontend/src/workspace.css"
      provides: "Palette shell classes at the locked --z-overlay layer"
      contains: ".ws-palette-scrim"
  key_links:
    - from: "app.go"
      to: "internal/search/search_indexed.go"
      via: "binding delegates to a.searchService.SearchIndexed(searchTerm, absPath)"
      pattern: "searchService\\.SearchIndexed"
    - from: "frontend/src/services/wailsAPI.ts"
      to: "frontend/wailsjs/go/main/App"
      via: "imports the regenerated SearchIndexed binding"
      pattern: "SearchIndexed"
    - from: "frontend/src/components/workspace/CommandPalette.tsx"
      to: "frontend/src/services/wailsAPI.ts"
      via: "debounced call to wailsAPI.searchIndexed(query, state.catalogDir)"
      pattern: "wailsAPI\\.searchIndexed"
    - from: "frontend/src/components/workspace/WorkspaceShell.tsx"
      to: "frontend/src/components/workspace/CommandPalette.tsx"
      via: "always-mounted CommandPalette driven by a paletteOpen state flag"
      pattern: "CommandPalette"
    - from: "frontend/src/components/workspace/Toolbar.tsx"
      to: "frontend/src/components/workspace/WorkspaceShell.tsx"
      via: "onOpenSearch prop drilled in, wired to the .ws-search button's onClick"
      pattern: "onOpenSearch"
---

<objective>
Prove the whole ⌘K search path end to end, through every layer this phase touches, on one thin slice.

A user presses ⌘K (or clicks the toolbar search field), types ≥2 characters, and sees real matches from every catalog in the configured directory — capped at 50 in Go, with the true total carried alongside. The Go walk that the CLI shares is not modified; a new thin wrapper caps it.

Purpose: this is the phase's riskiest unknown (RESEARCH Open Question #1 — whether ⌘K reaches a `window` keydown listener inside macOS WKWebView) and its whole architecture (Go-side cap, generated bridge, always-mounted overlay, debounce + request-id guard). Proving it now costs one commit; discovering it at task nine costs the phase.

Output: `models.SearchIndexResult`, `Service.SearchIndexed`, `App.SearchIndexed`, the regenerated Wails bridge, `wailsAPI.searchIndexed`, `CommandPalette.tsx`, the palette shell CSS, and the two open paths (⌘K listener + toolbar click).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-cmd-k-command-palette/24-CONTEXT.md
@.planning/phases/24-cmd-k-command-palette/24-RESEARCH.md
@.planning/phases/24-cmd-k-command-palette/24-PATTERNS.md
@.planning/phases/24-cmd-k-command-palette/24-UI-SPEC.md
</context>

## Artifacts this phase produces

Symbols created across all five Phase 24 plans. This plan creates the ones marked **[24-01]**.

**Go**
- **[24-01]** `models.SearchIndexResult` struct — fields `Results []*SearchResult` (json `results`), `Total int` (json `total`) in `pkg/models/catalog.go`
- **[24-01]** `search.SearchIndexedCap` exported const = `50` in `internal/search/search_indexed.go`
- **[24-01]** `(*search.Service).SearchIndexed(searchTerm, catalogDirectory string) (*models.SearchIndexResult, error)`
- **[24-01]** `(*App).SearchIndexed(searchTerm string, catalogDir string) (*models.SearchIndexResult, error)` — Wails binding
- **[24-01]** New files `internal/search/search_indexed.go`, `internal/search/search_indexed_test.go`

**Generated bridge (never hand-edited)**
- **[24-01]** `SearchIndexed` in `frontend/wailsjs/go/main/App.d.ts` and `App.js`
- **[24-01]** `models.SearchIndexResult` in `frontend/wailsjs/go/models.ts`

**Frontend**
- **[24-01]** `wailsAPI.searchIndexed(searchTerm, catalogDir)` in `frontend/src/services/wailsAPI.ts`
- **[24-01]** `CommandPalette` + `CommandPaletteProps` in `frontend/src/components/workspace/CommandPalette.tsx`
- **[24-01]** `ToolbarProps.onOpenSearch` in `frontend/src/components/workspace/Toolbar.tsx`
- **[24-01]** `paletteOpen` state + global ⌘K keydown listener in `frontend/src/components/workspace/WorkspaceShell.tsx`
- [24-03] `useModalBehavior` in `frontend/src/hooks/useModalBehavior.ts`
- [24-04] `PaletteResultRow` in `frontend/src/components/workspace/palette/PaletteResultRow.tsx`
- [24-04] `PaletteResultList` in `frontend/src/components/workspace/palette/PaletteResultList.tsx`
- [24-05] `findNodeIndexByPath`, `ancestorPathsOf`, `mergeExpanded` in `frontend/src/lib/reveal.ts`
- [24-05] `AppState.pendingReveal: string | null` and the `SET_PENDING_REVEAL` action in `frontend/src/contexts/AppContext.tsx`

**CSS classes in `frontend/src/workspace.css`**
- **[24-01]** `.ws-palette-scrim`, `.ws-palette-panel`, `.ws-palette-input-row`, `.ws-palette-input`, `.ws-palette-list`, `.ws-palette-row`
- [24-04] `.ws-palette-shape`, `.ws-palette-name`, `.ws-palette-path`, `.ws-palette-chip`, `.ws-palette-size`, `.ws-palette-truncation`, `.ws-palette-state`, `.ws-palette-footer`

## Flagged assumptions (specless probe — unresolved rows)

The deterministic edge probe surfaced 12 applicable edges for this phase's requirements. Eight were classified and are authored into `must_haves.truths` above (five in this plan, three in 24-05). Four came back **unclassified** and are therefore carried as explicit planner assumptions rather than silently dropped. Two of the four belong to this plan:

- **PLT-01 (unclassified)** — "open via ⌘K / toolbar click, autofocused". Assumption: a `window`-level `keydown` listener is sufficient to receive `⌘K`/`Ctrl+K` inside this app's WKWebView, on the strength of the working Escape-listener precedent at `WorkspaceShell.tsx:34-41`. **This assumption is not proven by anything in the repo** and is the explicit subject of plan 24-02's blocking gate. If it is false, PLT-01's keyboard path needs a Wails `options.App.Menu` accelerator, which is out of this phase's planned scope and must go back to discuss-phase.
- **PLT-03 (unclassified)** — "at most 50 results + 'Showing the first 50 of N hits'". Assumption: `Total` is meaningful and finite for every reachable input because `SearchCatalogs` always returns a fully materialised slice; there is no streaming or partial-count path to reconcile. The truncation copy itself lands in 24-04.

The remaining two unclassified rows (PLT-04, PLT-07) are carried in plans 24-04 and 24-03 respectively.

## Prohibition probe — canon referrals (dropped, not minted)

The Stage-1/Stage-2 prohibition pass over PLT-01, PLT-02 and PLT-03 surfaced one candidate that is canon rather than bespoke, so it is breadcrumbed and dropped here instead of being minted as a `must_haves.prohibitions` item:

- *A new Wails binding reading a directory outside the configured catalog directory* — path traversal / containment is canon, covered by `/gsd-secure-phase` and by threat **T-24-01** below, which registers `App.SearchIndexed` into the FU-23-A containment sweep already assigned to Phase 26. Not minted here, so the bespoke signal in `must_haves.prohibitions` stays undiluted.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Capped cross-catalog search in Go, with the CLI's walk provably untouched</name>
  <reversibility rating="costly">The binding's name, its result-struct shape, and the decision to cap in Go rather than in the frontend are consumed by the generated bridge, the wailsAPI wrapper, and the palette; changing any of them later is a coordinated four-file edit, but no migration and no external contract is involved.</reversibility>
  <files>pkg/models/catalog.go, internal/search/search_indexed.go, internal/search/search_indexed_test.go, cli/search_test.go</files>
  <read_first>
    - internal/search/service.go — `SearchCatalogs` (lines 64-92), `searchInCatalogFile`, `searchInCatalog` (lines 117-147). This is the walk being wrapped, verbatim and unmodified.
    - internal/search/flatten.go — `LoadCatalogFlat`'s doc-comment and delegation shape; it is the exact "thin GUI-shaping wrapper beside a CLI-shared method" analog for the new file.
    - internal/search/service_test.go — `writeTestCatalog` temp-dir fixture helper convention.
    - internal/search/flatten_test.go — `writeFlattenTestCatalog(t, dir, name, content)` helper plus the table-driven walk convention.
    - pkg/models/catalog.go — `SearchResult` (lines 11-20) and the doc-comment density of the surrounding structs.
    - cli/search.go — lines 61-62, the direct `svc.SearchCatalogs(term, dir)` call this task must leave untouched.
    - cli/search_test.go — `package cli_test`, `captureOutput`, `writeTestCatalog(t, dir, name)` conventions to extend.
  </read_first>
  <behavior>
    - `SearchIndexed` on a directory with 0 matching nodes returns a non-nil result with `Total == 0` and `len(Results) == 0`, and a nil error.
    - `SearchIndexed` on a fixture producing exactly 50 matches returns `Total == 50` and `len(Results) == 50` — no truncation at the boundary.
    - `SearchIndexed` on a fixture producing 51 matches returns `Total == 51` and `len(Results) == 50`.
    - For any fixture and term, `SearchIndexed(term, dir).Results` is element-for-element equal to the first `min(50, len(all))` elements of `SearchCatalogs(term, dir)`, and `.Total` equals `len(SearchCatalogs(term, dir))` — parity, in the same order, with no re-sort.
    - Two catalogs in one directory that both contain the identical node path produce two distinct results, one per catalog, distinguishable by `Catalog`/`CatalogFilePath`.
    - `SearchIndexed` on a directory that cannot be read returns a non-nil error (propagated from `SearchCatalogs`), not an empty result.
    - `cli.Run([]string{"search", term, dir})` prints the same table rows and exit code after this change as a golden expectation captured in the same test.
  </behavior>
  <action>
    Add `SearchIndexResult` to `pkg/models/catalog.go` as a sibling of `SearchResult`, with exported fields `Results []*SearchResult` tagged `json:"results"` and `Total int` tagged `json:"total"`. Doc-comment it in the same register as the neighbouring structs: it is the GUI-only capped transport for the ⌘K palette; `Total` is the true match count across every catalog, `Results` is the first `SearchIndexedCap` of them.

    Create `internal/search/search_indexed.go` in package `search` with an exported `const SearchIndexedCap = 50` and `func (s *Service) SearchIndexed(searchTerm, catalogDirectory string) (*models.SearchIndexResult, error)`. The body calls `s.SearchCatalogs(searchTerm, catalogDirectory)` unchanged, returns `(nil, err)` on error, sets `total := len(all)`, and truncates `all` to `SearchIndexedCap` only when `total > SearchIndexedCap`. Do not sort, filter, dedupe, re-walk, or re-match — the ordering and the matcher are inherited verbatim so `cli/search.go` and this method can never diverge. Follow `flatten.go`'s doc-comment style: state that it wraps the unmodified `SearchCatalogs`, that the CLI calls `SearchCatalogs` directly at `cli/search.go:61-62` and is untouched, and that only the response is capped — the walk cost is not reduced.

    Create `internal/search/search_indexed_test.go` in package `search` (matching the existing in-package test convention) covering every case in the behavior block. Write the fixture helper in the shape of `flatten_test.go`'s `writeFlattenTestCatalog`, using `t.TempDir()`. Build the 50/51-match fixtures by generating catalog JSON whose node names all contain a common substring, so the boundary counts are exact and derived, not hardcoded from a guess. The parity assertion is the load-bearing one: run `SearchCatalogs` and `SearchIndexed` against the same fixture and term, then compare element by element.

    Extend `cli/search_test.go` — do not replace or restructure it — with one regression test proving `cli/search.go`'s behavior is unchanged by this phase: build a temp catalog directory, run `cli.Run` with a `search` term through `captureOutput`, and assert both the exit code and the presence of the expected basenames in the rendered table. Name it so its intent is unmistakable, e.g. `TestRunSearch_UnaffectedByIndexedCap`, and include a comment stating that `cli/search.go` calls the uncapped `SearchCatalogs` directly and this test is the tripwire if that ever changes.

    Do not modify `internal/search/service.go`, `cli/search.go`, or any existing test.
  </action>
  <acceptance_criteria>
    - `go test ./internal/search/... -run TestSearchIndexed -count=1` exits 0 and reports at least four test functions or subtests run.
    - `go test ./cli/... -run TestRunSearch -count=1` exits 0.
    - `go build ./...` exits 0.
    - `grep -c 'func (s \*Service) SearchCatalogs' internal/search/service.go` is exactly `1` and `git diff --stat -- internal/search/service.go cli/search.go` produces no output (both files untouched by this task).
    - `grep -q 'SearchIndexedCap = 50' internal/search/search_indexed.go` succeeds.
    - `grep -q 'SearchIndexedCap' internal/search/search_indexed_test.go` succeeds — the test asserts against the named constant, not a second literal 50 that could drift from it.
    - `grep -q 'type SearchIndexResult struct' pkg/models/catalog.go` succeeds.
    - The test file contains an assertion comparing `SearchIndexed(...).Results` against a slice of `SearchCatalogs(...)` for the same fixture and term (source assertion: `grep -q 'SearchCatalogs(' internal/search/search_indexed_test.go`).
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./internal/search/... ./cli/... -count=1</automated>
  </verify>
  <done>The capped search exists in Go with boundary coverage at 0, 50, and 51 matches, is provably order- and matcher-identical to the walk the CLI uses, and `cli/search.go` is byte-unchanged with a regression test standing guard.</done>
</task>

<task type="auto">
  <name>Task 2: Expose SearchIndexed across the Wails bridge and the frontend service wrapper</name>
  <precondition>The `wails` CLI is on PATH and `wails version` prints v2.10.2 (the version `go.mod` pins).</precondition>
  <files>app.go, frontend/wailsjs/go/main/App.d.ts, frontend/wailsjs/go/main/App.js, frontend/wailsjs/go/models.ts, frontend/src/services/wailsAPI.ts</files>
  <read_first>
    - app.go — `LoadCatalogFlat` (lines 112-120) and `SearchCatalogs` (lines 83-91). The new binding copies their `filepath.Abs` + single-delegation shape exactly.
    - frontend/src/services/wailsAPI.ts — the destructured import block (lines 1-19), `extractErrorMessage`/`wailsError` (lines 27-41), and the `loadCatalogFlat` wrapper (lines 90-97) which is the literal template for the new entry.
    - frontend/wailsjs/go/main/App.d.ts — the generated signature shape, to confirm regeneration landed rather than assuming it.
  </read_first>
  <action>
    Add `func (a *App) SearchIndexed(searchTerm string, catalogDir string) (*models.SearchIndexResult, error)` to `app.go`, placed immediately after the existing `SearchCatalogs` binding so the pair reads together. Body: `filepath.Abs(catalogDir)`, return `(nil, err)` on failure, otherwise `return a.searchService.SearchIndexed(searchTerm, absPath)`. Add no logic of its own — no validation, no defaulting, no logging. Doc-comment it as the GUI-only capped sibling of `SearchCatalogs`, naming the cap constant.

    Run `wails generate module` from the repository root. This regenerates `frontend/wailsjs/go/main/App.d.ts`, `frontend/wailsjs/go/main/App.js`, and `frontend/wailsjs/go/models.ts`. Do not hand-edit any of those three files; if the expected symbols are absent afterwards, re-run rather than patching by hand.

    Add a `searchIndexed` entry to the `wailsAPI` object in `frontend/src/services/wailsAPI.ts`, positioned next to `searchCatalogs`. Extend the destructured import from `'../../wailsjs/go/main/App'` with `SearchIndexed`. The wrapper is `async (searchTerm: string, catalogDir: string)`, awaits `SearchIndexed(searchTerm, catalogDir)`, returns `{ success: true as const, indexed }` on success, and returns `wailsError(error)` from the catch — routing the rejection through the existing single reader, because this bridge rejects with a plain string and reading `error.message` directly always yields undefined. Do not add a second try/catch idiom and do not reach for the raw generated binding anywhere outside this file.
  </action>
  <acceptance_criteria>
    - `go build ./...` exits 0.
    - `grep -q 'func (a \*App) SearchIndexed' app.go` succeeds and `grep -q 'a.searchService.SearchIndexed(searchTerm, absPath)' app.go` succeeds.
    - `grep -q 'SearchIndexed' frontend/wailsjs/go/main/App.d.ts` succeeds and `grep -q 'SearchIndexed' frontend/wailsjs/go/main/App.js` succeeds — proof the generator ran, not that the binding was written by hand.
    - `grep -q 'SearchIndexResult' frontend/wailsjs/go/models.ts` succeeds.
    - `grep -q 'searchIndexed:' frontend/src/services/wailsAPI.ts` succeeds and the wrapper's catch block routes through the shared error reader: `grep -c 'wailsError(error)' frontend/src/services/wailsAPI.ts` is exactly `19` (18 before this task, plus the new wrapper).
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>`window.go.main.App.SearchIndexed` exists in the generated bridge with a typed `SearchIndexResult` return, and `wailsAPI.searchIndexed` is the single frontend entry point to it, error-handled like every other binding in the file.</done>
</task>

<task type="auto">
  <name>Task 3: The palette overlay, both open paths, and the live debounced search</name>
  <files>frontend/src/components/workspace/CommandPalette.tsx, frontend/src/workspace.css, frontend/src/components/workspace/WorkspaceShell.tsx, frontend/src/components/workspace/Toolbar.tsx</files>
  <read_first>
    - frontend/src/components/workspace/WorkspaceShell.tsx — the whole file. The `useEffect` Escape listener at lines 34-41 is the exact guard-while-open / cleanup idiom to copy for the ⌘K listener, and lines 49-72 are where the palette mounts.
    - frontend/src/components/workspace/Toolbar.tsx — `ToolbarProps` (lines 5-10) for the prop-drilling convention, and the `.ws-search` button (lines 58-106) which gains an onClick and nothing else.
    - frontend/src/components/workspace/TreePane.tsx — lines 192-229, the quiet-text no-spinner loading and empty branches; the palette's transient states use the identical treatment.
    - frontend/src/components/workspace/CatalogRail.tsx — the filter input's local-state-only placement (the query string never enters AppContext). Copy the placement, not its `useDeferredValue` mechanism.
    - frontend/src/components/workspace/StatusBar.tsx — the `catalogCount` / `filesIndexed` derivation the palette placeholder reuses.
    - frontend/src/lib/format.ts — `formatBytes` and `formatCount`.
    - frontend/src/workspace.css — lines 15-34 (density and z-index tokens) and lines 386-394 (`.ws-backdrop`, the click-to-close backdrop precedent).
    - frontend/src/services/wailsAPI.ts — the `searchIndexed` wrapper added in Task 2 and its `{ success, indexed } | { success: false, error }` result shape.
    - .planning/phases/24-cmd-k-command-palette/24-UI-SPEC.md — "Palette Contract" and "Interaction Contract" are the pixel and copy authority for this task.
  </read_first>
  <action>
    Create `frontend/src/components/workspace/CommandPalette.tsx` exporting `CommandPaletteProps { isOpen: boolean; onClose: () => void }` and a default `CommandPalette`. The component is **always mounted** by `WorkspaceShell` and returns `null` when `isOpen` is false — it must not be conditionally mounted, because plan 24-03's shared hook has to observe the `isOpen: true → false` transition to release scroll lock and restore focus, and Phase 25's 260ms animated exit depends on that same contract.

    Palette internals, all local `useState`/`useRef` — nothing here enters `AppContext` except the reveal request added in 24-05:
    - `query` string, reset to empty every time `isOpen` flips true (no persisted last query — search-history persistence is a deferred idea).
    - `results: models.SearchResult[]`, `total: number`, and a `settled: boolean` flag recording whether any query has produced a settled result set this session.
    - `requestIdRef = useRef(0)` as the stale-response guard. Wails bindings take no abort signal, so cancellation is impossible and gating the *handling* of the response is the only mechanism that works: increment the ref before each dispatch, capture the value in the closure, and drop the response when the captured value no longer equals the ref's current value.
    - Module-level constants `PALETTE_DEBOUNCE_MS = 200` and `PALETTE_MIN_QUERY = 2`.
    - One `useEffect` keyed on `[query, isOpen, state.catalogDir]`: bail when closed; when the trimmed query is shorter than `PALETTE_MIN_QUERY`, clear results and total to the hint branch without issuing any call; otherwise `setTimeout` for `PALETTE_DEBOUNCE_MS`, and in the timer call `wailsAPI.searchIndexed(trimmedQuery, state.catalogDir)`. The effect's cleanup clears the pending timer. On a failed result, treat it as zero results — there is no distinct error surface in this palette.
    - Read `catalogDir` from `useAppContext()`. When it is null, render the hint branch and issue no call.

    Render, per UI-SPEC: an outer scrim div `.ws-palette-scrim` whose `onClick` calls `onClose`, containing a panel div `.ws-palette-panel` with `role="dialog"`, `aria-modal="true"`, `aria-label="Search every catalog"`, and an `onClick` that stops propagation so a click inside never closes. Inside the panel: a `.ws-palette-input-row` holding a 15×15 magnifier SVG with `stroke="var(--ac)"` and `strokeWidth={1.6}` (the same circle+line shape the toolbar uses at 13px/`--dm`), the `.ws-palette-input` bare input, and a right-aligned 11px mono `--fn` readout; then a `.ws-palette-list` scroll region rendering one `.ws-palette-row` per result with the basename, the dimmed full path, the catalog name, and `formatBytes(result.size)`. Escape-to-close, focus trap, and scroll lock are deliberately **not** implemented here — they arrive as the shared hook in 24-03. For this slice, wire Escape inline in the same guard-while-open idiom `WorkspaceShell` already uses for the drawer, and leave a one-line comment saying 24-03 replaces it with the shared hook.

    The input placeholder is computed live, never a static demo string: `Search {formatCount(filesIndexed)} files across {catalogCount} catalogs…`, derived from `state.catalogs` exactly the way `StatusBar` derives them (skip catalogs whose `fileCount` is null rather than counting them as zero). "catalogs" stays unconditionally plural.

    Add the palette shell rules to `frontend/src/workspace.css`, appended after the `.ws-backdrop` rule: `.ws-palette-scrim` is `position: absolute; inset: 0; background: rgba(4,6,9,.62); display: flex; justify-content: center; padding-top: 15vh; z-index: var(--z-overlay)`. Use the existing `--z-overlay` token; do not declare a new z-index variable or a raw numeric z-index. `.ws-palette-panel` is `width: min(640px, calc(100vw - 32px)); max-height: min(520px, 70vh); border-radius: 12px; background: var(--p); border: 1px solid var(--l); box-shadow: 0 30px 70px rgba(0,0,0,.6); display: flex; flex-direction: column; overflow: hidden`. `.ws-palette-input-row` is `height: 50px; flex: none; display: flex; align-items: center; gap: 9px; padding: 0 14px; border-bottom: 1px solid var(--l)`. `.ws-palette-input` is `flex: 1; min-width: 0; background: transparent; border: none; outline: none; color: var(--tx); font-size: 14px` with the mono family. `.ws-palette-list` is `flex: 1; overflow-y: auto; min-height: 0; padding: 6px 0`. `.ws-palette-row` is `display: flex; align-items: center; gap: 8px; padding: var(--hp); cursor: pointer` with a `:hover` background of `var(--hov)`. The panel gets a 100ms scrim fade and no movement — no slide or scale on the panel itself.

    In `WorkspaceShell.tsx`: add `const [paletteOpen, setPaletteOpen] = useState(false)`. Add a `useEffect` with an empty dependency array registering a `window` `keydown` listener that fires on `(event.metaKey || event.ctrlKey) && event.key.toLowerCase() === 'k'`, calls `event.preventDefault()` unconditionally so no webview or browser default can engage on any platform, and then opens the palette. The listener must still fire while the rail filter input has focus — do not add an input-focus exclusion. Guard the open with a functional state update or an early return so a second press while already open is a no-op rather than a re-open that would discard the query in progress. Render `<CommandPalette isOpen={paletteOpen} onClose={() => setPaletteOpen(false)} />` as a sibling of the existing details-drawer block, after `ws-grid` and before `StatusBar`.

    In `Toolbar.tsx`: add `onOpenSearch: () => void` to `ToolbarProps` and pass it as the `.ws-search` button's `onClick`. Change nothing else about that button — not its classes, not its inline styles, not its `aria-label`, not the ⌘K badge. Thread the callback down from `WorkspaceShell` the same way `onToggleDetails` already is; do not read context from inside `Toolbar`.

    Do not cap or slice the results array on the frontend — the array arriving from Go is already the capped set, and re-capping it here would hide a backend regression.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0 and `cd frontend && npm run build` exits 0.
    - `grep -q 'isOpen: boolean' frontend/src/components/workspace/CommandPalette.tsx` succeeds and `grep -q 'onClose: () => void' frontend/src/components/workspace/CommandPalette.tsx` succeeds.
    - `grep -q 'PALETTE_DEBOUNCE_MS = 200' frontend/src/components/workspace/CommandPalette.tsx` succeeds and `grep -q 'PALETTE_MIN_QUERY = 2' frontend/src/components/workspace/CommandPalette.tsx` succeeds.
    - `grep -q 'requestIdRef' frontend/src/components/workspace/CommandPalette.tsx` succeeds — the stale-response guard exists as a ref, not as a comment.
    - `grep -q 'wailsAPI.searchIndexed' frontend/src/components/workspace/CommandPalette.tsx` succeeds.
    - `grep -c 'slice(0, 50)' frontend/src/components/workspace/CommandPalette.tsx` is `0` — the cap is Go's, never re-applied client-side.
    - `grep -q 'var(--z-overlay)' frontend/src/workspace.css` still succeeds and `grep -cE '^\s*z-index:\s*[0-9]' frontend/src/workspace.css` is `0` — no raw numeric z-index was introduced.
    - `grep -q '.ws-palette-scrim' frontend/src/workspace.css` succeeds and `grep -q 'rgba(4, *6, *9, *\.62)' frontend/src/workspace.css` succeeds.
    - `grep -q 'onOpenSearch' frontend/src/components/workspace/Toolbar.tsx` succeeds, and the button's existing markup survives untouched: `grep -c 'className="no-drag ws-search"' frontend/src/components/workspace/Toolbar.tsx` is `1`, `grep -c 'aria-label="Search every catalog"' frontend/src/components/workspace/Toolbar.tsx` is `1`, and `grep -c 'maxWidth: 460' frontend/src/components/workspace/Toolbar.tsx` is `1`.
    - `grep -q "metaKey || " frontend/src/components/workspace/WorkspaceShell.tsx` succeeds and `grep -q 'preventDefault' frontend/src/components/workspace/WorkspaceShell.tsx` succeeds.
    - `grep -q '<CommandPalette' frontend/src/components/workspace/WorkspaceShell.tsx` succeeds and the render is unconditional — the component itself decides to render null, so `grep -c 'paletteOpen && <CommandPalette' frontend/src/components/workspace/WorkspaceShell.tsx` is `0`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Both open paths reach one always-mounted palette; typing ≥2 characters issues one debounced, stale-guarded call to the capped Go binding; and real result rows render at the locked overlay layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer JS → Wails binding | Any script running in the app's webview can call `window.go.main.App.SearchIndexed` with arbitrary arguments; the call site in `CommandPalette.tsx` is not a gate. |
| user-typed search term → filesystem walk | A free-text term crosses into Go and drives a directory read plus a JSON parse per catalog file. |
| catalog file contents → rendered DOM | Node names read off disk become React children in the palette rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Information Disclosure | `App.SearchIndexed(searchTerm, catalogDir)` in `app.go` — a new Wails-exposed binding taking an arbitrary directory | medium | mitigate | The binding's parameter surface is byte-identical to the already-shipped `App.SearchCatalogs` and `App.BrowseCatalogs`: one `filepath.Abs` then a single delegation, no new file-read capability and no wider reach. It is registered here as a member of the binding family covered by the **FU-23-A containment sweep assigned to Phase 26** (thread `catalogDir` through and reuse `internal/osutil`'s `containsPath`) — recorded, not re-accepted. Verified by the Task 2 criterion that the binding body contains only `filepath.Abs` plus the delegation. |
| T-24-02 | Information Disclosure | search term → `strings.Contains(strings.ToLower(...))` in `searchInCatalog` | low | accept | The term is never used to construct a path, open a file, or build a command — it is a pure in-memory substring comparison against already-parsed catalog data. `strings.Contains` is a linear scan, not a regex, so there is no ReDoS surface either. Accepting an existing, unchanged characteristic of `SearchCatalogs`. |
| T-24-03 | Denial of Service | live search: one full cross-catalog walk plus a JSON parse per catalog, per settled keystroke | medium | mitigate | Three bounds, all in this plan: the 200ms debounce collapses a burst of keystrokes to one call, the 2-character minimum suppresses the widest-matching queries entirely, and the Go-side 50-cap bounds the marshaled payload (Phase 23 measured 5.83MB of JSON for a single 42,551-node catalog — an uncapped cross-library response is that multiplied by the catalog count). The walk cost itself is unchanged and is a pre-existing property of `SearchCatalogs`. |
| T-24-SC | Tampering | npm / go package installs | n/a | n/a | **No package-manager install occurs in this phase.** `24-RESEARCH.md`'s Package Legitimacy Audit records this explicitly and no task here runs `npm install`, `npm add`, or `go get`. Verifiable: no such command appears in any `<action>` or `<verify>` block in this plan. The legitimacy gate is out of scope rather than waived. |

## Carried obligations (recorded, not re-accepted, not in scope here)

- **T-22-05** — `CatalogModal`'s unsanitized `srcDoc`. Assigned to **Phase 26**. It matters to this plan only as the reason T-24-01 is not rated `low`: an unsanitized srcdoc iframe inheriting the app origin is the one realistic path by which non-first-party script could reach `window.go`. Not fixed here.
- **FU-23-A** — `GetCatalogHtmlPath` / `OpenExternal` lack the containment gate `RevealInFileManager` received. Assigned to **Phase 26**. `App.SearchIndexed` joins that sweep's target list (see T-24-01). Not fixed here.
</threat_model>

<verification>
- `go build ./... && go test ./... -race -count=1` is green.
- `cd frontend && npx tsc --noEmit && npm run build` is green.
- `internal/search/service.go` and `cli/search.go` are byte-unchanged: `git diff --stat -- internal/search/service.go cli/search.go` produces no output.
- The generated bridge carries `SearchIndexed` and `SearchIndexResult`, produced by `wails generate module` and not hand-written.
- Live proof of the end-to-end path is plan 24-02's job and is deliberately not claimed here.
</verification>

<success_criteria>
- A capped cross-catalog search exists in Go with a named cap constant, boundary-tested at 0, 50, and 51 matches, and proven order- and matcher-identical to the walk `cli/search.go` uses.
- `cli/search.go` behaves exactly as before, with a regression test standing guard.
- The Wails bridge exposes the new binding and struct through generated output.
- ⌘K and the toolbar search field both open one always-mounted palette overlay at `--z-overlay`, with the input focused.
- Typing ≥2 characters produces real, capped results from real catalogs after a single 200ms-debounced, stale-guarded call.
</success_criteria>

<output>
Create `.planning/phases/24-cmd-k-command-palette/24-01-SUMMARY.md` when done.
</output>
