---
phase: quick-260327-nh6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wails.json
  - app_test.go
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All source files reference version 2.2.1, not 2.2.0"
    - "The v2.2.0 GitHub release and tag are deleted"
    - "A v2.2.1 tag exists on the HEAD commit and is pushed to origin"
    - "The release.yml workflow is triggered and building artifacts"
  artifacts:
    - path: "wails.json"
      provides: "productVersion set to 2.2.1"
      contains: '"productVersion": "2.2.1"'
    - path: "app_test.go"
      provides: "Test comment references 2.2.1"
      contains: "2.2.1"
    - path: "README.md"
      provides: "Header and footer reference v2.2.1"
      contains: "StorCat v2.2.1"
  key_links:
    - from: "v2.2.1 git tag"
      to: ".github/workflows/release.yml"
      via: "tag push triggers on: push: tags: ['v*.*.*']"
      pattern: "release.yml builds all platforms and creates draft GitHub release"
---

<objective>
Bump version from 2.2.0 to 2.2.1, clean up the buggy v2.2.0 release/tag, push the fix, and trigger a new release build.

Purpose: The v2.2.0 tag points to buggy code (wrong title bar, wrong version). HEAD has the fix. We need to ship a clean v2.2.1 release.
Output: Pushed v2.2.1 tag triggers release.yml which builds all platforms and creates a draft GitHub release.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@wails.json
@app_test.go (line 134)
@README.md (lines 1, 471)
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version references from 2.2.0 to 2.2.1</name>
  <files>wails.json, app_test.go, README.md</files>
  <action>
Update version strings in these files (source code only, not planning docs):

1. `wails.json` line 16: Change `"productVersion": "2.2.0"` to `"productVersion": "2.2.1"`
2. `app_test.go` line 134: Change the test comment from `"2.2.0"` to `"2.2.1"`
3. `README.md` line 1: Change `# StorCat v2.2.0` to `# StorCat v2.2.1`
4. `README.md` line 471: Change `**StorCat v2.2.0**` to `**StorCat v2.2.1**`

NOTE: `main.go` window title is just "StorCat" (no version) and `frontend/index.html` title is just "StorCat" (no version) — no changes needed there.

Also stage and include any uncommitted changes (README.md update, deleted `.planning/v2.2.0-MILESTONE-AUDIT.md`).

Commit all changes with message: `fix: bump version to 2.2.1 for clean release`
  </action>
  <verify>
    <automated>grep -n "2.2.1" wails.json app_test.go README.md | head -10 && grep -c "2.2.0" wails.json app_test.go README.md</automated>
  </verify>
  <done>All four version references show 2.2.1. No 2.2.0 references remain in wails.json, app_test.go, or README.md (line 1 and 471). Changes committed.</done>
</task>

<task type="auto">
  <name>Task 2: Delete v2.2.0 release/tag, push code, create and push v2.2.1 tag</name>
  <files></files>
  <action>
Execute these steps in order:

1. Push the commit(s) to origin/main: `git push origin main`
2. Delete the existing v2.2.0 GitHub release: `gh release delete v2.2.0 --yes --cleanup-tag`
   (This deletes both the release and the remote tag. The `--cleanup-tag` flag removes the tag from the remote.)
3. Delete the local v2.2.0 tag: `git tag -d v2.2.0`
4. Create the v2.2.1 tag on HEAD: `git tag v2.2.1`
5. Push the tag: `git push origin v2.2.1`
6. Verify the release.yml workflow was triggered: `gh run list --workflow=release.yml --limit=1`

The release.yml workflow (triggered by tag push `v*.*.*`) will:
- Build macOS universal, Windows amd64, Linux amd64, Linux arm64
- Create a DRAFT GitHub release with all artifacts
- User must manually publish the draft to trigger distribute.yml (homebrew/winget updates)
  </action>
  <verify>
    <automated>gh run list --workflow=release.yml --limit=1 && git tag -l 'v2.2*' && gh release list --limit=3</automated>
  </verify>
  <done>v2.2.0 release and tag are gone. v2.2.1 tag exists locally and on remote. release.yml workflow is running (or completed). User informed they need to publish the draft release when builds complete.</done>
</task>

</tasks>

<verification>
- `grep "2.2.1" wails.json` shows productVersion 2.2.1
- `git tag -l 'v2.2*'` shows only v2.2.1
- `gh run list --workflow=release.yml --limit=1` shows a recent run for v2.2.1
- `gh release list --limit=3` shows no v2.2.0 release
</verification>

<success_criteria>
- All source files reference 2.2.1
- v2.2.0 GitHub release and tag deleted
- v2.2.1 tag pushed and release.yml building
- User knows to publish the draft release when builds complete
</success_criteria>

<output>
After completion, create `.planning/quick/260327-nh6-bump-version-to-2-2-1-push-bugfix-and-cr/260327-nh6-SUMMARY.md`
</output>
